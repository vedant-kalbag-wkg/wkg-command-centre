-- Query:
SELECT
      "hotel_groups"."id" AS group_id,
      "hotel_groups"."name" AS group_name,
      COALESCE(SUM("sales_records"."gross_amount"), $4) AS revenue,
      COUNT(*)::text AS transactions,
      COUNT(DISTINCT "sales_records"."location_id")::text AS hotel_count
    FROM "sales_records"
    INNER JOIN "locations" ON "sales_records"."location_id" = "locations"."id"
    INNER JOIN "location_hot

-- Plan:
EXPLAIN failed: unterminated quoted identifier at or near ""location_hot"
