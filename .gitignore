# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# env files
.env.local
.env.test

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage
/playwright-output/
/playwright-report/
/test-results/

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*
!.env.example

# tooling / agent state
.claude/
.planning/
.worktrees/

# auth credentials
auth.json

# Phase 7 — sales seed data is large (~30MB per CSV), operator-supplied;
# never commit. Drop into seed_data/ locally for `scripts/v2-wipe-and-reseed.ts`.
seed_data/

# Phase 7 — local UAT-branch credentials. Pinned per-branch in .env.uat.
.env.uat

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts
.playwright-cli/
*.png

# Local data dumps + browser/session artifacts (root only)
/*.csv
/portfolio-*.yml
/filter-bar.yml
