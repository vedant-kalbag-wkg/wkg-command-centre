import { test } from "@playwright/test";

/**
 * Installation CRUD tests — Wave 0 stubs.
 *
 * These will be implemented in Plan 03-02 once CRUD pages exist.
 */

test.describe("Installation CRUD", () => {
  test.fixme("can create an installation with name and dates", async ({ page }) => {
    void page;
  });

  test.fixme("can view installation detail with milestones", async ({ page }) => {
    void page;
  });

  test.fixme("can delete an installation", async ({ page }) => {
    void page;
  });
});
