---
phase: 04.1-data-migration-quality-correctness-inserted
plan: "05"
subsystem: ui
tags: [products, sidebar, tanstack-table, drizzle, server-actions, dialog, tooltip]

dependency_graph:
  requires:
    - phase: 04.1-01
      provides: schema.ts (base tables — products/providers/locationProducts added here as Rule 3 deviation)
  provides:
    - products, providers, locationProducts Drizzle schema tables
    - Package icon + Products nav item in app-sidebar.tsx
    - listProducts, getProductDetail, createProduct, deleteProduct server actions
    - /products page (server component, calls listProducts)
    - /products/[productId] detail page (per-hotel availability breakdown)
    - ProductsTable client component (TanStack Table, empty state, add/delete dialogs, row-click)
  affects:
    - Any plan adding per-hotel product config (uses locationProducts table)
    - Plan 04.1-06 (Kiosk Config Groups — adds another sidebar tab)

tech_stack:
  added: []
  patterns:
    - ProductsTable uses createColumnHelper from TanStack Table v8 (cleaner than ColumnDef array)
    - TooltipTrigger uses render prop pattern (no asChild in base-ui) per established STATE.md decision
    - Add/Delete dialogs in same client component as table (no separate dialog component files)
    - Server component page calls listProducts, passes array to client table — established kiosk/location pattern

key_files:
  created:
    - src/app/(app)/products/actions.ts
    - src/app/(app)/products/page.tsx
    - src/app/(app)/products/[productId]/page.tsx
    - src/components/products/products-table.tsx
  modified:
    - src/components/layout/app-sidebar.tsx
    - src/db/schema.ts

key_decisions:
  - "products/providers/locationProducts tables added to schema.ts as Rule 3 deviation — CONTEXT.md referenced them as existing but they were never added by prior plans"
  - "Add Product button placed inside ProductsTable component (not AppShell action prop) — ProductsTable is client component; avoids needing a separate client wrapper for server page action prop"
  - "TooltipTrigger uses render prop (not asChild) — base-ui Tooltip has no asChild; consistent with Phase 02/03 PopoverTrigger pattern"

requirements-completed: [MIGR-14]

duration: 3min
completed: 2026-04-01
---

# Phase 04.1 Plan 05: Products Tab — Sidebar Nav, Server Actions, Table, and Detail Page

**Products first-class sidebar entity: Package nav item, /products TanStack Table with 4 columns + empty state + add/delete dialogs + row-click drill-down to per-hotel availability page.**

## Performance

- **Duration:** ~3 min
- **Started:** 2026-04-01T17:19:24Z
- **Completed:** 2026-04-01T17:22:58Z
- **Tasks:** 2
- **Files modified:** 6

## Accomplishments

- Added `products`, `providers`, and `locationProducts` tables to `src/db/schema.ts` (pulled from Phase 4 commit `9f1497c` — they existed on the phase-04 branch but were never merged into the 04.1 branch)
- Sidebar Products entry (Package icon, `/products` href) inserted after Installations
- Four server actions: `listProducts` (N+1 query per plan spec), `getProductDetail`, `createProduct`, `deleteProduct`
- `/products` and `/products/[productId]` routes registered in Next.js build
- `ProductsTable`: 4 columns, empty state, Add Product dialog, Delete confirmation dialog (destructive), row-click navigation, icon-only actions with `aria-label` + Tooltip

## Task Commits

1. **Task 1: Products sidebar nav, schema tables, server actions, page, detail page** - `e49a05d` (feat)
2. **Task 2: ProductsTable component** - `a49b4d5` (feat)

## Files Created/Modified

- `src/db/schema.ts` — Added products, providers, locationProducts pgTable definitions
- `src/components/layout/app-sidebar.tsx` — Added Package import + Products navItem
- `src/app/(app)/products/actions.ts` — listProducts, getProductDetail, createProduct, deleteProduct
- `src/app/(app)/products/page.tsx` — Server component calling listProducts + ProductsTable
- `src/app/(app)/products/[productId]/page.tsx` — Detail page with hotel breakdown table + availability badges
- `src/components/products/products-table.tsx` — TanStack Table v8 client component

## Decisions Made

- **Schema tables as Rule 3 deviation:** `CONTEXT.md` referenced `products/providers/locationProducts` as existing in `schema.ts`, but they were missing from the 04.1 branch. Added them from Phase 4 commit `9f1497c` definitions.
- **Add Product button in ProductsTable:** The plan says to pass via `AppShell action` prop but `ProductsTable` is a client component — keeping the button inside the component avoids needing a separate `AddProductButton` client component wrapper for the server page.
- **TooltipTrigger render prop:** `base-ui` Tooltip has no `asChild` — used `render={<Button ... />}` pattern consistent with established base-ui component patterns.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] products/providers/locationProducts tables missing from schema.ts**
- **Found during:** Task 1 (actions.ts creation)
- **Issue:** `actions.ts` imports `products`, `providers`, `locationProducts` from `@/db/schema` but none existed. CONTEXT.md said they existed; they were on `gsd/phase-04-data-migration` branch but never merged into 04.1.
- **Fix:** Added all three table definitions to `src/db/schema.ts` using the exact definitions from Phase 4 commit `9f1497c`.
- **Files modified:** `src/db/schema.ts`
- **Committed in:** `e49a05d` (Task 1 commit)

**2. [Rule 1 - Bug] TooltipTrigger has no asChild prop in base-ui**
- **Found during:** Task 2 TypeScript compile
- **Issue:** Used `asChild` on `TooltipTrigger`; base-ui Tooltip API does not support `asChild`. TypeScript error TS2322.
- **Fix:** Changed to `render={<Button ... />}` prop pattern per established base-ui component pattern (same as `SidebarMenuButton`, `DialogClose`, etc. in codebase).
- **Files modified:** `src/components/products/products-table.tsx`
- **Committed in:** `a49b4d5` (Task 2 commit)

---

**Total deviations:** 2 auto-fixed (1 Rule 3 blocking, 1 Rule 1 bug)
**Impact on plan:** Both fixes necessary for compilation and functionality. No scope creep.

## Issues Encountered

None beyond the deviations documented above.

## Known Stubs

None — all connections are wired. The products page calls `listProducts()` from the DB; the detail page calls `getProductDetail()`; `createProduct`/`deleteProduct` write to DB. The products/providers/locationProducts tables need a `drizzle-kit push` to exist in the Supabase DB at runtime, but that's an infrastructure step not a code stub.

## Next Phase Readiness

- `/products` and `/products/[productId]` routes are live
- Products CRUD (add/delete) is wired — edit is stubbed with a toast (future plan can add inline edit)
- `locationProducts` table enables per-hotel configuration on the Locations detail page
- Ready for Plan 04.1-06 (Kiosk Config Groups tab)

---
*Phase: 04.1-data-migration-quality-correctness-inserted*
*Completed: 2026-04-01*

## Self-Check: PASSED

| Check | Result |
|-------|--------|
| src/db/schema.ts has products/providers/locationProducts | FOUND |
| src/components/layout/app-sidebar.tsx has Products nav item | FOUND |
| src/app/(app)/products/actions.ts has listProducts | FOUND |
| src/app/(app)/products/actions.ts has createProduct | FOUND |
| src/app/(app)/products/actions.ts has deleteProduct | FOUND |
| src/app/(app)/products/actions.ts has getProductDetail | FOUND |
| src/app/(app)/products/page.tsx has ProductsTable | FOUND |
| src/app/(app)/products/[productId]/page.tsx exists | FOUND |
| src/components/products/products-table.tsx exists | FOUND |
| Commit e49a05d exists | VERIFIED |
| Commit a49b4d5 exists | VERIFIED |
| TypeScript compiles without errors | VERIFIED (0 errors) |
| Next.js build success with /products route | VERIFIED |
