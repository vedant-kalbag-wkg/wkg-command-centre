---
phase: 01-foundation
plan: 03
type: execute
wave: 3
depends_on:
  - 01-02
files_modified:
  - src/app/(app)/settings/users/page.tsx
  - src/components/admin/invite-user-dialog.tsx
  - src/components/admin/user-table.tsx
  - src/components/admin/change-role-dialog.tsx
  - src/components/admin/deactivate-dialog.tsx
  - src/app/(app)/settings/users/actions.ts
  - src/lib/rbac.ts
  - src/components/layout/app-sidebar.tsx
  - tests/admin/invite-user.spec.ts
  - tests/admin/change-role.spec.ts
  - tests/admin/deactivate-user.spec.ts
  - tests/rbac/sensitive-fields.spec.ts
  - tests/rbac/viewer-controls.spec.ts
autonomous: true
requirements:
  - AUTH-04
  - AUTH-05

must_haves:
  truths:
    - "Admin can invite a new user by email with a role (Admin/Member/Viewer)"
    - "Invited user receives a password-set email (uses password reset flow as invite)"
    - "Admin can see a list of all users with name, email, role, status, and join date"
    - "Admin can change a user's role with a confirmation dialog"
    - "Admin can deactivate a user (ban) — no permanent deletion"
    - "Deactivated user row shows at 0.5 opacity with Inactive badge"
    - "Viewer role cannot see banking details or contract values — fields are redacted"
    - "Viewer sees disabled controls with tooltip explaining permission restriction"
    - "Non-admin users cannot access user management actions (invite, role change, deactivate)"
  artifacts:
    - path: "src/app/(app)/settings/users/page.tsx"
      provides: "User management page"
      contains: "Users"
    - path: "src/components/admin/invite-user-dialog.tsx"
      provides: "Invite user modal"
      contains: "Send invite"
    - path: "src/components/admin/user-table.tsx"
      provides: "User list table"
      contains: "Role"
    - path: "src/components/admin/change-role-dialog.tsx"
      provides: "Role change confirmation"
      contains: "Change role"
    - path: "src/components/admin/deactivate-dialog.tsx"
      provides: "Deactivate user confirmation"
      contains: "Deactivate user"
    - path: "src/app/(app)/settings/users/actions.ts"
      provides: "Server actions for user management"
      contains: "inviteUser"
    - path: "src/lib/rbac.ts"
      provides: "Role-based access helpers"
      exports: ["requireRole", "canAccessSensitiveFields", "isAdmin"]
  key_links:
    - from: "src/app/(app)/settings/users/actions.ts"
      to: "src/lib/auth.ts"
      via: "auth.api.createUser + auth.api.requestPasswordReset"
      pattern: "auth\\.api\\.createUser"
    - from: "src/lib/rbac.ts"
      to: "src/lib/auth.ts"
      via: "session role check"
      pattern: "session\\.user\\.role"
    - from: "src/components/admin/user-table.tsx"
      to: "src/app/(app)/settings/users/actions.ts"
      via: "server action calls"
      pattern: "inviteUser|changeRole|deactivateUser"
---

<objective>
Build the admin user management interface (Settings > Users) and role-based access control system: invite users, change roles, deactivate accounts, and enforce sensitive field restrictions for the Viewer role.

Purpose: AUTH-04 (admin manages users) and AUTH-05 (sensitive field RBAC) complete the Foundation phase's access control requirements. The invite-only flow and role system are prerequisites for all downstream features.
Output: Working user management page with invite, role change, deactivate flows, and RBAC helpers for sensitive field redaction.
</objective>

<execution_context>
@/Users/vedant/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vedant/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-UI-SPEC.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
@.planning/phases/01-foundation/01-02-SUMMARY.md

<interfaces>
<!-- From Plan 01-02 outputs that this plan depends on -->

From src/lib/auth.ts:
```typescript
export const auth: BetterAuthInstance;
// auth.api.createUser({ body: { email, role, name } })
// auth.api.listUsers()
// auth.api.setRole({ body: { userId, role } })
// auth.api.banUser({ body: { userId, banReason } })
// auth.api.requestPasswordReset({ body: { email, redirectTo } })
```

From src/lib/auth-client.ts:
```typescript
export const authClient: BetterAuthClient;
export const signIn, signOut, useSession, getSession;
// useSession() returns { data: { user: { id, name, email, role } }, isPending }
```

From src/lib/email.ts:
```typescript
export function sendPasswordResetEmail({ to, resetUrl }): Promise<void>;
export function sendInviteEmail({ to, resetUrl }): Promise<void>;
```

From src/app/(app)/layout.tsx:
```typescript
// Session check: auth.api.getSession() — redirects to /login if no session
// Passes session.user to AppSidebar
```

From src/db/schema.ts:
```typescript
// locations table has: bankingDetails (jsonb), contractValue (numeric),
// contractTerms (text), contractDocuments (jsonb) — these are SENSITIVE fields
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Settings > Users page with server actions (invite, role change, deactivate) and RBAC helpers</name>
  <files>
    src/app/(app)/settings/users/page.tsx,
    src/app/(app)/settings/users/actions.ts,
    src/components/admin/invite-user-dialog.tsx,
    src/components/admin/user-table.tsx,
    src/components/admin/change-role-dialog.tsx,
    src/components/admin/deactivate-dialog.tsx,
    src/lib/rbac.ts
  </files>
  <read_first>
    .planning/phases/01-foundation/01-UI-SPEC.md (Settings > Users Page contract, Invite user dialog, Role badges, Disabled States, Copywriting Contract)
    .planning/phases/01-foundation/01-CONTEXT.md (User Management decisions, Role Permissions decisions)
    .planning/phases/01-foundation/01-RESEARCH.md (Pattern 5: Invite-Only Flow)
    src/lib/auth.ts (verify admin plugin API: createUser, listUsers, setRole, banUser)
    src/lib/auth-client.ts (verify useSession export)
    src/lib/email.ts (verify sendInviteEmail export)
    src/components/ui/dialog.tsx (shadcn dialog API)
    src/components/ui/table.tsx (shadcn table API)
    src/components/ui/badge.tsx (shadcn badge API)
    src/components/ui/dropdown-menu.tsx (shadcn dropdown API)
    src/components/ui/tooltip.tsx (shadcn tooltip API)
  </read_first>
  <action>
1. Create `src/lib/rbac.ts` — role-based access control helpers:
   ```typescript
   import { auth } from "@/lib/auth";
   import { headers } from "next/headers";

   export type Role = "admin" | "member" | "viewer";

   export async function getSessionOrThrow() {
     const session = await auth.api.getSession({ headers: await headers() });
     if (!session) throw new Error("Unauthorized");
     return session;
   }

   export async function requireRole(...roles: Role[]) {
     const session = await getSessionOrThrow();
     if (!roles.includes(session.user.role as Role)) {
       throw new Error("Forbidden");
     }
     return session;
   }

   export function isAdmin(role: string): boolean {
     return role === "admin";
   }

   export function canAccessSensitiveFields(role: string): boolean {
     // LOCKED DECISION: Admin + Member can see/edit; Viewer sees redacted
     return role === "admin" || role === "member";
   }

   export function redactSensitiveFields<T extends Record<string, unknown>>(data: T, role: string): T {
     if (canAccessSensitiveFields(role)) return data;
     return {
       ...data,
       bankingDetails: null,
       contractValue: null,
       contractTerms: null,
       contractDocuments: null,
     };
   }
   ```

2. Create `src/app/(app)/settings/users/actions.ts` — Server Actions ("use server"):

   **`inviteUser(email: string, role: Role)`:**
   - Calls `requireRole("admin")` — only admins can invite
   - Validates email format with zod
   - Calls `auth.api.createUser({ body: { email, role, name: "" }, headers: await headers() })`
   - Then calls `auth.api.requestPasswordReset({ body: { email, redirectTo: "/set-password" }, headers: await headers() })`
   - The password reset email IS the invite email (per RESEARCH.md Pattern 5)
   - Returns `{ success: true }` or `{ error: "message" }`

   **`listUsers()`:**
   - Calls `requireRole("admin")` — only admins see user list
   - Calls Better Auth admin API to list all users (may need `auth.api.listUsers({ headers: await headers() })`)
   - Returns array of `{ id, name, email, role, banned, createdAt }`

   **`changeUserRole(userId: string, newRole: Role)`:**
   - Calls `requireRole("admin")`
   - Validates role is one of "admin", "member", "viewer"
   - Calls `auth.api.setRole({ body: { userId, role: newRole }, headers: await headers() })`
   - Returns `{ success: true }` or `{ error: "message" }`

   **`deactivateUser(userId: string)`:**
   - Calls `requireRole("admin")`
   - Calls `auth.api.banUser({ body: { userId, banReason: "Deactivated by admin" }, headers: await headers() })`
   - LOCKED DECISION: deactivate only, no permanent deletion — records preserved for audit trail
   - Returns `{ success: true }` or `{ error: "message" }`

3. Create `src/components/admin/user-table.tsx` — Client Component:
   - Renders shadcn Table with columns: Name, Email, Role (Badge), Status, Joined, Actions
   - Role badges per UI-SPEC:
     - Admin: `bg-[#121212] text-white` — 6px radius, 6px/12px padding
     - Member: `bg-[#F4F4F4] text-[#121212]` — 6px radius, 6px/12px padding
     - Viewer: `bg-[#E5F1F9] text-[#575A5C]` — 6px radius, 6px/12px padding
   - Deactivated users: row at 0.5 opacity, status badge "Inactive"
   - Active users: status "Active" (no badge, just text)
   - Joined column: formatted date
   - Actions column: DropdownMenu with:
     - "Change role" menu item
     - "Deactivate" menu item — `text-[#F41E56]` destructive colour
   - Actions dropdown only visible if current user is admin
   - If current user is NOT admin: show disabled action controls with Tooltip "You don't have permission to manage users"
   - Empty state: "No users yet. Invite your first team member to get started."

4. Create `src/components/admin/invite-user-dialog.tsx` — Client Component:
   - shadcn Dialog, 480px max-width
   - Title: "Invite user"
   - react-hook-form + zod: email (required, email format), role (required, select from Admin/Member/Viewer)
   - Email input + label
   - Role select dropdown (shadcn Select or DropdownMenu): Admin, Member, Viewer
   - Submit: "Send invite" — Azure fill
   - Cancel button below CTA
   - On submit: call `inviteUser` server action
   - On success: close dialog, show Sonner toast "Invite sent to {email}", refresh user list
   - On error: show toast with error message
   - Loading state: button shows Loader2 spinner, disabled

5. Create `src/components/admin/change-role-dialog.tsx` — Client Component:
   - shadcn Dialog
   - Title: "Change role"
   - Body: "Change {name}'s role to {role}? This will update their permissions immediately." (per Copywriting Contract)
   - Role select: dropdown with Admin/Member/Viewer, pre-selected to current role
   - Confirm CTA: "Change role" — Azure fill
   - Cancel button
   - On confirm: call `changeUserRole` server action
   - On success: close dialog, toast "{name}'s role updated to {role}", refresh list

6. Create `src/components/admin/deactivate-dialog.tsx` — Client Component:
   - shadcn Dialog
   - Title: "Deactivate user"
   - Body: "Deactivate {name}? They will lose access immediately. Their data and audit history will be preserved." (per Copywriting Contract)
   - Confirm CTA: "Deactivate user" — destructive red (#F41E56) background, white text
   - Cancel button
   - On confirm: call `deactivateUser` server action
   - On success: close dialog, toast "{name} has been deactivated", refresh list

7. Create `src/app/(app)/settings/users/page.tsx`:
   - Server Component that fetches user list via `listUsers()` action
   - Check session role — if not admin, show the page but with controls disabled
   - Page title: "Users" (per content header convention)
   - "Invite user" button top-right: Azure fill, 40px height, Lucide `UserPlus` icon + "Invite user" label
   - If not admin: "Invite user" button disabled with Tooltip "You don't have permission to invite users"
   - Renders UserTable with users data
   - Includes InviteUserDialog (triggered by Invite button)
   - LOCKED DECISION: Non-admin users see disabled controls with tooltips (not hidden, not click-to-error)
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/rbac.ts exports `requireRole`, `canAccessSensitiveFields`, `isAdmin`, `redactSensitiveFields`
    - src/lib/rbac.ts contains `role === "admin" || role === "member"` in canAccessSensitiveFields
    - src/app/(app)/settings/users/actions.ts contains "use server" directive
    - src/app/(app)/settings/users/actions.ts contains `auth.api.createUser` and `auth.api.requestPasswordReset` in inviteUser function
    - src/app/(app)/settings/users/actions.ts contains `auth.api.banUser` in deactivateUser (NOT delete)
    - src/components/admin/invite-user-dialog.tsx contains "Invite user" and "Send invite"
    - src/components/admin/user-table.tsx contains role badge colours: `#121212`, `#F4F4F4`, `#E5F1F9`
    - src/components/admin/change-role-dialog.tsx contains "Change role" and "permissions immediately"
    - src/components/admin/deactivate-dialog.tsx contains "Deactivate user" and "audit history will be preserved"
    - src/app/(app)/settings/users/page.tsx contains `UserPlus` icon import and "Invite user"
    - `npm run build` exits with code 0
  </acceptance_criteria>
  <done>Settings > Users page with invite, role change, and deactivate flows working. RBAC helpers exported for sensitive field redaction. All admin dialogs match UI-SPEC copywriting contract. Non-admin users see disabled controls with permission tooltips.</done>
</task>

<task type="auto">
  <name>Task 2: Write Playwright E2E tests for admin user management and RBAC sensitive field restrictions</name>
  <files>
    tests/admin/invite-user.spec.ts, tests/admin/change-role.spec.ts,
    tests/admin/deactivate-user.spec.ts, tests/rbac/sensitive-fields.spec.ts,
    tests/rbac/viewer-controls.spec.ts
  </files>
  <read_first>
    .planning/phases/01-foundation/01-VALIDATION.md (Per-Task Verification Map, Wave 0 Requirements)
    .planning/phases/01-foundation/01-UI-SPEC.md (Settings > Users Page, Disabled States, Copywriting Contract)
    tests/auth/setup.ts (shared auth helpers from Plan 01-02)
    tests/auth/login.spec.ts (test patterns established in Plan 01-02)
    src/app/(app)/settings/users/page.tsx (page structure to test against)
    src/components/admin/invite-user-dialog.tsx (dialog elements to assert)
    src/lib/rbac.ts (RBAC logic to verify via E2E)
  </read_first>
  <action>
1. Create `tests/admin/invite-user.spec.ts`:
   - Test: "admin can open invite user dialog"
     - Sign in as admin (use auth setup helper)
     - Navigate to /settings/users
     - Click "Invite user" button
     - Verify dialog opens with "Invite user" title, email input, role select, "Send invite" button
   - Test: "admin can invite a new member user"
     - Open invite dialog, fill email "newmember@test.com", select "Member" role
     - Click "Send invite"
     - Verify toast "Invite sent to newmember@test.com" appears
     - Verify new user appears in the table with role "Member"
   - Test: "invite shows error for invalid email"
     - Open invite dialog, submit with empty email
     - Verify inline validation error appears

2. Create `tests/admin/change-role.spec.ts`:
   - Test: "admin can change a user's role"
     - Sign in as admin, navigate to /settings/users
     - Click actions dropdown on a non-admin user
     - Click "Change role"
     - Verify confirmation dialog with text about permissions
     - Select new role, click "Change role" button
     - Verify toast with role updated message
     - Verify table shows updated role badge

3. Create `tests/admin/deactivate-user.spec.ts`:
   - Test: "admin can deactivate a user"
     - Sign in as admin, navigate to /settings/users
     - Click actions dropdown, click "Deactivate"
     - Verify confirmation dialog with text "audit history will be preserved"
     - Click "Deactivate user" (destructive button)
     - Verify toast "{name} has been deactivated"
     - Verify user row shows 0.5 opacity and "Inactive" status

4. Create `tests/rbac/sensitive-fields.spec.ts`:
   NOTE: Sensitive fields (banking details, contract values) are on Location records (Phase 2). For Phase 1, test the RBAC helper logic via a minimal API endpoint or test the Settings > Users page access restrictions.
   - Test: "viewer cannot access admin API endpoints"
     - Sign in as viewer
     - POST to invite/role-change/deactivate endpoints
     - Verify 403 or error response
   - Test: "admin and member roles are distinguished from viewer for sensitive access"
     - Sign in as admin, verify /settings/users loads with action controls
     - Sign in as viewer, verify /settings/users loads with disabled controls

5. Create `tests/rbac/viewer-controls.spec.ts`:
   - Test: "viewer sees invite button disabled with permission tooltip"
     - Sign in as viewer, navigate to /settings/users
     - Verify "Invite user" button is visible but disabled
     - Hover over it, verify tooltip "You don't have permission to invite users"
   - Test: "viewer sees action dropdown disabled"
     - Verify actions column shows disabled state with tooltip

NOTE: These tests require at least 3 seeded users (admin, member, viewer). Extend the seed script or use setup fixtures to create them before test runs. Add a `tests/global-setup.ts` if needed that runs the seed.
  </action>
  <verify>
    <automated>npx playwright test tests/admin/ tests/rbac/ --reporter=line 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - tests/admin/invite-user.spec.ts contains test for "invite user dialog" and "invite a new member user"
    - tests/admin/change-role.spec.ts contains test for "change a user's role" with confirmation dialog assertion
    - tests/admin/deactivate-user.spec.ts contains test for "deactivate a user" with "Deactivate user" button assertion
    - tests/rbac/sensitive-fields.spec.ts contains test verifying viewer cannot call admin endpoints
    - tests/rbac/viewer-controls.spec.ts contains test for disabled button with "permission" tooltip
    - `npx playwright test tests/admin/ tests/rbac/` passes (exit code 0)
  </acceptance_criteria>
  <done>Full Playwright E2E test coverage for AUTH-04 (admin user management: invite, role change, deactivate) and AUTH-05 (RBAC sensitive field restrictions, disabled controls with tooltips for viewer role). All tests green.</done>
</task>

</tasks>

<verification>
- `npm run build` succeeds
- `npx playwright test` — full suite green (all auth + admin + rbac tests)
- Admin can invite user from Settings > Users
- Admin can change a user's role with confirmation
- Admin can deactivate (not delete) a user
- Viewer sees disabled controls with permission tooltips
- RBAC helpers correctly distinguish admin/member (can access) from viewer (redacted)
- All UI copy matches UI-SPEC Copywriting Contract exactly
</verification>

<success_criteria>
- Admin can create and manage user accounts (AUTH-04): invite with email+role, change role, deactivate
- Sensitive fields restricted to authorized roles (AUTH-05): RBAC helpers exported, viewer sees redacted data
- Viewer sees disabled controls with tooltip explanations (not hidden, not click-to-error)
- Deactivate-only policy enforced (no permanent deletion)
- All Playwright E2E tests pass for admin and RBAC flows
- Full Phase 1 Playwright suite green: `npx playwright test` exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md`
</output>
