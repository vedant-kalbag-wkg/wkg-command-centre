import { test } from "@playwright/test";

/**
 * Gantt view tests — Wave 0 stubs.
 *
 * Covers GANTT-01 through GANTT-04 and supporting interactions.
 * Will be implemented in Plan 03-03 once the Gantt component exists.
 */

test.describe("Gantt View", () => {
  test.fixme("GANTT-01: renders Gantt tab with installation bars", async ({ page }) => {
    void page;
  });

  test.fixme("GANTT-02: groups installations by region with collapsible headers", async ({ page }) => {
    void page;
  });

  test.fixme("GANTT-03: shows milestone diamonds on Gantt timeline", async ({ page }) => {
    void page;
  });

  test.fixme("GANTT-04: shows team members in left-side resource column", async ({ page }) => {
    void page;
  });

  test.fixme("pending drag: Apply saves new dates", async ({ page }) => {
    void page;
  });

  test.fixme("pending drag: Discard reverts bar position", async ({ page }) => {
    void page;
  });

  test.fixme("milestone quick-add popover creates milestone", async ({ page }) => {
    void page;
  });
});
