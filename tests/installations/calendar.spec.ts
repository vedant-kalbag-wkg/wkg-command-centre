import { test } from "@playwright/test";

/**
 * Calendar view tests — Wave 0 stubs.
 *
 * Covers CAL-01 and CAL-02.
 * Will be implemented in Plan 03-04 once the Calendar component exists.
 */

test.describe("Calendar View", () => {
  test.fixme("CAL-01: shows installation blocks on calendar", async ({ page }) => {
    void page;
  });

  test.fixme("CAL-01: shows milestone markers on calendar", async ({ page }) => {
    void page;
  });

  test.fixme("CAL-01: shows trial expiry dots on calendar", async ({ page }) => {
    void page;
  });

  test.fixme("CAL-02: filtering by region hides non-matching events", async ({ page }) => {
    void page;
  });

  test.fixme("CAL-02: filtering by status hides non-matching events", async ({ page }) => {
    void page;
  });

  test.fixme("calendar event click opens popover with details", async ({ page }) => {
    void page;
  });
});
