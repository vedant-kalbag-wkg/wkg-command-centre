# Handoff — UI Modernization + Backend Bug Backlog

**Date:** 2026-04-19
**Session duration:** one long session, ~45 UI-modernization commits landed.

## TL;DR

- UI modernization is on `ui/modernize-shadcn-magic`. **Phases 1 + 2 done and green.** Not yet merged to main (per user call) — held because the branch was cut from `phase4/commission-deployment`, not `main`, so a direct merge would pull in 51 unrelated phase4 commits.
- Backend bugs (the `outlet_code = 'TEST'` SQL family, commission page broken, admin user creation w/o email, etc.) live on `phase4/commission-deployment`. A second worktree has been set up at `../wkg-kiosk-tool-phase4` to work on them in parallel without disturbing the UI branch.
- Phase 3 of the UI modernization (sweep remaining pages onto the new kit) is NOT started — deferred until backend bugs are unblocked (so we can actually QA the analytics pages we'd migrate).

## Repo layout after this session

```
/Users/vedant/Work/WeKnowGroup/
├── wkg-kiosk-tool/                  ← primary checkout, branch ui/modernize-shadcn-magic
└── wkg-kiosk-tool-phase4/           ← worktree for backend fixes, branch phase4/commission-deployment
```

Both point to the same `.git` directory; they're different working copies of different branches. Commits made in `wkg-kiosk-tool-phase4/` land on `phase4/commission-deployment` without disturbing the UI branch in `wkg-kiosk-tool/`.

To inspect:
```bash
git -C /Users/vedant/Work/WeKnowGroup/wkg-kiosk-tool worktree list
```

To delete the backend worktree when done:
```bash
git worktree remove /Users/vedant/Work/WeKnowGroup/wkg-kiosk-tool-phase4
```

## Branch topology

```
main
  └── phase4/commission-deployment       (51 commits ahead of main — pre-existing)
        └── ui/modernize-shadcn-magic    (42 commits ahead of phase4 — our UI work)
```

Both branches are independently mergeable to main, but neither has been pushed or PR'd. Decision on merge strategy is deferred.

## UI modernization — WHAT'S DONE

Branch: `ui/modernize-shadcn-magic`. Details in `docs/plans/2026-04-19-ui-modernization-design.md` and `docs/plans/2026-04-19-ui-modernization-plan.md`.

### Phase 1 — Foundation (complete)

- `next-themes` wired, dark-mode semantic tokens added to `src/app/globals.css`.
- Theme toggle button in the top bar.
- Swept **~700 `wk-*` utility class occurrences + ~30 brand hex literals** across **~66 files** to semantic shadcn tokens. Project-wide grep returns zero matches.
- Retired the top-only `app-navbar.tsx` (511 LOC deleted).
- New shell: **left sidebar + top bar** built on shadcn `Sidebar` primitive. Sidebar flips with theme. Mobile converts to `Sheet`.
- Dark-mode overrides for `@svar-ui/react-gantt` and `react-big-calendar`.
- Fixes applied during QA:
  - Sidebar light-mode tokens (was inheriting dark-graphite — fixed to light-grey).
  - Fixed-width tables (`kiosk-table`, `location-table`, `installation-table`) now `max(100%, ${total}px)` so they fill the container.
  - `overflow-x-clip` on `<main>` to preserve `position: sticky` for the analytics filter bar (was broken by `overflow-x-auto`).
  - Half-height separator next to sidebar trigger removed.
  - Analytics filter bar collapsed to a single scrollable row.

### Phase 2 — Component kit + reference pages (complete)

**Primitives refreshed:**
- `button` — `sm` variant present (already was).
- `card` — flat by default, `elevated` variant added.
- `input` / `select` / `textarea` — 36px default height.
- `badge` — `subtle-primary`, `subtle-success`, `subtle-destructive`, `subtle-muted` variants.
- `table` — dense row heights, optional sticky header.
- `breadcrumb` — added via shadcn CLI.

**Net-new components:**
- `layout/page-header.tsx` — `title` + `description` + `count` + `actions` + `toolbar` + `breadcrumb` slots.
- `ui/empty-state.tsx` — icon + title + description + action.
- `ui/data-table.tsx` — TanStack wrapper with sort/filter/pagination state; pagination UI deferred.
- `ui/chart-card.tsx` — wraps recharts with title, action, loading, empty, collapsible.
- `analytics/stat-card.tsx` — KPI card with value, delta, optional sparkline.
- `table/sparkline-cell.tsx` — compact recharts sparkline.

**Reference pages rebuilt:**
- `/kiosks` — `<PageHeader>` with count + description + "Add kiosk" button; segmented view switcher surfacing all 4 views (Table / Kanban / Gantt / Calendar); `<DataTable>` + `<EmptyState>` integration. Playwright 2/2 passing (`tests/kiosks/list.spec.ts`).
- `/analytics/portfolio` — dropped `SectionAccordion`. Now `<PageHeader>` with MoM/YoY toggle + "Active flags" Sheet. 5-card KPI strip (Revenue, Transactions, Avg Basket, Unique Outlets, Unique Products). Six full-width `<ChartCard>`s: High Performer Patterns (top), Daily Trends, Category Performance, Top Products, Hourly Distribution, Outlet Tiers. Playwright 3/3 passing (`tests/analytics/portfolio.spec.ts`).

**Quality gates:**
- `npx tsc --noEmit` clean.
- `npm run lint` — no new regressions (pre-existing issues untouched).
- Playwright smoke green (with `TEST_ADMIN_PASSWORD="Admin123!"`).
- Manual QA walk of light + dark mode completed across both reference pages.

## UI modernization — WHAT'S REMAINING (Phase 3)

Sweep the remaining pages onto the new kit. Per the plan doc's "PR 3.x" structure:

**Kiosk Management side** (~6 PRs):
- `/locations` list
- `/locations/[id]` detail
- `/installations` list
- `/installations/[id]` detail
- `/kiosks/[id]` detail
- `/kiosks/new`
- `/products` list + detail
- `/kiosk-config-groups` list

**Analytics side** (~11 pages):
- `/analytics/heat-map`
- `/analytics/trend-builder`
- `/analytics/hotel-groups`
- `/analytics/regions`
- `/analytics/location-groups`
- `/analytics/maturity`
- `/analytics/pivot-table`
- `/analytics/experiments`
- `/analytics/compare`
- `/analytics/commission` (blocked on A.4 — page is currently broken)
- `/analytics/actions-dashboard`

**Settings side:**
- `/settings` overview
- `/settings/business-events`, `/settings/analytics-presets`, `/settings/outlet-exclusions`, `/settings/thresholds`, `/settings/pipeline-stages`
- `/settings/users`, `/settings/data-import/sales`, `/settings/data-quality`, `/settings/duplicates`, `/settings/audit-log`

**Final Phase 3 housekeeping:**
- Retire `src/components/layout/app-shell.tsx` (per-page wrapper — still used by several pages until they migrate to `<PageHeader>`).
- Wire `<DataTable>` pagination UI when the first consumer needs it.
- Polish follow-ups in `docs/bug-report.md` sections D.1–D.7 (hardcoded `bg-white`, role-badge dark-mode contrast, `hover:bg-primary` no-op, overlay dimming).

**Template per page** (from the plan doc): swap per-page `AppShell` → `PageHeader`; replace ad-hoc empty states with `<EmptyState>`; wrap chart surfaces in `<ChartCard>`; tables use `<DataTable>` where it fits (otherwise keep bespoke); Playwright happy-path test per page.

## BACKEND — WHAT'S REMAINING

All tracked in `docs/bug-report.md`. The below are the unblocking items for Phase 3 QA and ongoing product work.

### High priority

**A.1 — `outlet_code = 'TEST'` SQL family (multiple pages)**
- Pages: `/analytics/heat-map`, `/analytics/maturity`, `/analytics/pivot-table`, `/analytics/portfolio` hourly/tod, potentially others.
- Symptom: "Failed query" banner on each; page shell renders but data doesn't.
- Root cause hypothesis: hardcoded/misconfigured outlet exclusion in the analytics filter layer that always passes `'TEST'` — but no real outlet has that code in the current DB, and the exclusion either doesn't apply cleanly or is a symptom of a deeper filter-layer bug.
- Where to look: `src/lib/analytics/queries.ts`, `src/lib/analytics/filters.ts`, `/settings/outlet-exclusions` table contents, `src/db/seed*.ts` for any `'TEST'` exclusion inserts.

**A.4 — `/analytics/commission` page broken**
- Symptom: not reproducible in detail yet — user reports "the commission analytics doesn't work". Likely a "Failed query" banner, possibly same family as A.1.
- Where to look: `src/app/(app)/analytics/commission/page.tsx`, `src/app/(app)/analytics/commission/actions.ts`, any `src/lib/analytics/commission*`.

**B.1 — Trend builder series-wise filtering no longer works**
- Regressed on an earlier commit (pre-UI-sweep; confirmed not caused by this session's work).
- Likely suspects: `320c391 fix(ui): match original trend builder and pivot table UX`, `e6cade0 feat(analytics): add rolling average seasonality controls`, `4f5998c feat(analytics): add YoY comparison toggle`.
- Where to look: `src/app/(app)/analytics/trend-builder/series-row.tsx`, `src/lib/stores/trend-store.ts`.

### Medium priority

**A.2 — Kiosk config groups import failing** (exact error needs reproduction)
**A.3 — Kiosks missing asset IDs on localhost** (probably seed/Monday-import divergence — check `src/db/seed-kiosks.ts` and `scripts/import-from-monday.ts`)
**B'.1 — Admin user creation without email** (new flow needed — `better-auth` server-side user creation with a password; replaces the email-invite flow for dev/staging or environments without SMTP)

### Feature requests (C.x — backlog, not blocking)

- **C.1** Trend builder weather conditional on single region/city/location group filter.
- **C.2** Hotel Groups page redesign — dropdown filter (name + # hotels + revenue) instead of cards; collapse top panel on selection.
- **C.3** Location Groups — same pattern as C.2.
- **C.4** Regions — keep cards, add "select a region to see its performance metrics" empty-state prompt.
- **C.5** Commission page needs multi-select hotel-name filter.
- **C.6** Heat-map weights user-customizable with 100%-sum validation.
- **C.7** Portfolio — add Low Performer Patterns section (mirror of High Performer Patterns, immediately below it).

## How to parallelize with subagents

**Approach 1 — UI branch stays paused, all work on phase4:**
- Work from `/Users/vedant/Work/WeKnowGroup/wkg-kiosk-tool-phase4/` (branch `phase4/commission-deployment`).
- Dispatch subagents in parallel for each disjoint bug:
  - One for A.1 (analytics queries)
  - One for A.4 (commission page)
  - One for B.1 (trend builder filtering)
  - One for A.3 (kiosks asset IDs — data seeding fix)
  - One for B'.1 (admin user creation)
- Rule: subagents that touch the same file must NOT run concurrently. Group by domain, verify file disjointness before dispatching in parallel.

**Approach 2 — Mixed:**
- Fix the highest-priority backend bug (A.1) on phase4.
- Meanwhile, dispatch UI Phase 3 sweeps on pages that aren't blocked by A.1 (e.g., `/locations`, `/installations`, `/products`, `/kiosk-config-groups`) on `ui/modernize-shadcn-magic`.
- Parallelism ceiling is roughly one controller + N non-conflicting subagents; coordination matters.

**Recommended first wave of parallel subagents:**
1. A.1 — investigate the `outlet_code = 'TEST'` filter in `src/lib/analytics/*`. Reproduce on `/analytics/heat-map`, fix the filter application, verify heatmap + maturity + pivot + portfolio hourly.
2. A.4 — reproduce `/analytics/commission`, diagnose, fix. Likely related to A.1 but isolate.
3. B'.1 — implement admin user creation with a default password via `better-auth` server API. New dialog in `/settings/users` + server action. Probably touches `src/components/admin/*` and `src/lib/auth.ts`.
4. A.3 — extend `src/db/seed-kiosks.ts` to populate asset IDs (or document the Monday-import script as the required localhost bootstrap step).

Each subagent should write to `docs/bug-report.md` to move its entry from "Open" → "Fixed in <commit SHA>" when done.

## Resume commands

**To continue UI Phase 3:**
```bash
cd /Users/vedant/Work/WeKnowGroup/wkg-kiosk-tool
git status   # should show clean, branch ui/modernize-shadcn-magic
npm run dev  # port 3003
```

**To work on backend:**
```bash
cd /Users/vedant/Work/WeKnowGroup/wkg-kiosk-tool-phase4
git status   # should show clean, branch phase4/commission-deployment
npm run dev  # may conflict with UI dev server — stop one first
```

**Tests:**
```bash
TEST_ADMIN_PASSWORD="Admin123!" npx playwright test
```

**Seed creds:**
- `admin@weknow.co` / `Admin123!` (admin role)

## Open merge decision

When ready to ship UI work:
- Option A: Merge phase4 to main first (via PR), then merge ui/modernize onto main cleanly.
- Option B: Merge ui/modernize to main directly — pulls phase4 with it, couples the PRs.
- Option C: Rebase ui/modernize onto main (drops phase4 commits from the branch) — requires phase4 to ship via its own PR separately or live on indefinitely.

No decision made yet. Recommend **A** once backend work is stable.
