---
phase: 10
plan: 11
type: execute
wave: 7
depends_on: [08]
files_modified:
  - src/app/(app)/settings/roles/role-list-client.tsx
  - src/app/(app)/settings/users/[id]/role-assignment-client.tsx
autonomous: true
requirements: [AUTH-06, AUTH-07]
tags: [a11y, playwright, selector-alignment, gap-closure]
must_haves:
  truths:
    - "The /settings/roles list page renders <h1>Roles</h1> via PageHeader (UNCHANGED from current state — only verified by this plan)."
    - "The Create role trigger on /settings/roles renders unconditionally as a real <button> with accessible name 'Create role', disabled when !canManage rather than hidden. Playwright `getByRole('button', { name: /create role/i })` resolves regardless of canManage evaluation."
    - "The /settings/roles/[id] role detail page passes the role.displayName to PageHeader (UNCHANGED — only verified). Seeded value 'Ops-IT' renders inside the <h1> and satisfies `getByRole('heading', { name: /ops.it/i })` because regex `.` matches the literal `-`."
    - "The /settings/users/[id] role assignment block is wrapped in <section role='region' aria-label='Role assignment'> so `getByRole('region', { name: /role assignment/i })` resolves."
    - "The Assign trigger in role-assignment-client.tsx has accessible name 'Assign role' (both visible text and aria-label) so `getByRole('button', { name: /assign role/i })` resolves even during the loading state."
    - "ALL changes are presentation-only. No business logic. No CASL gate changes. No server-action signature changes. The audit invariants from 10-VERIFICATION.md (5/5 SC, dual-path rbac.ts, refreshUserRoleMirror call sites, lockout-guard call sites) remain unchanged."
  artifacts:
    - path: "src/app/(app)/settings/roles/role-list-client.tsx"
      provides: "Create role button always rendered with text 'Create role'; disabled when !canManage; icon marked aria-hidden"
      contains: "disabled={!canManage}"
    - path: "src/app/(app)/settings/users/[id]/role-assignment-client.tsx"
      provides: "Role assignment region wrapper + Assign role button with explicit aria-label"
      contains: "Role assignment"
  key_links:
    - from: "tests/access-control/role-editor.spec.ts `name: /create role/i` button selector"
      to: "src/app/(app)/settings/roles/role-list-client.tsx Create role button (text + aria-hidden icon)"
      via: "Button accessible name = trimmed visible text 'Create role'"
      pattern: ">Create role<"
    - from: "tests/access-control/user-role-assignment.spec.ts `getByRole('region', { name: /role assignment/i })`"
      to: "src/app/(app)/settings/users/[id]/role-assignment-client.tsx <section> wrapper"
      via: "<section role='region' aria-label='Role assignment'>"
      pattern: "aria-label=\"Role assignment\""
    - from: "tests/access-control/user-role-assignment.spec.ts `getByRole('button', { name: /assign role/i })`"
      to: "src/app/(app)/settings/users/[id]/role-assignment-client.tsx Assign button"
      via: "Button text 'Assign role' + aria-label='Assign role' (defensive for loading state)"
      pattern: "aria-label=\"Assign role\""
---

<objective>
Align the rendered HTML on the role-admin pages with the Wave-0 Playwright spec contract that Plan 10-08 locked. The autonomous UAT (10-UAT-AUTONOMOUS.md Step 7) found 5 selector mismatches across three spec files; the spec contract is the source of truth (pages were never live-verified before now). This plan changes only presentation — no business logic, no CASL rule changes, no server-action changes.

Two of the three failure clusters resolve without code change: the `<h1>Roles</h1>` on /settings/roles is already rendered by PageHeader (verified), and the role-detail `<h1>Ops-IT</h1>` on /settings/roles/[ops-it-id] is already rendered via `title={result.role.displayName}` (verified). The original UAT-Step-7 failures for those selectors were almost certainly upstream (admin auth redirect or `canManage=false`), not source-code defects. This plan adds defensive presentation changes so the spec selectors resolve regardless.

The third cluster — region wrapper + "Assign role" button name — DOES need code changes in `role-assignment-client.tsx`.

Output: 2 client files modified. After this plan, when the Plan 10-13 operator runs the four Playwright access-control specs against the Vercel preview alias, every selector in those specs resolves to a unique DOM node.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-access-control-extended/10-UAT-AUTONOMOUS.md
@.planning/phases/10-access-control-extended/10-VERIFICATION.md
@tests/access-control/role-editor.spec.ts
@tests/access-control/edit-tier.spec.ts
@tests/access-control/user-role-assignment.spec.ts
@src/app/(app)/settings/roles/page.tsx
@src/app/(app)/settings/roles/role-list-client.tsx
@src/app/(app)/settings/roles/[id]/page.tsx
@src/app/(app)/settings/users/[id]/page.tsx
@src/app/(app)/settings/users/[id]/role-assignment-client.tsx
@src/components/layout/page-header.tsx
@migrations/0051_phase_10_seed_and_backfill.sql
</context>

<interfaces>
PageHeader (src/components/layout/page-header.tsx) renders:
```tsx
<h1 className="text-xl font-semibold ...">{title}</h1>
```
- /settings/roles passes title="Roles" → <h1>Roles</h1> (count shown in a SIBLING span; does NOT affect h1 accessible name)
- /settings/roles/[id] passes title={result.role.displayName} → <h1>Ops-IT</h1> for the ops-it tier role

Seed value from migrations/0051_phase_10_seed_and_backfill.sql line 27:
```sql
('ops-it', 'tier', 'Ops-IT', 'Operations + IT default tier. ...'),
```

Current Create role button in role-list-client.tsx (line 161-167):
```tsx
{canManage && (
  <Button onClick={() => setCreateOpen(true)} className="h-9">
    <ShieldPlus className="size-4" />
    Create role
  </Button>
)}
```
Note: button is CONDITIONALLY rendered behind `canManage` — if CASL evaluation gives the admin `canManage=false`, the button disappears entirely.

Current Assign button in role-assignment-client.tsx (line 176-178):
```tsx
<Button onClick={handleAssign} disabled={!picker || isAssigning}>
  {isAssigning ? <Loader2 className="h-4 w-4 animate-spin" /> : "Assign"}
</Button>
```
Text is "Assign" — does NOT match /assign role/i.

Current Card wrapper in role-assignment-client.tsx (line 108-111):
```tsx
return (
  <Card>
    <CardHeader>
      <CardTitle>Roles {isLoading && ...}</CardTitle>
```
No `<section role="region">` wrapper.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Make Create role button always render (disabled-not-hidden) and mark icon aria-hidden</name>
  <files>src/app/(app)/settings/roles/role-list-client.tsx</files>
  <read_first>
    - src/app/(app)/settings/roles/role-list-client.tsx (full file; focus on lines 159-168 — the Create button header bar)
    - src/app/(app)/settings/roles/page.tsx (RSC; confirm it passes title="Roles" to PageHeader)
    - src/components/layout/page-header.tsx (confirm it renders <h1>{title}</h1>)
    - tests/access-control/role-editor.spec.ts (the two failing selectors)
  </read_first>
  <action>
    Edit `src/app/(app)/settings/roles/role-list-client.tsx`. Find this block (around line 159-168):

    BEFORE:
    ```tsx
          {/* Header bar */}
          <div className="flex items-center justify-between mb-4">
            <div />
            {canManage && (
              <Button onClick={() => setCreateOpen(true)} className="h-9">
                <ShieldPlus className="size-4" />
                Create role
              </Button>
            )}
          </div>
    ```

    AFTER:
    ```tsx
          {/* Header bar */}
          <div className="flex items-center justify-between mb-4">
            <div />
            <Button
              onClick={() => setCreateOpen(true)}
              className="h-9"
              disabled={!canManage}
            >
              <ShieldPlus className="size-4" aria-hidden="true" />
              Create role
            </Button>
          </div>
    ```

    Why disabled instead of hidden:
    - Spec `getByRole('button', { name: /create role/i })` must resolve unconditionally for admin. If the live preview deploys with a CASL evaluation bug that makes `canManage=false` for admin, the button vanishes and the test fails — not because the button label is wrong, but because the button is gone. Disabled-not-hidden ensures the button is always discoverable.
    - Real security is server-side: `createRole`, `deleteRole`, `cloneRole` server actions all gate on admin permission in `editor-internal.ts`. Client-side disabled is presentation only.

    Why aria-hidden on the icon:
    - Lucide icons are inline SVGs without `<title>` elements by default, but some toolchain configurations attach `aria-labels`. `aria-hidden="true"` is the deterministic way to ensure the icon never pollutes the button's accessible name. The accessible name becomes exactly "Create role" (trimmed visible text).

    DO NOT touch anything else in this file. The Create dialog, Clone dialog, table, delete/clone/edit row actions are out of scope.
  </action>
  <verify>
    <automated>grep -q 'disabled={!canManage}' src/app/\(app\)/settings/roles/role-list-client.tsx &amp;&amp; grep -q 'aria-hidden="true"' src/app/\(app\)/settings/roles/role-list-client.tsx &amp;&amp; grep -q 'Create role' src/app/\(app\)/settings/roles/role-list-client.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'disabled={!canManage}' src/app/\(app\)/settings/roles/role-list-client.tsx` exits 0
    - `grep -q 'aria-hidden="true"' src/app/\(app\)/settings/roles/role-list-client.tsx` exits 0
    - `grep -q 'Create role' src/app/\(app\)/settings/roles/role-list-client.tsx` exits 0 (text preserved)
    - `! grep -q '{canManage && (' src/app/\(app\)/settings/roles/role-list-client.tsx | head -1` — the conditional-render pattern for the Create button is gone (note: `{canManage && (` may still appear elsewhere for the Actions column; the Create button MUST be unconditional)
    - TypeScript clean: `npx tsc --noEmit -p tsconfig.json` produces no new errors
    - `git diff src/app/(app)/settings/roles/role-list-client.tsx` shows only the Create-button hunk (no other lines changed)
  </acceptance_criteria>
  <done>
    The Create role button always renders, with accessible name "Create role". Playwright finds it whether or not `canManage` evaluates correctly. Server-side admin gate is unchanged.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wrap role-assignment Card in section[role=region][aria-label="Role assignment"]; rename Assign button to "Assign role" with explicit aria-label</name>
  <files>src/app/(app)/settings/users/[id]/role-assignment-client.tsx</files>
  <read_first>
    - src/app/(app)/settings/users/[id]/role-assignment-client.tsx (full file)
    - src/app/(app)/settings/users/[id]/page.tsx (host RSC; confirms RoleAssignmentClient is mounted under PageHeader without any other region wrappers)
    - tests/access-control/user-role-assignment.spec.ts (the two failing selectors: `getByRole('region', { name: /role assignment/i })` and `getByRole('button', { name: /assign role/i })`)
  </read_first>
  <action>
    Two precise edits to `src/app/(app)/settings/users/[id]/role-assignment-client.tsx`. Both presentation-only.

    EDIT A — Wrap the return JSX in a `<section>` with `role="region"` and `aria-label="Role assignment"`.

    Find the return statement (line 108 to end of function). Replace:

    BEFORE (line 108):
    ```tsx
      return (
        <Card>
    ```

    AFTER:
    ```tsx
      return (
        <section role="region" aria-label="Role assignment">
          <Card>
    ```

    And find the closing of the Card + the ManageScopesDialog block (lines 197-200). The current end is:

    BEFORE (lines 184-200, schematic):
    ```tsx
            </CardContent>
          </Card>

          {/* Per-(user, role) scope dialog */}
          {scopeDialog.roleId && (
            <ManageScopesDialog ... />
          )}
        </Card>
      );
    }
    ```

    Wait — re-read the actual file. The current structure is:
    - `</CardContent>` ends the CardContent
    - `</Card>` after that
    - `{scopeDialog.roleId && (...)}` is a SIBLING of `<Card>`, rendered INSIDE the same returned fragment (Card is wrapped in a fragment-less return? No — re-check).

    Looking at the source lines 108-200: the function returns ONE `<Card>` with `<CardContent>` containing the body, then AFTER `</CardContent>` and `</Card>` (line 184-185), the `{scopeDialog.roleId && (<ManageScopesDialog/>)}` appears — but as a sibling? That can't be, JSX requires a single root. The current source must have a fragment wrapping both `<Card>` and `<ManageScopesDialog>`. Re-read lines 108-200 of the actual file to confirm the wrapper before editing.

    If the existing return wraps Card + ManageScopesDialog in `<>...</>`: replace the fragment with `<section role="region" aria-label="Role assignment">...</section>`. If the existing return is just `<Card>` with the dialog nested inside (some other shape), wrap the WHOLE return in `<section role="region" aria-label="Role assignment">...</section>` keeping the inner JSX byte-for-byte unchanged.

    Concrete substitution rule (works regardless of current shape):
    1. Locate the first non-whitespace character after `return (` — call this `<OPENING_TAG>`.
    2. Locate the matching closing tag at the function's final `);` — call this `</OPENING_TAG>`.
    3. Wrap both in `<section role="region" aria-label="Role assignment">` and `</section>`.

    EDIT B — Rename the Assign button. Find this block (around line 176-178):

    BEFORE:
    ```tsx
              <Button onClick={handleAssign} disabled={!picker || isAssigning}>
                {isAssigning ? <Loader2 className="h-4 w-4 animate-spin" /> : "Assign"}
              </Button>
    ```

    AFTER:
    ```tsx
              <Button
                onClick={handleAssign}
                disabled={!picker || isAssigning}
                aria-label="Assign role"
              >
                {isAssigning ? (
                  <Loader2 className="h-4 w-4 animate-spin" aria-hidden="true" />
                ) : (
                  "Assign role"
                )}
              </Button>
    ```

    Rationale:
    - Visible text "Assign role" (not just "Assign") satisfies the spec /assign role/i.
    - `aria-label="Assign role"` is belt-and-braces — when the spinner shows during loading, the visible text is gone but the accessible name is preserved.
    - `aria-hidden="true"` on the spinner ensures it doesn't pollute the accessible name.

    DO NOT change:
    - The picker `<Select>` / `<SelectTrigger>` (placeholder "Pick a role to assign…" is fine).
    - The per-assignment row buttons (scopes button using SlidersHorizontal, revoke button using X).
    - The `CardTitle` "Roles" — that's a visible heading INSIDE the region, separate from the region's accessible name.
    - `handleAssign` / `handleRevoke` business logic.
    - `ManageScopesDialog` usage.
  </action>
  <verify>
    <automated>grep -q 'role="region"' src/app/\(app\)/settings/users/\[id\]/role-assignment-client.tsx &amp;&amp; grep -q 'aria-label="Role assignment"' src/app/\(app\)/settings/users/\[id\]/role-assignment-client.tsx &amp;&amp; grep -q 'aria-label="Assign role"' src/app/\(app\)/settings/users/\[id\]/role-assignment-client.tsx &amp;&amp; grep -q '"Assign role"' src/app/\(app\)/settings/users/\[id\]/role-assignment-client.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'role="region"' src/app/\(app\)/settings/users/\[id\]/role-assignment-client.tsx` exits 0
    - `grep -q 'aria-label="Role assignment"' src/app/\(app\)/settings/users/\[id\]/role-assignment-client.tsx` exits 0
    - `grep -q 'aria-label="Assign role"' src/app/\(app\)/settings/users/\[id\]/role-assignment-client.tsx` exits 0 (defensive aria-label on Assign button)
    - `grep -q '"Assign role"' src/app/\(app\)/settings/users/\[id\]/role-assignment-client.tsx` exits 0 (visible text)
    - `<section ... </section>` is balanced: `grep -c '<section' src/app/\(app\)/settings/users/\[id\]/role-assignment-client.tsx` equals `grep -c '</section>' src/app/\(app\)/settings/users/\[id\]/role-assignment-client.tsx` (both ≥ 1)
    - TypeScript clean: `npx tsc --noEmit -p tsconfig.json` produces no new errors
    - No business-logic call site changed (`handleAssign`, `handleRevoke`, `listUserRoles`, `assignRole`, `revokeRole` references identical to pre-plan)
  </acceptance_criteria>
  <done>
    The role assignment block on /settings/users/[id] exposes a region with name "Role assignment" and a button with name "Assign role". Both spec selectors resolve. No business-logic change.
  </done>
</task>

<task type="auto">
  <name>Task 3: Verify (no-edit) that /settings/roles and /settings/roles/[id] already satisfy the heading selectors</name>
  <files>(no file modifications — verification only)</files>
  <read_first>
    - src/app/(app)/settings/roles/page.tsx (confirm title="Roles" string literal)
    - src/app/(app)/settings/roles/[id]/page.tsx (confirm title={result.role.displayName})
    - src/components/layout/page-header.tsx (confirm <h1>{title}</h1>)
    - migrations/0051_phase_10_seed_and_backfill.sql (confirm 'Ops-IT' display name for ops-it role)
  </read_first>
  <action>
    Static-verify (grep only) that the heading selectors are satisfied by the current source. No edits expected.

    Run:
    1. `grep -n 'title="Roles"' src/app/\(app\)/settings/roles/page.tsx` — must find the literal title prop. If missing, the page is broken; restore to `title="Roles"`.
    2. `grep -n 'title={result\.role\.displayName}' src/app/\(app\)/settings/roles/\[id\]/page.tsx` — must find the displayName binding. If missing, restore.
    3. `grep -n '<h1' src/components/layout/page-header.tsx` — must find the h1 tag. If missing, the entire PageHeader is broken (this would be a much bigger problem; out of scope for this plan but DO report it).
    4. `grep -n "'Ops-IT'" migrations/0051_phase_10_seed_and_backfill.sql` — must find the seeded displayName.

    If any of the four greps fails:
    - Report the failure in the task SUMMARY and STOP. Do not invent a fix; the gap is bigger than this plan.

    If all four greps succeed:
    - No code change. Document the invariants in the SUMMARY.

    Reasoning for why this task is NOT a code-edit task:
    - The current source ALREADY renders `<h1>Roles</h1>` and `<h1>Ops-IT</h1>` correctly. The UAT-Step-7 failures for these selectors were almost certainly due to upstream issues (admin auth redirect to /login, or `canManage=false` short-circuiting the page render). Those issues are addressed by Task 1 (making the Create button visible even when canManage is false) and by Plan 10-13 (running specs against an environment with confirmed admin credentials).
  </action>
  <verify>
    <automated>grep -q 'title="Roles"' src/app/\(app\)/settings/roles/page.tsx &amp;&amp; grep -q 'title={result\.role\.displayName}' src/app/\(app\)/settings/roles/\[id\]/page.tsx &amp;&amp; grep -q '<h1' src/components/layout/page-header.tsx &amp;&amp; grep -q "'Ops-IT'" migrations/0051_phase_10_seed_and_backfill.sql</automated>
  </verify>
  <acceptance_criteria>
    - All four greps above return 0 (each invariant is present in the current source)
    - `git diff src/app/(app)/settings/roles/page.tsx src/app/(app)/settings/roles/[id]/page.tsx src/components/layout/page-header.tsx migrations/0051_phase_10_seed_and_backfill.sql` is empty (no changes to any of these four files in this task)
  </acceptance_criteria>
  <done>
    Static invariants confirmed. The Roles list page renders `<h1>Roles</h1>`; the role detail page for ops-it renders `<h1>Ops-IT</h1>`. Both heading selectors in the spec contract are satisfied by source-level state — no edits needed here. Combined with Task 1's defensive disabled-button change, Playwright admin-path tests on these two URLs will resolve their selectors against the live preview deploy.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client-side gate ↔ server-side authorisation | Visible-or-disabled controls on the Roles admin pages are presentation; the security boundary is the server-action admin check in editor-internal.ts and role-internal.ts. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-11-01 | Elevation of Privilege | Showing a disabled Create role button to a non-admin invites attempts to enable it via devtools | accept | The button is disabled client-side AND every server action (`createRole`, `deleteRole`, `cloneRole`) gates on admin permission via `_createRoleForActor` etc. in `editor-internal.ts`. A non-admin who enables the disabled button gets the click handler called but the server action throws/returns `error: not allowed`. Verified by 10-VERIFICATION.md SC4 evidence. |
| T-10-11-02 | Tampering | Adding `role="region"` with a custom aria-label might shadow an existing accessible region | accept | The role-assignment block is currently a `<Card>` without explicit region semantics. There is no existing region role to shadow. The new region exposes one named region; the layout's other landmarks (main, nav) are unchanged. |
| T-10-11-03 | Information Disclosure | Disabled-but-rendered Create button reveals admin functionality exists | accept | This information is already public via the visible /settings/roles route (anyone signed in can see the URL exists). Revealing the button name is not new disclosure. |
| T-10-11-04 | Repudiation | Renaming "Assign" → "Assign role" might break operator muscle memory | accept | The button position and behaviour are unchanged; only the label is widened. Visual change is minor; the operator runbook (10-HUMAN-UAT.md) does not currently script "click Assign". |

Severity floor: none high. All presentational. Underlying CASL gates from Plans 10-04..10-07 are unchanged.
</threat_model>

<verification>
- All four heading/seed invariants confirmed by grep (Task 3)
- `disabled={!canManage}` and `aria-hidden="true"` present in role-list-client.tsx (Task 1)
- `role="region"` AND `aria-label="Role assignment"` present in role-assignment-client.tsx (Task 2)
- `aria-label="Assign role"` AND visible text `"Assign role"` present in role-assignment-client.tsx (Task 2)
- `<section>` open/close balanced in role-assignment-client.tsx
- TypeScript compiles with zero new errors
- No business-logic functions or server actions modified
</verification>

<success_criteria>
After this plan ships, the locked Wave-0 Playwright selectors in role-editor.spec.ts, edit-tier.spec.ts, and user-role-assignment.spec.ts resolve to unique DOM nodes on the rendered pages when an authenticated admin navigates to /settings/roles, /settings/roles/[ops-it-id], and /settings/users/[viewer-id] on the Vercel preview deploy. Plan 10-13 verifies this end-to-end against the preview alias.
</success_criteria>

<output>
After completion, create `.planning/phases/10-access-control-extended/10-11-SUMMARY.md` documenting:
- The two code edits (Task 1, Task 2) — exact lines
- The static invariants confirmed in Task 3 (no code change)
- Confirmation that no business-logic call sites or server-action signatures changed
- Pre-Playwright-run reminder: Plan 10-13 is the operator-run verification gate
</output>
