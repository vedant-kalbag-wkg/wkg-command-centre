---
phase: 10
slug: access-control-extended
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-05-10
---

# Phase 10 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution. Generated by `/gsd-plan-phase 10`; planner will populate the per-task map and the Wave 0 list. See `10-RESEARCH.md` § Validation Architecture for the testable invariants this strategy must cover.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | vitest (unit / integration) + Playwright (E2E) — existing project tooling |
| **Config file** | `vitest.config.ts`, `playwright.config.ts` |
| **Quick run command** | `npm run test:unit -- src/lib/casl src/lib/auth` |
| **Full suite command** | `npm test && npm run test:e2e` |
| **Estimated runtime** | ~120 seconds full suite (existing baseline) |

---

## Sampling Rate

- **After every task commit:** Run `npm run test:unit -- <files-in-task-scope>`
- **After every plan wave:** Run `npm test` (vitest full)
- **Before `/gsd-verify-work`:** Full suite + `npx playwright test tests/access-control-extended` must be green
- **Max feedback latency:** 30 seconds for the per-task quick run

---

## Per-Task Verification Map

> Planner will populate this table when PLAN.md files are produced. Each plan's tasks must reference a row here. Until populated, this section is the placeholder.

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 10-XX-XX | XX | N | AUTH-06/07 | T-10-XX | {expected secure behavior} | unit/integration/e2e | `{command}` | ⬜ W0 | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

> Test stubs that must exist before downstream waves run. Planner will refine.

- [ ] `tests/casl/ability-builder.test.ts` — fixtures for tier defaults + custom roles + deny-wins precedence
- [ ] `tests/casl/permitted-fields.test.ts` — `permittedFieldsOf` parity with legacy `redactSensitiveFields`
- [ ] `tests/casl/external-user-invariant.test.ts` — `userType='external'` `bankingDetails` strip is unconditional
- [ ] `tests/casl/lock-out-guard.test.ts` — server-side check refuses save when zero users have effective `manage all`
- [ ] `tests/casl/scope-conditions.test.ts` — per-(user, role) scope rows produce correct CASL `conditions`
- [ ] `tests/access-control-extended/role-editor.spec.ts` — Playwright E2E for `/settings/roles` add/edit/clone/delete + diff preview
- [ ] `tests/access-control-extended/user-role-assignment.spec.ts` — Playwright E2E for per-user multi-role assignment with scope
- [ ] `tests/access-control-extended/migration-backfill.test.ts` — Drizzle migration test verifying default tier seeds + `user.role` text mirror

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Better Auth admin endpoints (`set-role`, `impersonate`, `ban`) still work after the `user.role` text mirror is denormalised | AUTH-06 | Better Auth's plugin reads `session.user.role` directly; no automated harness drives those endpoints from our test suite today | After migration: log in as admin, run impersonate flow against a non-admin; confirm session shows correct role and that revert works |
| Production lock-out recovery | AUTH-06 | Lock-out guard is server-side; manual verification that the recovery script (`scripts/reset-admin-password.ts` per CLAUDE.md) still works against the new schema | Run `scripts/reset-admin-password.ts` against a staging copy after migration; confirm credential reset still succeeds without depending on legacy `user.role` semantics |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 30s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
