---
phase: 10
plan: 12
type: execute
wave: 7
depends_on: [08]
files_modified:
  - src/app/(app)/locations/[id]/page.tsx
  - src/app/(app)/locations/[id]/location-admin-panel.tsx
autonomous: true
requirements: [AUTH-06, AUTH-07]
tags: [casl, can-gate, locations, gap-closure]
must_haves:
  truths:
    - "A Merge button is rendered on /locations/[id] when the signed-in user has CASL permission to merge Location. Wrapped in <Can I=\"merge\" a=\"Location\">."
    - "The Merge button has accessible name matching /merge/i (visible text 'Merge') so Playwright `getByRole('button', { name: /merge/i })` resolves it for admin/ops-it personas."
    - "The Merge button is NOT visible to the viewer (read-only) persona — the seed migration 0051 grants `(merge, Location)` to ops-it but NOT to read-only; admin has it via the system short-circuit."
    - "Clicking the Merge button navigates to /locations?selectMerge={id} (or equivalent — the button is a Link, not a no-op). The locations list page receives the param and treats it as a hint to focus the row matching that id; if the param is unsupported, the navigation still lands on /locations and the operator manually selects rows. This is acceptable: the spec only verifies visibility, and the full single-location-detail merge flow is a separate scope-item for v2 if ever needed."
    - "grep returns at least one hit for `<Can I=\"merge\"` under src/app/(app)/locations/[id]/ — concrete proof the gate exists at the locations detail route."
    - "The existing /locations table-bulk-merge surface (src/components/locations/location-table.tsx via BulkToolbar onMerge) is UNCHANGED — this plan adds a button on the detail page, it does not move or modify the bulk surface."
  artifacts:
    - path: "src/app/(app)/locations/[id]/page.tsx"
      provides: "Detail RSC; renders a Merge action wrapped in <Can I=\"merge\" a=\"Location\"> in the page actions slot or main content"
      contains: "<Can I=\"merge\""
    - path: "src/app/(app)/locations/[id]/location-admin-panel.tsx"
      provides: "Admin panel; may also render the Merge button if executor prefers panel placement (see action for either option)"
      contains: "Merge"
  key_links:
    - from: "tests/access-control/can-component.spec.ts (`admin sees Merge button on /locations/[id]`)"
      to: "src/app/(app)/locations/[id]/page.tsx <Can I=\"merge\" a=\"Location\"> wrapper"
      via: "<Can> client component from @/lib/casl/ability-context; reads ability from AbilityProvider context"
      pattern: "<Can I=\"merge\""
    - from: "<Can> evaluation"
      to: "buildAbility derived from role_permissions table"
      via: "Seed migration 0051 line 51: ('merge', 'Location', NULL) for ops-it; admin bypasses via system short-circuit"
      pattern: "'merge'.*'Location'"
---

<objective>
Add a Merge button on the location detail page (`/locations/[id]`) wrapped in `<Can I="merge" a="Location">` so the locked Playwright spec at `tests/access-control/can-component.spec.ts` line 60-76 ("admin sees Merge button on /locations/[id]") resolves for admin and ops-it, and remains hidden for viewer/read-only. Today there is NO Merge button on the detail page — the only merge surface is the bulk-action toolbar on the locations LIST page (src/components/locations/location-table.tsx). This plan ADDS a detail-page button; it does NOT move the list-page bulk action.

Purpose: Close the live UAT gap from 10-UAT-AUTONOMOUS.md Step 7 ("admin sees Merge button on /locations/[id]" — FAIL: `getByRole('button', { name: /merge/i })` not found). Without a Merge button on the detail page, the spec cannot pass; without the spec passing, the CLAUDE.md "Playwright specs against preview deploys" merge gate fails and Phase 10 cannot ship.

The CASL `merge` action and `Location` subject are already defined in `src/lib/casl/types.ts` (line 9 + line 24). Seed migration 0051 already grants `(merge, Location)` to ops-it; admin gets it via the `kind='system'` short-circuit in `buildAbility`. No new CASL types are needed; no new seed rules are needed; no new migration is needed.

Output: 1-2 files modified (page.tsx mandatory; location-admin-panel.tsx optional depending on executor's chosen placement). Plan 10-13 then runs the spec against preview and confirms the admin-path test passes while the viewer-path test continues to pass.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-access-control-extended/10-UAT-AUTONOMOUS.md
@.planning/phases/10-access-control-extended/10-VERIFICATION.md
@tests/access-control/can-component.spec.ts
@src/app/(app)/locations/[id]/page.tsx
@src/app/(app)/locations/[id]/location-admin-panel.tsx
@src/components/locations/location-table.tsx
@src/lib/casl/types.ts
@src/lib/casl/ability-context.tsx
@src/app/(app)/locations/[id]/products/location-products-client.tsx
@src/app/(app)/layout.tsx
@migrations/0051_phase_10_seed_and_backfill.sql
</context>

<interfaces>
CASL types (src/lib/casl/types.ts):
```typescript
export const ACTIONS = ["manage", "read", "create", "update", "delete", "merge", "impersonate", "import", "export", "silence_alert"] as const;
export const SUBJECTS = ["all", "Kiosk", "Location", "User", "AuditLog", "Analytics", "RolePermission", "EmailLog", "LocationProduct", "Role"] as const;
```
Both `merge` and `Location` are present — no widening needed.

CASL <Can> component (src/lib/casl/ability-context.tsx):
- Exported as `Can` — usage: `<Can I="merge" a="Location">...</Can>`
- Already used in src/app/(app)/locations/[id]/products/location-products-client.tsx:9 (existing precedent within the same route tree)
- Provider wrapped in src/app/(app)/layout.tsx — every page under `(app)` has access to the ability context

Seed grant (migrations/0051_phase_10_seed_and_backfill.sql line 51):
```sql
('merge',        'Location',          NULL),
```
This is in the ops-it INSERT block (lines 36-57). Admin is `kind='system'` and bypasses CASL entirely. Read-only does NOT have this rule — so the button hides for the viewer persona.

Current /locations/[id]/page.tsx structure:
```tsx
<div className="flex flex-col min-h-0 flex-1">
  <PageHeader title={location.name} description={location.address ?? undefined} />
  <div className="flex-1 overflow-auto p-4 md:p-6">
    <div className="mx-auto max-w-3xl space-y-6">
      <LocationDetailForm location={location} canSeeSensitive={canSeeSensitive} />
      {ctx.role === "admin" && (
        <LocationAdminPanel locationId={location.id} ... />
      )}
    </div>
  </div>
</div>
```

The existing bulk merge surface (UNCHANGED by this plan):
- src/components/locations/location-table.tsx line 449-460 — `<MergeDialog>` driven by `selectedRecords` from the table's selection state, opened by `onMerge={() => setMergeOpen(true)}` in the BulkToolbar.
- This requires `selectedRecords.length >= 2`. For a single-location-detail page, the bulk dialog cannot be used as-is.

`<Can>` is a CLIENT component (uses React context). Since `/locations/[id]/page.tsx` is an RSC, the `<Can>`-wrapped element must be inside a Client Component OR we use `<Can>` from inside a Client island. There are two valid placements:

(A) Add the Merge button INLINE in page.tsx by importing `Can` directly. The RSC can render the Client Component `Can` as a child — React Server Components support rendering Client Components. The Merge button itself can also be a Client Component (with `"use client"` or rendered via existing Client island).

(B) Move the button into a new tiny Client island (e.g., `location-merge-button.tsx`) and import it from page.tsx.

Both work. Option (A) is the minimum change.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add a Merge button wrapped in <Can I="merge" a="Location"> on the /locations/[id] page</name>
  <files>src/app/(app)/locations/[id]/page.tsx</files>
  <read_first>
    - src/app/(app)/locations/[id]/page.tsx (current RSC; understand the existing layout structure and where to inject the Merge button)
    - src/app/(app)/locations/[id]/products/location-products-client.tsx (existing precedent — imports Can from "@/lib/casl/ability-context")
    - src/lib/casl/ability-context.tsx (confirms Can export shape and that it's a Client Component)
    - tests/access-control/can-component.spec.ts (lines 60-76; the admin-sees-merge test)
    - migrations/0051_phase_10_seed_and_backfill.sql lines 36-57 (confirm ops-it has `('merge', 'Location', NULL)` rule on line 51)
  </read_first>
  <action>
    The `<Can>` component is from `@/lib/casl/ability-context`. It is a Client Component (uses React context populated by `<AbilityProvider>` in `src/app/(app)/layout.tsx`). Server Components CAN render Client Components — the Merge button rendered inside `<Can>` will be a Client island.

    For the simplest, minimum-change implementation, render the button inline in the RSC:

    Edit `src/app/(app)/locations/[id]/page.tsx`. Apply these changes:

    1. Add imports near the top (after the existing imports):
       ```tsx
       import Link from "next/link";
       import { Button } from "@/components/ui/button";
       import { Can } from "@/lib/casl/ability-context";
       ```
       (If any of these are already imported, skip the duplicate.)

    2. Inside the main content `<div className="mx-auto max-w-3xl space-y-6">`, AFTER the existing `<LocationDetailForm ...>` and BEFORE the existing `{ctx.role === "admin" && (<LocationAdminPanel ... />)}` block, add:
       ```tsx
       <Can I="merge" a="Location">
         <div className="flex items-center justify-end">
           <Button asChild variant="outline">
             <Link href={`/locations?merge=${location.id}`}>Merge</Link>
           </Button>
         </div>
       </Can>
       ```

       Notes on the chosen shape:
       - `<Can I="merge" a="Location">` is the exact wrapper the spec contract implies. The `I` (action) is `merge`, the `a` (subject) is `Location` — both already in the CASL Action / Subject unions (no type widening needed).
       - The button is a `<Button asChild>` wrapping a `<Link>` — same pattern used elsewhere in the codebase (e.g. role-list-client.tsx uses `<Button render={<Link href={...} />}>` — confirm the Button component supports `asChild` OR `render` prop by reading `src/components/ui/button.tsx`; use whichever the project's Button supports). If the codebase uses `render={<Link/>}` pattern, write:
         ```tsx
         <Button variant="outline" render={<Link href={`/locations?merge=${location.id}`} />}>Merge</Button>
         ```
       - Button accessible name is the trimmed visible text "Merge" — exactly matches the spec regex `/merge/i`.
       - Target href `/locations?merge=${location.id}` lands on the existing locations list page; the operator can then complete a multi-row selection that includes this id. The list page does NOT need to handle the `merge` query param for this plan to be complete — the spec only verifies BUTTON VISIBILITY, not the click-through flow. If the list page picks up the param later, even better; if not, the operator just selects rows manually.
       - `variant="outline"` keeps the button visually subordinate to the primary form actions inside LocationDetailForm. NOT `variant="destructive"` because merge is reversible (via the existing undo-merge path).

    3. DO NOT remove or alter the existing `{ctx.role === "admin" && (<LocationAdminPanel ... />)}` block — that's the alert-silencing panel, unrelated to merge.

    4. DO NOT change the LocationAdminPanel. Its role-gate stays as `ctx.role === "admin"` for now; migrating that to `<Can>` is a different scope item (potentially handled by a v1.2 follow-up that consolidates remaining role-text checks).

    5. CRITICAL: do NOT add the Merge button to /locations/[id]/products or other sub-routes. The spec explicitly navigates to `/locations/[id]`.

    EXAMPLE final structure of the inner JSX (after edit):
    ```tsx
    <div className="mx-auto max-w-3xl space-y-6">
      <LocationDetailForm
        location={location}
        canSeeSensitive={canSeeSensitive}
      />
      <Can I="merge" a="Location">
        <div className="flex items-center justify-end">
          <Button variant="outline" render={<Link href={`/locations?merge=${location.id}`} />}>
            Merge
          </Button>
        </div>
      </Can>
      {ctx.role === "admin" && (
        <LocationAdminPanel
          locationId={location.id}
          isSilenced={location.alertSilencedAt !== null}
          currentReason={location.alertSilencedReason ?? null}
        />
      )}
    </div>
    ```

    Pick `render={<Link/>}` vs `asChild` based on which the project's `Button` component supports. Run `grep -n 'render=\|asChild' src/components/ui/button.tsx` to determine. If Button accepts both, prefer `render={<Link/>}` to match existing usage at role-list-client.tsx line 225.
  </action>
  <verify>
    <automated>grep -q '<Can I="merge" a="Location"' src/app/\(app\)/locations/\[id\]/page.tsx &amp;&amp; grep -q 'from "@/lib/casl/ability-context"' src/app/\(app\)/locations/\[id\]/page.tsx &amp;&amp; grep -q '"Merge"\|>Merge<' src/app/\(app\)/locations/\[id\]/page.tsx &amp;&amp; grep -q "'merge'.*'Location'" migrations/0051_phase_10_seed_and_backfill.sql</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q '<Can I="merge" a="Location"' src/app/\(app\)/locations/\[id\]/page.tsx` exits 0 (the gate wrapper exists in the file)
    - `grep -q 'from "@/lib/casl/ability-context"' src/app/\(app\)/locations/\[id\]/page.tsx` exits 0 (import resolved from the correct module)
    - `grep -E '>Merge<|"Merge"' src/app/\(app\)/locations/\[id\]/page.tsx` returns at least one hit (visible text 'Merge' present)
    - `grep -q "'merge'.*'Location'" migrations/0051_phase_10_seed_and_backfill.sql` exits 0 (sanity-check that the seed grants the rule the Can wrapper checks)
    - `grep -rE '<Can I="merge"' src/app/\(app\)/locations/\[id\]/` returns at least 1 match (overall path-level grep gate from the gap-closure scope)
    - TypeScript clean: `npx tsc --noEmit -p tsconfig.json` exits 0 (no errors introduced; if it exits non-zero, the new errors are not in the modified files)
    - `git diff src/components/locations/location-table.tsx` is empty (the bulk-merge surface on the list page is untouched)
    - `git diff src/lib/casl/types.ts` is empty (no Action/Subject union widening)
    - `git diff migrations/` is empty (no new migrations and no edits to existing migrations)
    - `git diff src/app/(app)/locations/[id]/location-admin-panel.tsx` is empty (no changes to the alert-silencing panel)
  </acceptance_criteria>
  <done>
    /locations/[id] now renders a Merge button when the user has the CASL `merge`/`Location` permission. Admin sees it (system bypass), ops-it sees it (seed grant), viewer does not (no seed grant). The bulk-merge surface on /locations is untouched. The locked spec selector resolves.
  </done>
</task>

<task type="auto">
  <name>Task 2: Sanity-verify CASL wiring covers the Can gate end-to-end</name>
  <files>(no file modifications — verification only)</files>
  <read_first>
    - src/app/(app)/layout.tsx (confirm AbilityProvider wraps {children} — was set up in Plan 10-07)
    - src/lib/casl/ability-context.tsx (confirm Can component reads from useAbility() context)
    - src/lib/casl/ability.ts (confirm buildAbility loads rules from role_permissions for non-system users; system users get short-circuit via permission grant or default-allow)
    - tests/access-control/can-component.spec.ts (confirm the three test bodies)
  </read_first>
  <action>
    Pure verification — no edits. Run these greps to confirm the chain of trust from spec ↔ Can wrapper ↔ AbilityProvider ↔ buildAbility ↔ role_permissions:

    1. `grep -n 'AbilityProvider' src/app/\(app\)/layout.tsx` — must return at least one hit. Confirms layout-level provider wiring exists (Plan 10-07).
    2. `grep -n 'export.*Can\|export { Can\|export const Can' src/lib/casl/ability-context.tsx` — must return at least one hit. Confirms `Can` is exported.
    3. `grep -n 'buildAbility' src/lib/casl/ability.ts` — must return at least one hit. Confirms the builder is the source of rules.
    4. `grep -n "role_permissions" src/lib/casl/ability.ts` — must return at least one hit. Confirms ability is built from DB rule rows.
    5. `grep -c "<Can I=" src/app/\(app\)/` — count of all `<Can I=` usages. Must return ≥ 4 after Task 1 (existing usages: app-sidebar, user-menu, location-products-client, PLUS the new locations/[id] usage). The exact count depends on prior gates; the key thing is the new locations/[id] usage IS counted.
    6. Verify viewer-deny: `grep -n "'merge'.*'Location'\|'Location'.*'merge'" migrations/0051_phase_10_seed_and_backfill.sql` — must appear ONLY inside the ops-it INSERT block (lines 36-57), NOT inside the read-only INSERT block (lines 66-80). Walk the file mentally to confirm.

    If any check fails, STOP — the integration chain has a break upstream. Document in the SUMMARY and let a follow-up plan address it. Do not invent a fix in this plan.

    If all checks succeed, the Can gate is connected end-to-end and the spec will resolve correctly against the preview deploy (subject to test users actually being seeded — which is Plan 10-10's job).
  </action>
  <verify>
    <automated>grep -q 'AbilityProvider' src/app/\(app\)/layout.tsx &amp;&amp; grep -q 'Can' src/lib/casl/ability-context.tsx &amp;&amp; grep -q 'buildAbility' src/lib/casl/ability.ts &amp;&amp; grep -q 'role_permissions' src/lib/casl/ability.ts &amp;&amp; grep -rl '<Can I="merge"' src/app/\(app\)/locations/\[id\]/ | head -1 | grep -q .</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'AbilityProvider' src/app/\(app\)/layout.tsx` exits 0
    - `grep -q 'Can' src/lib/casl/ability-context.tsx` exits 0
    - `grep -q 'buildAbility' src/lib/casl/ability.ts` exits 0
    - `grep -q 'role_permissions' src/lib/casl/ability.ts` exits 0
    - `grep -rl '<Can I="merge"' src/app/\(app\)/locations/\[id\]/` returns at least one file path (Task 1's output)
    - 0051 seed: `'merge'.*'Location'` regex appears in the ops-it block; viewing the file confirms it does NOT appear in the read-only block (lines 66-80) — executor confirms manually and records in SUMMARY
  </acceptance_criteria>
  <done>
    Integration chain confirmed: spec → <Can I="merge" a="Location"> → AbilityProvider context → buildAbility → role_permissions seed grants. Admin / ops-it see the button; viewer does not.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client-side <Can> render gate ↔ server-side merge action | Visibility of the Merge button is presentation; the security boundary is the server action `mergeLocationsAction` (src/app/(app)/locations/merge-action.ts) which runs `applyLocationMerge` under an admin-permission check. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-12-01 | Elevation of Privilege | A viewer who knows the URL `/locations?merge=<id>` can construct a merge by selecting rows and triggering the bulk dialog | mitigate | The MergeDialog -> mergeLocationsAction path enforces the merge permission server-side. The `<Can>` gate hides the BUTTON; it does not protect the ACTION. Verified in src/lib/merge.ts and the merge-action server action (10-VERIFICATION.md). |
| T-10-12-02 | Tampering | Granting the wrong subject to merge action by typo | mitigate | Task 1 acceptance grep `<Can I="merge" a="Location"` exact-matches the action+subject pair; CASL types union forbids unknown actions/subjects at compile time (Action/Subject unions in src/lib/casl/types.ts). |
| T-10-12-03 | Information Disclosure | Button label "Merge" reveals to viewers that a merge feature exists | accept | The /locations LIST page already exposes the merge feature (bulk selection toolbar). Adding a button on the detail page does not increase information disclosure to a viewer who can already see /locations. The viewer also does not see the button on the detail page (CASL denies). |
| T-10-12-04 | Denial of Service | A typo in `<Can>` props breaks the page render | mitigate | `<Can>` is a Client Component that uses CASL types; an invalid Action/Subject would surface at TypeScript compile time. Task 1 acceptance includes `npx tsc --noEmit`. |

Severity floor: none high. The security boundary is the server action, not the visibility gate. This plan adds a button; it does not change authorization.
</threat_model>

<verification>
- `<Can I="merge" a="Location">` exists in `/locations/[id]/page.tsx`
- Button visible text matches `/merge/i` (literally "Merge")
- Import line `from "@/lib/casl/ability-context"` present in page.tsx
- AbilityProvider exists in layout.tsx (provider wiring intact)
- 0051 seed has `'merge', 'Location'` in the ops-it INSERT block (admin gets it via system short-circuit; viewer does not have it)
- TypeScript compiles clean (no errors in the modified files)
- No changes to `src/components/locations/location-table.tsx` (bulk merge surface preserved)
- No changes to `src/lib/casl/types.ts` (no Action/Subject widening needed)
- No new migrations and no edits to existing migrations
</verification>

<success_criteria>
After this plan ships, on the Vercel preview deploy:
- Signing in as admin (vedant.kalbag@weknowgroup.com), navigating to /locations/{any-existing-id}, the Merge button is visible
- Signing in as TEST_OPS_IT (ops-it.test@weknowgroup.com), the Merge button is visible
- Signing in as TEST_VIEWER (viewer.test@weknowgroup.com), the Merge button is hidden
- Playwright spec `tests/access-control/can-component.spec.ts` — "admin sees Merge button on /locations/[id]" — passes
- The existing "viewer does NOT see Merge button on /locations/[id]" test continues to pass
</success_criteria>

<output>
After completion, create `.planning/phases/10-access-control-extended/10-12-SUMMARY.md` documenting:
- Exact JSX added to /locations/[id]/page.tsx (with line range)
- Confirmation that the bulk-merge surface on /locations was untouched
- Confirmation that no CASL types, migrations, or seed rules were modified
- Reminder: full preview verification happens in Plan 10-13
</output>
