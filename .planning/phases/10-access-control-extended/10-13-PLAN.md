---
phase: 10
plan: 13
type: execute
wave: 8
depends_on: [09, 10, 11, 12]
files_modified:
  - .planning/phases/10-access-control-extended/10-VERIFICATION.md
  - .planning/phases/10-access-control-extended/deferred-items.md
  - .planning/phases/10-access-control-extended/10-HUMAN-UAT.md
  - .planning/STATE.md
autonomous: false
requirements: [AUTH-06, AUTH-07]
tags: [playwright, uat, preview-deploy, doc-closeout, gap-closure]
must_haves:
  truths:
    - "All 4 Playwright access-control specs (can-component.spec.ts, edit-tier.spec.ts, role-editor.spec.ts, user-role-assignment.spec.ts) executed against the Vercel preview alias (PLAYWRIGHT_BASE_URL=git-branch-alias) and exited 0 with at least 7/7 PASS across all assertable admin+viewer paths."
    - "10-UAT-AUTONOMOUS.md's '2 PASS / 6 FAIL' tally is closed — current run shows 0 unexpected failures. Any test that remains skipped/quarantined is documented with a specific reason in 10-VERIFICATION.md."
    - "Migration journal sync from Plan 10-09 verified end-to-end: a fresh Drizzle migrate pass against the preview DB applied 0050, 0051, 0053 (or confirmed already-applied), and 0052 is documented as operator-gated."
    - "Canonical seeder from Plan 10-10 verified: `npx tsx scripts/seed-test-users.ts` against the preview DATABASE_URL completed without errors and seeded TEST_OPS_IT + TEST_VIEWER."
    - "A11y alignment from Plan 10-11 verified: the 5 previously-failing tests across role-editor.spec.ts (2), edit-tier.spec.ts (1), user-role-assignment.spec.ts (2) now pass against preview."
    - "Merge button gate from Plan 10-12 verified: admin sees Merge button on /locations/[id]; viewer does not; can-component.spec.ts admin-path test passes."
    - "10-VERIFICATION.md is updated with the live-run results and `status: verified` (or `status: human_needed` retained ONLY for items that are still genuinely operator-blocked, e.g., impersonation flows). DEFERRED-10-02 in deferred-items.md is either resolved (deleted/marked closed) or populated with any final residual UAT-discovered gaps."
    - "STATE.md Phase 10 close entry mentions the gap-closure wave (plans 10-09..10-13) and reflects the final Playwright PASS state."
  artifacts:
    - path: ".planning/phases/10-access-control-extended/10-VERIFICATION.md"
      provides: "Final live-verified status; live Playwright tally; resolved gaps; outstanding human-only items (if any)"
      contains: "verified|live Playwright"
    - path: ".planning/phases/10-access-control-extended/deferred-items.md"
      provides: "DEFERRED-10-02 resolved or final residuals listed; DEFERRED-10-01 (drop user.role text in v1.2) preserved"
      contains: "DEFERRED-10-01"
    - path: ".planning/phases/10-access-control-extended/10-HUMAN-UAT.md"
      provides: "Updated to reflect canonical seeder name + journal-sync result (the runbook is now fully accurate for future re-runs)"
      contains: "seed-test-users.ts"
    - path: ".planning/STATE.md"
      provides: "Phase 10 close entry mentions gap-closure plans 10-09..10-13 and the final Playwright GREEN tally"
      contains: "10-13"
  key_links:
    - from: "Plan 10-09 journal sync"
      to: "preview DB migration runner"
      via: "drizzle-kit migrate against PREVIEW_DB_URL — or psql-applied state from autonomous UAT"
      pattern: "0050|0051|0053"
    - from: "Plan 10-10 canonical seeder"
      to: "preview DB test users"
      via: "npx tsx scripts/seed-test-users.ts with DATABASE_URL=PREVIEW_DB_URL"
      pattern: "seed-test-users\\.ts"
    - from: "Plans 10-11 + 10-12 source-level fixes"
      to: "Playwright spec assertions against preview HTML"
      via: "getByRole resolves to unique DOM nodes after the source changes"
      pattern: "getByRole"
---

<objective>
Run the 4 Playwright access-control specs against the Vercel preview deploy (after plans 10-09..10-12 have shipped), confirm a clean PASS state, and update phase docs to reflect live-verified status. This is the merge gate per CLAUDE.md `## Playwright specs against preview deploys` — list-pass is NOT sufficient; the specs must actually execute.

Purpose: Close the live UAT gap definitively. The autonomous UAT (10-UAT-AUTONOMOUS.md) ran on 2026-05-10 and yielded 2 PASS / 6 FAIL. After plans 10-09..10-12 land, all known root causes (unregistered journal, broken seeder, missing CASL Merge gate, mis-aligned selectors/aria) are resolved. This plan executes the verification.

This plan is `autonomous: false` because it requires:
- A live Vercel preview deploy with the gap-closure changes built
- A valid `BETTER_AUTH_URL` pinned to the git-branch alias
- A valid `DATABASE_URL` for the preview DB (pulled via `vercel env pull`)
- Operator-confirmed admin credentials (`vedant.kalbag@weknowgroup.com` / current admin password — rotate via `scripts/reset-admin-password.ts` if needed; the password is in `.env.test`)

The executor (operator or Claude with operator-provided env vars) runs the suite, captures results, and updates the four doc files based on what actually happens.

Output: 4 doc files updated. Phase 10 reaches "Playwright-verified against preview" state, the LAST step before the phase-completion squash commit + PR + merge.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-access-control-extended/10-CONTEXT.md
@.planning/phases/10-access-control-extended/10-VERIFICATION.md
@.planning/phases/10-access-control-extended/10-UAT-AUTONOMOUS.md
@.planning/phases/10-access-control-extended/10-HUMAN-UAT.md
@.planning/phases/10-access-control-extended/deferred-items.md
@.planning/phases/10-access-control-extended/10-09-PLAN.md
@.planning/phases/10-access-control-extended/10-10-PLAN.md
@.planning/phases/10-access-control-extended/10-11-PLAN.md
@.planning/phases/10-access-control-extended/10-12-PLAN.md
@tests/access-control/can-component.spec.ts
@tests/access-control/edit-tier.spec.ts
@tests/access-control/role-editor.spec.ts
@tests/access-control/user-role-assignment.spec.ts
@CLAUDE.md
@playwright.config.ts
</context>

<interfaces>
The four Playwright specs and their expected pass counts after plans 10-09..10-12 ship:

| Spec | Tests | Expected after gap closure |
|------|-------|----------------------------|
| `tests/access-control/can-component.spec.ts` | 3 | 3 PASS (admin sees Merge, viewer does NOT, viewer does NOT see Configure) |
| `tests/access-control/role-editor.spec.ts` | 2 | 2 PASS (admin sees Roles h1; admin creates a custom role and sees toast + new row) |
| `tests/access-control/edit-tier.spec.ts` | 1 | 1 PASS (admin modifies Ops-IT rule, sees diff modal with impacted count, saves, ops-it user sees effect) |
| `tests/access-control/user-role-assignment.spec.ts` | 2 | 2 PASS (admin sees role assignment region; admin assigns Ops-IT to viewer with south-west scope) |
| **Total** | **8** | **≥7/8 PASS** (the diff-modal "ops-it user sees effect" test may require a sign-out+sign-in flow that times out depending on session state; if it flakes, document and either fix or skip with rationale) |

The Vercel preview alias from the autonomous UAT (10-UAT-AUTONOMOUS.md line 6):
```
https://wkg-command-centre-git-gsd-p-aec3bc-vedant-kalbag-wkgs-projects.vercel.app
```

If the branch is re-pushed, Vercel mints a new git-branch alias automatically; find it via `vercel alias ls | grep gsd-phase-10`.

Required env vars for the Playwright run (per 10-UAT-AUTONOMOUS.md Step 7):
- `PLAYWRIGHT_BASE_URL` — git-branch alias URL
- `TEST_ADMIN_EMAIL` / `TEST_ADMIN_PASSWORD` — `vedant.kalbag@weknowgroup.com` + current admin password
- `TEST_OPS_IT_EMAIL` / `TEST_OPS_IT_PASSWORD` — `ops-it.test@weknowgroup.com` / `OpsItTest!2026`
- `TEST_VIEWER_EMAIL` / `TEST_VIEWER_PASSWORD` — `viewer.test@weknowgroup.com` / `ViewerTest!2026`

Test-helper UUIDs (optional but useful):
- `TEST_LOCATION_ID` — any valid location id on preview DB (the spec falls back to `/api/admin/locations` if unset)
- `TEST_OPS_IT_ROLE_ID` — the ops-it role's UUID from preview DB
- `TEST_VIEWER_USER_ID` — the viewer user's UUID from preview DB
</interfaces>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <what-built>
    Plans 10-09 (journal sync), 10-10 (canonical seeder), 10-11 (a11y alignment), and 10-12 (Merge button CASL gate) have shipped on the branch. The operator now needs to push the branch, wait for Vercel preview to deploy, and provide a live preview URL + credentials so the Playwright run can execute.
  </what-built>
  <how-to-verify>
    1. Confirm the branch contains the gap-closure commits:
       ```bash
       git log --oneline -n 10
       ```
       Look for commit subjects mentioning "journal", "seed-test-users", "a11y", "merge gate" (or similar — phrasing depends on commit conventions used in execution).

    2. Push the branch (if not already pushed):
       ```bash
       git push origin gsd/phase-10-access-control-extended
       ```

    3. Wait for Vercel preview to become READY. Check via:
       ```bash
       vercel ls | head -5
       ```
       The latest deploy on the branch must show `Ready`.

    4. Locate the git-branch alias (NOT the per-deploy `<hash>` URL):
       ```bash
       vercel alias ls | grep wkg-command-centre-git-gsd-phase-10
       ```
       Expected shape (per 10-HUMAN-UAT.md Step 1):
       `wkg-command-centre-git-gsd-phase-10-access-control-extended-vedant-kalbag-wkgs-projects.vercel.app`

       If the branch name is too long for the auto-generated alias to fit Vercel's char limit (the autonomous UAT used `wkg-command-centre-git-gsd-p-aec3bc-...` — truncated), use that truncated alias. Either way, the alias must survive redeploys (it does — it's branch-keyed, not hash-keyed).

    5. Confirm `BETTER_AUTH_URL` is pinned to the alias (per CLAUDE.md `## Vercel preview env vars`):
       ```bash
       vercel env ls | grep BETTER_AUTH_URL
       ```
       If it points at a `<hash>` URL or is missing, set it:
       ```bash
       ALIAS="https://<the-git-branch-alias>"
       echo "$ALIAS" | vercel env add BETTER_AUTH_URL preview gsd/phase-10-access-control-extended
       ```
       Trigger a fresh redeploy after env change so the build picks up the new value.

    6. Pull the preview DATABASE_URL into a local `.env.preview` file:
       ```bash
       vercel env pull --environment=preview --git-branch=gsd/phase-10-access-control-extended .env.preview
       ```
       Confirm `.env.preview` contains a `DATABASE_URL=` line pointing at the Neon preview DB.

    7. Provide back to the executor:
       - Confirmed preview alias URL (full https URL)
       - Confirmed admin password (or rotate via `scripts/reset-admin-password.ts` if forgotten and provide the new password)
  </how-to-verify>
  <resume-signal>
    Operator replies with:
    ```
    PREVIEW_ALIAS: https://<git-branch-alias>.vercel.app
    BETTER_AUTH_URL pinned: yes
    .env.preview pulled: yes
    Admin credentials: <email> / <password>
    ```
    Or, if a step blocked: describe what's broken and STOP — do not continue to Task 2.
  </resume-signal>
</task>

<task type="auto">
  <name>Task 2: Apply Phase 10 migrations to preview DB (verify journal-sync from Plan 10-09 works) and run canonical seeder</name>
  <files>(no source-file modifications — verification commands only)</files>
  <read_first>
    - .planning/phases/10-access-control-extended/10-09-PLAN.md (journal sync verification expectations)
    - .planning/phases/10-access-control-extended/10-10-PLAN.md (canonical seeder)
    - .planning/phases/10-access-control-extended/10-HUMAN-UAT.md Steps 3-6 (existing operator runbook for migration application; this task overlays Plan 10-09's journal-sync proof)
  </read_first>
  <action>
    Operator-driven (executor runs commands; operator provides DATABASE_URL).

    Step 1 — Confirm journal sync from Plan 10-09 took effect locally and in the build artifact:
    ```bash
    jq '.entries | length' migrations/meta/_journal.json
    # Expected: 54
    jq -r '.entries[-1].tag' migrations/meta/_journal.json
    # Expected: 0053_phase_10_role_permissions_unique
    ```

    Step 2 — Apply migrations to preview DB. Two paths depending on prior state:

    PATH A — Fresh preview DB (or migrations not yet applied):
    Vercel's build pipeline auto-runs `drizzle-kit migrate` if configured (per the existing 10-HUMAN-UAT.md Step 3). With Plan 10-09's journal sync in place, this pass NOW picks up 0050, 0051, 0053. Confirm by checking the Vercel build log for:
    ```
    Applying migration: 0050_phase_10_roles_schema
    Applying migration: 0051_phase_10_seed_and_backfill
    Applying migration: 0053_phase_10_role_permissions_unique
    ```
    (0052 is operator-gated; should NOT appear in auto-apply.)

    If the build does NOT auto-run migrations, the executor MUST run them manually:
    ```bash
    PREVIEW_DB_URL=$(grep '^DATABASE_URL=' .env.preview | cut -d= -f2-)
    DATABASE_URL="$PREVIEW_DB_URL" npx drizzle-kit migrate --config drizzle.config.ts
    ```

    PATH B — Preview DB already has 0050, 0051, 0053 applied (from the autonomous UAT psql-apply on 2026-05-10):
    `drizzle-kit migrate` is idempotent — already-applied migrations are skipped. The journal sync from Plan 10-09 records the tags in `drizzle.__drizzle_migrations` (or wherever drizzle tracks applied state) on first run. Confirm with:
    ```bash
    psql "$PREVIEW_DB_URL" -c "SELECT name, kind FROM roles ORDER BY name;"
    # Expected: admin (system), ops-it (tier), read-only (tier)
    psql "$PREVIEW_DB_URL" -c "SELECT COUNT(*) FROM role_permissions;"
    # Expected: >= 23 (15 ops-it + 8 read-only per 0051; +1 if 0053 dedupe ran)
    ```

    Step 3 — Decide 0052: Run the pre-check first:
    ```bash
    psql "$PREVIEW_DB_URL" -c "SELECT COUNT(*) FROM user_scopes WHERE role_id IS NULL;"
    # Expected: 0 (per 10-VERIFICATION.md SC1 evidence; the autonomous UAT confirmed this)
    ```
    If 0 NULL rows, apply 0052:
    ```bash
    psql "$PREVIEW_DB_URL" -f migrations/0052_phase_10_user_scopes_role_id_required.sql
    ```
    If NOT 0, STOP — do not apply 0052; document the gap in 10-VERIFICATION.md and DEFERRED-10-02 (Task 4).

    Step 4 — Run canonical seeder (verify Plan 10-10 works against preview):
    ```bash
    DATABASE_URL="$PREVIEW_DB_URL" \
    TEST_OPS_IT_EMAIL='ops-it.test@weknowgroup.com' \
    TEST_OPS_IT_PASSWORD='OpsItTest!2026' \
    TEST_VIEWER_EMAIL='viewer.test@weknowgroup.com' \
    TEST_VIEWER_PASSWORD='ViewerTest!2026' \
      npx tsx scripts/seed-test-users.ts
    ```
    Expected output: `Created` (first run) or `Updated existing` (re-run) lines for both fixture users, followed by `Test users seeded.`.

    Step 5 — Verify test users are present and have user_roles rows:
    ```bash
    psql "$PREVIEW_DB_URL" -c "SELECT u.email, r.name FROM \"user\" u JOIN user_roles ur ON ur.user_id = u.id JOIN roles r ON r.id = ur.role_id WHERE u.email IN ('ops-it.test@weknowgroup.com', 'viewer.test@weknowgroup.com') ORDER BY u.email;"
    # Expected 2 rows:
    #   ops-it.test@weknowgroup.com | ops-it
    #   viewer.test@weknowgroup.com | read-only
    ```
    If user_roles rows are missing for the test users (the autonomous UAT noted this happened because 0051 backfill ran BEFORE seeding), insert them manually:
    ```bash
    psql "$PREVIEW_DB_URL" <<'SQL'
    INSERT INTO user_roles (user_id, role_id)
      SELECT u.id, r.id FROM "user" u, roles r
      WHERE u.email = 'ops-it.test@weknowgroup.com' AND r.name = 'ops-it'
      ON CONFLICT DO NOTHING;
    INSERT INTO user_roles (user_id, role_id)
      SELECT u.id, r.id FROM "user" u, roles r
      WHERE u.email = 'viewer.test@weknowgroup.com' AND r.name = 'read-only'
      ON CONFLICT DO NOTHING;
    SQL
    ```
  </action>
  <verify>
    <automated>jq -r '.entries[-1].tag' migrations/meta/_journal.json | grep -q '^0053_phase_10_role_permissions_unique$' &amp;&amp; test -f .env.preview &amp;&amp; test -f scripts/seed-test-users.ts</automated>
  </verify>
  <acceptance_criteria>
    - Journal length 54 with tag 0053 last
    - `.env.preview` exists with a DATABASE_URL line
    - `scripts/seed-test-users.ts` (canonical) exists; `scripts/seed-test-users-direct.ts` does NOT exist
    - On preview DB: `SELECT COUNT(*) FROM roles` returns 3 (admin, ops-it, read-only)
    - On preview DB: `SELECT COUNT(*) FROM role_permissions` returns ≥ 23
    - On preview DB: `SELECT COUNT(*) FROM user_scopes WHERE role_id IS NULL` returns 0
    - On preview DB: TEST_OPS_IT joined to roles 'ops-it'; TEST_VIEWER joined to roles 'read-only' (one row each from the SELECT above)
    - `psql` exit codes 0 throughout (no errors)
  </acceptance_criteria>
  <done>
    Preview DB is in the exact state the Playwright suite expects: 3 roles seeded, all user_scopes have role_id, both test users have user_roles rows linking them to their intended tier roles. 0052 either applied (if 0 NULL pre-check passed) or documented as deferred.
  </done>
</task>

<task type="auto">
  <name>Task 3: Run 4 Playwright access-control specs against preview alias; capture results</name>
  <files>(no source-file modifications — execution + result capture only; results will feed Task 4 doc updates)</files>
  <read_first>
    - tests/access-control/can-component.spec.ts (3 tests)
    - tests/access-control/edit-tier.spec.ts (1 test)
    - tests/access-control/role-editor.spec.ts (2 tests)
    - tests/access-control/user-role-assignment.spec.ts (2 tests)
    - playwright.config.ts (confirm PLAYWRIGHT_BASE_URL is honoured; webServer is skipped when BASE_URL is set per project CLAUDE.md)
    - CLAUDE.md `## Playwright specs against preview deploys` (the merge-gate rule)
  </read_first>
  <action>
    Run the 4 specs against the preview alias. From the worktree root:

    ```bash
    PLAYWRIGHT_BASE_URL='<PREVIEW_ALIAS_FROM_TASK_1>' \
    TEST_ADMIN_EMAIL='vedant.kalbag@weknowgroup.com' \
    TEST_ADMIN_PASSWORD='<admin-password-from-Task-1>' \
    TEST_OPS_IT_EMAIL='ops-it.test@weknowgroup.com' \
    TEST_OPS_IT_PASSWORD='OpsItTest!2026' \
    TEST_VIEWER_EMAIL='viewer.test@weknowgroup.com' \
    TEST_VIEWER_PASSWORD='ViewerTest!2026' \
      npx playwright test tests/access-control/ --reporter=list 2>&1 | tee /tmp/playwright-10-13.log
    ```

    Capture the output. The exit code from `npx playwright test` is the primary signal:
    - Exit 0 → all tests in the listed suite passed
    - Exit non-0 → at least one test failed (or the suite errored)

    On non-zero exit, dump the failure details:
    ```bash
    cat /tmp/playwright-10-13.log | grep -A 5 -E '✘|FAIL|TimeoutError|Error:'
    ```

    Capture results in this shape (for use in Task 4's doc update):

    ```
    PLAYWRIGHT_RESULTS (gap-closure run, Plan 10-13):
    Run date: <YYYY-MM-DD>
    Preview alias: <full URL>
    Branch: gsd/phase-10-access-control-extended (head: <git rev-parse HEAD>)

    can-component.spec.ts (3 tests):
      ✓/✘ viewer does NOT see Merge button on /locations/[id]
      ✓/✘ admin sees Merge button on /locations/[id]
      ✓/✘ viewer does NOT see Configure nav-group in sidebar

    role-editor.spec.ts (2 tests):
      ✓/✘ admin can navigate to /settings/roles and see Roles heading
      ✓/✘ admin creates a custom role and sees toast + new row

    edit-tier.spec.ts (1 test):
      ✓/✘ admin modifies Ops-IT rule, sees diff modal with impacted count, saves, ops-it user sees effect

    user-role-assignment.spec.ts (2 tests):
      ✓/✘ admin can navigate to viewer user profile and see role-assignment block
      ✓/✘ admin assigns Ops-IT role to viewer user with south-west scope

    Totals: <N PASS> / <M FAIL>
    Playwright exit code: <code>
    ```

    Handling specific known-flaky scenarios:
    - The edit-tier "ops-it user sees effect" test signs out then signs in as ops-it. This is a stateful flow and the most likely place for flakiness. If it fails ONLY because the sign-out → sign-in handoff times out (not because the rule edit didn't apply), treat it as a known-quarantined item: document in 10-VERIFICATION.md and DEFERRED-10-02, and proceed.
    - The user-role-assignment "assign Ops-IT with south-west scope" test relies on the scope picker UI. If the picker doesn't expose the labels the test expects (`/dimension type/i`, `/dimension (id|value)/i`), treat as a residual a11y mismatch and document in DEFERRED-10-02 — DO NOT extend Plan 10-11 scope here, just record the gap.

    Acceptance for this task is binary: spec runs executed against preview alias AND results captured in writing. PASS COUNT is recorded but not gating for THIS task — it gates Task 4's `status: verified` decision.

    Re-run policy: If a test fails on the first run, re-run only that file once (`npx playwright test tests/access-control/<file>.spec.ts`). If it fails twice, mark FAIL — do not chase flakes.
  </action>
  <verify>
    <automated>test -f /tmp/playwright-10-13.log &amp;&amp; grep -E '(passed|failed|tests)' /tmp/playwright-10-13.log | head -1 | grep -q .</automated>
  </verify>
  <acceptance_criteria>
    - `/tmp/playwright-10-13.log` (or equivalent capture file) exists and contains the suite's stdout/stderr
    - The captured log shows all 8 tests were attempted (search for the test description text in the log; each must appear)
    - PASS/FAIL counts are recorded in the format above and ready to paste into Task 4's doc edits
    - At MINIMUM: ≥6/8 tests PASS — anything less indicates Plans 10-09..10-12 did not fully close their respective gaps; STOP and escalate (a follow-up gap-closure round may be needed)
    - GOAL: 8/8 PASS (full GREEN)
    - Playwright exit code is recorded (0 or non-zero)
  </acceptance_criteria>
  <done>
    Live Playwright run against preview is complete. Results are captured in a form usable by Task 4 doc updates. The merge-gate per CLAUDE.md is satisfied (specs executed, not just listed).
  </done>
</task>

<task type="auto">
  <name>Task 4: Update 10-VERIFICATION.md, deferred-items.md, 10-HUMAN-UAT.md, STATE.md with live-run results</name>
  <files>.planning/phases/10-access-control-extended/10-VERIFICATION.md, .planning/phases/10-access-control-extended/deferred-items.md, .planning/phases/10-access-control-extended/10-HUMAN-UAT.md, .planning/STATE.md</files>
  <read_first>
    - .planning/phases/10-access-control-extended/10-VERIFICATION.md (current state — has `status: human_needed` and 7 deferred human-verify items)
    - .planning/phases/10-access-control-extended/deferred-items.md (current state — DEFERRED-10-01 preserved, DEFERRED-10-02 a placeholder)
    - .planning/phases/10-access-control-extended/10-HUMAN-UAT.md (current state — references canonical seeder if Plan 10-10 ran; need to add "post-gap-closure" run note if helpful)
    - .planning/STATE.md (Phase 10 close entry — line ~212-225)
    - Task 3 captured results
  </read_first>
  <action>
    Use the Edit tool for each file (NOT Write — preserve all unrelated content).

    Edit 1 — `.planning/phases/10-access-control-extended/10-VERIFICATION.md`:

    Update the frontmatter:
    - If Task 3 captured ≥ 7/8 PASS: change `status: human_needed` → `status: verified` (or `status: live_verified`, whichever is consistent with the project's verifier conventions — read 9.1's VERIFICATION.md for precedent if unsure)
    - If < 7/8 PASS: keep `status: human_needed` and add a `notes:` field documenting the remaining gaps

    Update the `### Human Verification Required` section:
    - Replace the "1. Playwright UAT (4 specs)" entry with a "RESOLVED" marker and a 1-sentence summary of the live run (date, alias, PASS count)
    - For each of items 2-7, if Task 3's live run covered them, mark RESOLVED with evidence; if not, leave the entry but note "still operator-blocked" with the specific reason

    Add a new section at the bottom: `### Gap Closure Wave (Plans 10-09..10-13)` with bullets:
    - 10-09: Migration journal sync — registered tags 0050..0053 in _journal.json
    - 10-10: Canonical seeder — scripts/seed-test-users.ts now uses direct-DB-insert pattern (bypasses disableSignUp)
    - 10-11: A11y selector alignment — Create role button always-rendered (disabled-not-hidden); role-assignment block wrapped in section[role=region][aria-label="Role assignment"]; Assign button widened to "Assign role"
    - 10-12: Merge button CASL gate — <Can I="merge" a="Location"> on /locations/[id]
    - 10-13: Live Playwright run on preview — <N/8> PASS, <date>, <alias>

    Edit 2 — `.planning/phases/10-access-control-extended/deferred-items.md`:

    - PRESERVE DEFERRED-10-01 (drop user.role text in v1.2) unchanged
    - Update DEFERRED-10-02:
      - If live run was 8/8 GREEN with no residual gaps: replace the placeholder text with: "Resolved during gap closure (Plans 10-09..10-13). No residual UAT-discovered gaps." (or delete the DEFERRED-10-02 section entirely if the convention is to remove resolved items)
      - If 1-2 specific tests are quarantined: list each with a clear "tracking pickup" note (e.g., "Edit-tier sign-out/sign-in roundtrip flake — re-investigate in Phase 11 if it recurs")

    Edit 3 — `.planning/phases/10-access-control-extended/10-HUMAN-UAT.md`:

    Append a small section at the end (BEFORE any existing "Post-merge close-out" section):
    ```
    ## Post-gap-closure run note (Plan 10-13, <date>)

    A second Playwright run against the preview alias was performed after gap-closure plans 10-09..10-12 shipped. Results: <N/8 PASS>. Migration journal sync confirmed (drizzle-kit migrate sees the four Phase 10 entries). Canonical seeder (scripts/seed-test-users.ts) confirmed working against preview DB. <Any residuals listed>.
    ```

    Edit 4 — `.planning/STATE.md`:

    Locate the Phase 10 close entry (around line 212-225 in the current STATE.md; search for "### Phase 10 close (post-execution)"). Append a sentence at the end:
    ```
    Plans 10-09..10-13 (gap closure wave) shipped <date> resolving the 4 clusters from 10-UAT-AUTONOMOUS.md: migration journal sync (PROD BLOCKER), canonical seeder, a11y selector alignment, Merge button CASL gate. Live Playwright run on preview alias: <N/8 PASS>.
    ```

    Also update the v1.1 Phase Index entry for Phase 10 (around line 68) — append:
    ```
    Gap closure wave (plans 10-09..10-13) shipped <date> closing 4 clusters from autonomous UAT.
    ```

    DO NOT modify ROADMAP.md or REQUIREMENTS.md in this task — those are owned by a separate phase-completion commit if needed, and the Phase 10 entries there are already accurate (AUTH-06 and AUTH-07 boxes ticked).
  </action>
  <verify>
    <automated>grep -q "Gap Closure Wave\|Plans 10-09\|gap closure" .planning/phases/10-access-control-extended/10-VERIFICATION.md &amp;&amp; grep -q "DEFERRED-10-01" .planning/phases/10-access-control-extended/deferred-items.md &amp;&amp; grep -q "10-13\|gap.closure\|Plans 10-09" .planning/STATE.md</automated>
  </verify>
  <acceptance_criteria>
    - 10-VERIFICATION.md frontmatter `status` reflects the live run (verified if ≥ 7/8 PASS; otherwise human_needed retained with specific reason)
    - 10-VERIFICATION.md contains a `Gap Closure Wave (Plans 10-09..10-13)` section with one bullet per plan
    - deferred-items.md preserves DEFERRED-10-01 (drop user.role in v1.2)
    - deferred-items.md DEFERRED-10-02 is either resolved or populated with specific residuals (no leftover placeholder text)
    - 10-HUMAN-UAT.md has a "Post-gap-closure run note" section with the live run summary
    - STATE.md Phase 10 close entry mentions Plans 10-09..10-13 AND the live Playwright run tally
    - STATE.md v1.1 Phase Index Phase 10 row (around line 68) mentions the gap closure
    - No source code changes (`git diff src/ tests/ scripts/ migrations/` is empty for this task)
    - `git diff .planning/phases/10-access-control-extended/` shows 3 doc files modified in this task; `git diff .planning/STATE.md` shows 1 file modified
  </acceptance_criteria>
  <done>
    Phase 10 docs reflect the live-verified state. Future operators can read 10-VERIFICATION.md and see (a) the final live Playwright result, (b) what the gap-closure wave changed, and (c) any genuine residuals captured in deferred-items.md.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → preview DB credentials | This plan requires the operator to provide DATABASE_URL for the preview DB. Mishandled credential goes into shell history or .env files. |
| Playwright run → preview deploy | The Playwright run signs in as admin with the real admin credential and writes test data (role assignment, role creation, rule edit) to the preview DB. This is intentional — but it means the preview DB state changes during the run. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-13-01 | Information Disclosure | DATABASE_URL or admin password leaks to shell history | mitigate | Plan instructs operator to use `vercel env pull --environment=preview` to load DATABASE_URL into `.env.preview` (file is .gitignored by Next.js conventions; verify via `grep -E '^\.env\.preview$' .gitignore`). Admin password is passed as a one-shot env var on the Playwright command line — NOT stored in any file. Task 4 doc updates contain NO passwords or DB URLs. |
| T-10-13-02 | Tampering | Playwright run leaves orphan test data (created roles, dangling user_roles) on preview DB | accept | The preview DB is a per-branch Neon DB; it's torn down or recycled when the branch is merged/closed. Orphan test data is bounded in scope. Optionally the executor can clean up by deleting the test-created "Custom Kiosk Reader" role after the run; this is operator's choice, not gate-blocking. |
| T-10-13-03 | Elevation of Privilege | The Playwright admin login uses prod admin credential against preview DB — could a bug in `BETTER_AUTH_URL` pinning route the request to prod? | mitigate | CLAUDE.md `## Vercel preview env vars` explicitly forbids per-deploy `<hash>` URL pinning. Task 1 of this plan validates `BETTER_AUTH_URL` is on the git-branch alias. Better Auth `trustedOrigins` defaults to `BETTER_AUTH_URL` only; if it's misconfigured, the request 403s (visible failure), it does not silently route to prod. |
| T-10-13-04 | Denial of Service | A Playwright run that consumes all preview DB rate limit on Neon's free tier | accept | Neon free tier limits are generous for our scale; 8 tests doing a handful of DB ops each will not trip rate limits. If hit, retry after a 1-2 min wait. |
| T-10-13-05 | Repudiation | The live Playwright result is captured to /tmp (ephemeral) — could be overwritten or lost | mitigate | Task 3 instructs `tee /tmp/playwright-10-13.log`; Task 4 transcribes the result into 10-VERIFICATION.md (committed). The log file is intermediate; the doc is the durable record. |

Severity floor: T-10-13-03 (cross-environment routing) is the most security-sensitive; mitigation is the existing CLAUDE.md rule + Task 1's verification. None high after mitigation.
</threat_model>

<verification>
- Task 1 checkpoint completed (operator provided alias + credentials + .env.preview)
- Preview DB confirmed in expected state (3 roles, ≥23 role_permissions, 0 NULL role_id in user_scopes, 2 test users linked to user_roles)
- Playwright suite executed against preview alias and stdout/stderr captured
- ≥ 7/8 tests PASS (target: 8/8)
- 10-VERIFICATION.md updated with live-run status and Gap Closure Wave section
- deferred-items.md DEFERRED-10-01 preserved; DEFERRED-10-02 resolved or populated with concrete residuals
- 10-HUMAN-UAT.md has Post-gap-closure run note
- STATE.md Phase 10 close entry mentions Plans 10-09..10-13 and Playwright tally
- No source code edits in this plan (verification + doc-only)
</verification>

<success_criteria>
Phase 10 is "Playwright-verified against preview" — the CLAUDE.md merge gate per `## Playwright specs against preview deploys` is fully satisfied. The doc tree (10-VERIFICATION.md, deferred-items.md, 10-HUMAN-UAT.md, STATE.md) reflects the live state. Next step after this plan: phase-completion squash commit + PR + merge to main.
</success_criteria>

<output>
After completion, create `.planning/phases/10-access-control-extended/10-13-SUMMARY.md` documenting:
- Preview alias used for the run + date
- Full Playwright PASS/FAIL tally per spec file
- Confirmation that the four gap clusters (A: journal, B: seeder, C: merge gate, D: a11y) are closed
- Any residuals captured in DEFERRED-10-02
- Confirmation that no source code was modified in this plan
</output>
