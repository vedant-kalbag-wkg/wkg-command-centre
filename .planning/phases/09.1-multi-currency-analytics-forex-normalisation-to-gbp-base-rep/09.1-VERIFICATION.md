---
phase: 09.1-multi-currency-analytics-forex-normalisation-to-gbp-base-rep
verified: 2026-05-10T12:00:00Z
status: gaps_found
score: 13/16 must-haves verified
overrides_applied: 0
re_verification:
  previous_status: gaps_found
  previous_score: 9/13
  gaps_closed:
    - "Gap 1 — FX alert dispatch reaches Resend (template=plain-text branch + per-kind text builders)"
    - "Gap 2 / CR-01 — sql.raw UUID interpolation replaced with inArray() / sql.param()::uuid[] + ANY"
    - "Gap 2 / WR-04 — page-level isUuid filter on regions/hotel-groups/location-groups via centralised src/lib/uuid.ts"
    - "Gap 3 / CR-02 — hardcoded prod admin literal removed; getFxAlertRecipient() helper + FX_ALERT_TO env-required + .env.example entry"
    - "Gap 4 / CR-03 — hotel-groups.ts migrated to buildActiveLocationCondition; legacy alias kept @deprecated for the 7 external callers"
    - "CR-04 — DST-blind daysBetweenIso helper wired into rate-lookup.ts and backfill-net-amount-gbp.ts"
    - "CR-05 — backfill cursor uses uuid-typed (transaction_date, id) > ($1::date, $2::uuid) + ORDER BY uuid column directly"
    - "WR-05 — commission processor cumulative-base subquery now NOT (id = ANY($1::uuid[])); single bind regardless of N"
    - "WR-09 — pivot-engine currency_key asymmetry comment block tightened with marker-assertion test"
  gaps_remaining: []
  regressions:
    - "NEW CR-01 (introduced by 09.1-09 task 3) — getFxAlertRecipient() called in argument-eval position inside the FX cron's catch-block step.sendEvent(...) AND inside azure-etl's inngest.send(...). When FX_ALERT_TO is unset, the helper throws and (a) masks the original BoE error / staleness reason and (b) prevents the alert event from ever firing. Net operator effect is strictly worse than the prior hardcoded literal: no email AND wrong root cause in the Inngest dashboard."
    - "NEW CR-02 (introduced by 09.1-09 task 1) — EmailTemplate closed union in src/inngest/events.ts:28 was NOT extended with 'plain-text', but fx-rates-fetch-daily.ts:123 and azure-etl.ts:196 emit template: 'plain-text'. The closed-union typo-catch contract is now silently bypassed for the FX kinds; the runtime works only because inngest.send/step.sendEvent are loosely typed and the SUT casts via unknown."
    - "Test regression (introduced by 09.1-09 task 2) — src/lib/analytics/queries/__tests__/num-rooms-subquery.test.ts:126 asserts /lgm\\.location_group_id\\s+IN\\s*\\(/i but inArray() now emits parameterised SQL with `in ($1, $2, ...)` (lowercase `in`, with quoted column reference). The regression-guard test breaks the unit project at HEAD and CI's `vitest run` will fail."
    - "Test regression (manifestation of NEW CR-01) — tests/inngest/fx-rates-fetch-daily.integration.test.ts:131 'D-06/D-08 emits a fx_rate_fetch_failed fan-out exactly once' fails because FX_ALERT_TO is unset in the test environment, getFxAlertRecipient() throws inside the catch-block argument-eval, and inngestSendMock is never called. The test was correct under the prior hardcoded literal."
gaps:
  - truth: "FX alert events actually reach Resend on the failure path (D-06 / D-08 contract)"
    status: failed
    reason: "Plan 09.1-09 closed the dispatch-side gap (template=plain-text branch in send-email.ts) but introduced a new caller-side regression: getFxAlertRecipient() is called in argument-evaluation position inside step.sendEvent(...) (fx-rates-fetch-daily.ts:121) and inside inngest.send(...) (azure-etl.ts:194). If FX_ALERT_TO is unset, the helper throws BEFORE the event is dispatched — masking the original BoE/stale error and preventing the alert email entirely. The post-gap-closure REVIEW (09.1-REVIEW.md HEAD) flags this as NEW CR-01. The integration test tests/inngest/fx-rates-fetch-daily.integration.test.ts:131 fails on this exact failure mode at HEAD."
    artifacts:
      - path: "src/inngest/functions/fx-rates-fetch-daily.ts"
        issue: "Line 121: `to: getFxAlertRecipient()` is evaluated when constructing the event payload inside the catch-block. If the env var is unset, the helper throws and replaces the original `err`."
      - path: "src/lib/sales/etl/azure-etl.ts"
        issue: "Line 194: same pattern inside the per-blob fx_rate_stale fan-out."
      - path: "tests/inngest/fx-rates-fetch-daily.integration.test.ts"
        issue: "Line 131 D-06/D-08 fan-out spec fails at HEAD because FX_ALERT_TO is not set in the test env and the helper throws before inngestSendMock is called."
    missing:
      - "Resolve FX alert recipient ONCE at the top of `_handleFxRatesFetchDaily` (BEFORE the try/catch around fetch-boe). Pass the resolved string into the catch's event payload — getFxAlertRecipient() must NEVER be called inside the catch's argument-eval phase."
      - "Same fix in azure-etl.ts: lift getFxAlertRecipient() to the top of `runAzureEtl` (alongside containerName) so a misconfigured deploy fails on the first ETL invocation rather than silently dropping FX-stale alerts when the gate trips."
      - "Set FX_ALERT_TO in vitest integration test env (or stub getFxAlertRecipient) so the existing D-06/D-08 fan-out spec passes; add a NEW spec that drives _handleFxRatesFetchDaily with FX_ALERT_TO unset and asserts the throw fires AT RUN-START, not inside the catch."

  - truth: "EmailTemplate closed-union contract is preserved across new template kinds (events.ts intentional invariant)"
    status: failed
    reason: "src/inngest/events.ts:28 documents the invariant: 'closed unions catch typos at the call-site… must also appear in send-email.ts's TEMPLATES dispatch table'. Plan 09.1-09 added a sentinel template literal 'plain-text' (consumed in send-email.ts:86) but did NOT extend EmailTemplate. The runtime works only because inngest.send / step.sendEvent are loosely typed and the SUT casts via unknown. A future typed call-site (e.g. typed event-bus wrapper) would compile-error and someone would 'fix' it by stripping the type rather than adding the union member — at which point a typo like 'plian-text' ships silently. 09.1-REVIEW.md HEAD flags this as NEW CR-02."
    artifacts:
      - path: "src/inngest/events.ts"
        issue: "Line 28: EmailTemplate = 'password-changed' | 'poc-underperformance' — missing 'plain-text'."
      - path: "src/inngest/functions/fx-rates-fetch-daily.ts"
        issue: "Line 123: emits template: 'plain-text' against a union that does not declare it."
      - path: "src/lib/sales/etl/azure-etl.ts"
        issue: "Line 196: same pattern."
    missing:
      - "Extend EmailTemplate with `| 'plain-text'` in src/inngest/events.ts."
      - "Update the events.ts comment block to document the sentinel convention: 'plain-text' routes through send-email.ts's `template === \"plain-text\"` branch and is dispatched by `kind` rather than by the React TEMPLATES table."
      - "Add a typed unit test that imports EmailSendRequested and constructs the FX alert payload with strict typing, asserting it compiles."

  - truth: "Vitest unit + integration test suite passes on the merged branch (no regressions introduced by gap-closure)"
    status: failed
    reason: "Two test regressions block CI on the merged branch: (1) src/lib/analytics/queries/__tests__/num-rooms-subquery.test.ts:126 asserts /lgm\\.location_group_id\\s+IN\\s*\\(/i, but Plan 09.1-09's CR-01 closure changed the SQL from `IN ${idList}` to `= ANY($1::uuid[])` — the regex never matches the new shape. (2) tests/inngest/fx-rates-fetch-daily.integration.test.ts:131 fails because FX_ALERT_TO is unset in the test env (manifestation of NEW CR-01). Unit project: 1 fail / 698 pass. Integration project FX cron suite: 1 fail / 3 pass."
    artifacts:
      - path: "src/lib/analytics/queries/__tests__/num-rooms-subquery.test.ts"
        issue: "Line 126 regex /lgm\\.location_group_id\\s+IN\\s*\\(/i no longer matches the parameterised SQL emitted by inArray()."
      - path: "tests/inngest/fx-rates-fetch-daily.integration.test.ts"
        issue: "Line 131 inngestSendMock.toHaveBeenCalledOnce() fails — getFxAlertRecipient() throws before fan-out runs."
    missing:
      - "Update num-rooms-subquery.test.ts regex to accept either `IN (...)` (legacy) OR `= ANY(...)` (post-CR-01 closure). Recommended assertion: /lgm\\.location_group_id\\s+(IN|=\\s*ANY)\\s*\\(/i. Alternatively, assert against the new `= ANY(...)` shape and add a comment explaining the CR-01 migration."
      - "Once NEW CR-01 is fixed (recipient lifted to run-start), add FX_ALERT_TO to the integration test env via vi.stubEnv or a vitest setup file; OR mock @/lib/fx/alert-recipient to return a stub email when the helper is intentionally exercised."
deferred:
  - truth: "Run BoE Inngest cron once against preview alias and confirm exchange_rates populates"
    addressed_in: "09.1-HUMAN-UAT.md operator runbook step 6"
    evidence: "HUMAN-UAT explicitly defers the live BoE network call to the operator; not a verification gap."
  - truth: "Run scripts/backfill-net-amount-gbp.ts dry-run + apply against staging Neon"
    addressed_in: "09.1-HUMAN-UAT.md operator runbook step 7"
    evidence: "HUMAN-UAT explicitly defers DB-mutating backfill to the operator; not a verification gap."
  - truth: "Visual UAT: GBP-vs-native renderer dispatch on analytics dashboards"
    addressed_in: "09.1-HUMAN-UAT.md operator runbook step 9 (Playwright + visual)"
    evidence: "HUMAN-UAT explicitly requires Playwright preview-alias run; not a verification gap."
  - truth: "Admin /admin/performance-alerts shows stale-rate banner when last fetched_at > 24h"
    addressed_in: "09.1-HUMAN-UAT.md operator runbook step 9"
    evidence: "Visual / DOM-state-dependent banner test deferred to operator preview-alias UAT."
  - truth: "Production migrations 0046 → 0047 → backfill → 0048 sequence"
    addressed_in: "09.1-HUMAN-UAT.md operator runbook step 10"
    evidence: "Destructive against prod; gated through standard migration flow."
  - truth: "Vercel env-var FX_ALERT_TO propagation to preview AND production"
    addressed_in: "09.1-HUMAN-UAT.md operator runbook step 4 (REQUIRED — Phase 9.1 CR-02)"
    evidence: "Operator action requiring vercel env add CLI; not a code-side verification gap."
  - truth: "EXPLAIN ANALYZE staging gate to confirm CR-05 cursor uses Index Scan path"
    addressed_in: "09.1-HUMAN-UAT.md / Plan 09.1-10 Task 4 checkpoint:human-verify"
    evidence: "Operator action against staging Neon DB; documented in plan 09.1-10 acceptance criteria."
human_verification:
  - test: "After fixing NEW CR-01: drive _handleFxRatesFetchDaily with FX_ALERT_TO unset and confirm the helper throws AT RUN-START (not inside the catch)"
    expected: "Throw fires before the BoE fetch step; original BoE error is preserved when FX_ALERT_TO is set and BoE returns 503"
    why_human: "Behavioural property of the fix — best validated against a live preview deploy with FX_ALERT_TO toggled off briefly"
---

# Phase 9.1: Multi-currency analytics — forex normalisation to GBP base reporting Verification Report

**Phase Goal:** Cross-currency portfolios rank correctly. exchange_rates populated daily from BoE; net_amount_gbp denormalised at ingest with carry-forward + 7-day staleness ceiling; analytics dual-emit; classifier + commission swap to GBP; admin GBP + stale banner; per-kiosk POC email native preserved.

**Verified:** 2026-05-10
**Status:** gaps_found (re-verification after Plans 09.1-09 + 09.1-10 gap closure)
**Re-verification:** Yes — original `gaps_found 9/13` reduced to 0/9 prior gaps but 3 new gaps surfaced from Plan 09.1-09 regressions.

---

## Goal Achievement

### Observable Truths

| #   | Truth | Status | Evidence |
| --- | ----- | ------ | -------- |
| 1   | D-01 — BoE source: `boe-fetch.ts` exists with ~27 currency mapping, native fetch + native CSV split, points to BoE IADB CSV endpoint | ✓ VERIFIED | Unchanged from prior; src/lib/fx/boe-fetch.ts:153 BoE URL; currencies.ts:15-43 BOE_SERIES_TO_CCY has 27 entries. |
| 2   | D-02 — Inngest cron `fx-rates-fetch-daily` registered + idempotent upsert composite PK | ✓ VERIFIED | route.ts:13 registers fxRatesFetchDailyFn; cron schedule `TZ=Europe/London 0 6 * * *`; onConflictDoNothing on (currency, rate_date). |
| 3   | D-04 / D-05 / D-07 — carry-forward + 7-day staleness via DST-blind staleDays | ✓ VERIFIED (improved by 09.1-10) | rate-lookup.ts:90 now uses daysBetweenIso() (CR-04 closed). pipeline.ts:402-406 hard-fails when staleDays > 7. days-between-iso.test.ts has 8 specs covering UK DST boundaries. |
| 4   | D-06 / D-08 — alert events emitted with correct kinds AND reach Resend | ✗ FAILED | Gap 1 from prior verification IS closed: send-email.ts now has a `template === "plain-text"` sentinel branch (lines 83-113) that resolves both kinds via fxRateFetchFailedText / fxRateStaleText → Resend.emails.send. tests/inngest/send-email-fx-dispatch.integration.test.ts (3 specs) PASSES. **However Plan 09.1-09 introduced a new caller-side regression (NEW CR-01)**: getFxAlertRecipient() throws inside step.sendEvent argument-eval when FX_ALERT_TO is unset, masking the original error and preventing the event from firing. tests/inngest/fx-rates-fetch-daily.integration.test.ts:131 fails on this exact mode. |
| 5   | D-09 — schema additions: `exchange_rates` + `sales_records.net_amount_gbp` (NOT NULL post-0048) | ✓ VERIFIED | Migrations 0046/0047/0048 present and unchanged. |
| 6   | D-10 / D-11 — every analytics SUM(net_amount) dual-emits + currency_key | ✓ VERIFIED | Unchanged from prior; 67 net_amount_gbp references; 70 currency_key/currencyKey references. |
| 7   | D-12 — Sorting / ranking always GBP across cohorts | ✓ VERIFIED | Unchanged from prior; 14 ORDER BY revenue_gbp DESC sites with D-12 annotation. |
| 8   | D-13 / D-14 — POC email contract preserved (format-currency.ts untouched) | ✓ VERIFIED | git log on src/lib/performance-alerts/format-currency.ts shows last touched in 93eb9a5 (Phase 9 merge); no commits in this phase modified the file. |
| 9   | D-14 — classifier swap to GBP | ✓ VERIFIED | classify-locations.ts:181 SUM(s.net_amount_gbp::numeric). |
| 10  | D-15 — commission processor base swapped to GBP | ✓ VERIFIED (improved by 09.1-10) | processor.ts:233 sum(salesRecords.netAmountGbp); cumulative-base subquery now NOT (id = ANY($1::uuid[])) — single bind, WR-05 closed. tests/commission/processor-bind-ceiling.integration.test.ts confirms 70k-id batch passes without bind-overflow. |
| 11  | D-16 — /admin/performance-alerts always GBP + stale-rate banner | ✓ VERIFIED | Unchanged; banner conditional on fxStale (>24h since last fetched_at). |
| 12  | D-17 — pivot-engine `net_amount` field id preserved + currency_key asymmetry documented | ✓ VERIFIED (improved by 09.1-10) | pivot-engine.ts:393-413 documents the intentional booking_fee/net_amount asymmetry (WR-09 closed); pivot-engine.test.ts has marker-assertion specs. |
| 13  | Code review BLOCKERs from 09.1-REVIEW.md (CR-01 SQL injection, CR-02 hardcoded admin email, CR-03 hotel-groups inconsistency, CR-04 DST off-by-one, CR-05 cursor type-mismatch) addressed | ✓ VERIFIED | 5 BLOCKERs from prior REVIEW closed. CR-01: sql.raw IN-list interpolations replaced (regions.ts:187,224, location-groups.ts:166-167, hotel-groups.ts:245). CR-02: getFxAlertRecipient() helper + .env.example:47. CR-03: hotel-groups.ts:21,44-47 buildActiveLocationCondition. CR-04: daysBetweenIso() in rate-lookup.ts:90 + backfill-net-amount-gbp.ts:70. CR-05: backfill cursor uuid-typed comparison. |
| 14  | (NEW) Centralised UUID-shape validator + page-level isUuid filter on analytics surface | ✓ VERIFIED | src/lib/uuid.ts (UUID_REGEX + isUuid + assertUuid + assertUuidArray) created with 5 unit specs; regions/hotel-groups/location-groups page.tsx all call `.filter(isUuid)` after parseIdParam. |
| 15  | (NEW) Test suite preserved on merged branch — no regressions in unit + integration | ✗ FAILED | 1 unit test failure (num-rooms-subquery.test.ts:126 stale regex post-CR-01 inArray migration) + 1 integration test failure (fx-rates-fetch-daily.integration.test.ts:131, manifestation of NEW CR-01). CI's `vitest run` will fail on this branch. |
| 16  | (NEW) EmailTemplate closed-union contract preserved | ✗ FAILED | src/inngest/events.ts:28 EmailTemplate is still `"password-changed" | "poc-underperformance"` — missing `"plain-text"`. fx-rates-fetch-daily.ts:123 and azure-etl.ts:196 emit a value not declared in the union. The events.ts comment block calls this contract load-bearing ("catches typos at the call-site"); 09.1-09 silently violated it. |

**Score:** 13/16 truths verified. (Truth 4 partially verified — dispatch fix lands but caller-side regression breaks the failure path. Truths 15 + 16 fully failed.)

### Deferred Items

Items not yet met but explicitly addressed in 09.1-HUMAN-UAT.md operator runbook (NOT actionable verification gaps):

| #   | Item | Addressed In | Evidence |
| --- | ---- | ------------ | -------- |
| 1   | Run BoE Inngest cron once against preview alias | HUMAN-UAT step 6 | Operator-gated live BoE network call |
| 2   | Backfill dry-run + apply against staging Neon, then 0048 NOT NULL flip | HUMAN-UAT step 7 | DB-mutating script execution by operator |
| 3   | Visual UAT: GBP-vs-native renderer on analytics dashboards | HUMAN-UAT step 9 | Playwright preview-alias run; visual judgement |
| 4   | Admin stale-rate banner visible against preview alias | HUMAN-UAT step 9 | DOM rendering + state-dependent banner |
| 5   | Production migration 0046 → 0047 → backfill → 0048 sequence | HUMAN-UAT step 10 | Destructive against prod; standard migration flow |
| 6   | Vercel FX_ALERT_TO env-var propagation (preview + production) | HUMAN-UAT step 4 | Operator action via `vercel env add` CLI |
| 7   | EXPLAIN ANALYZE staging gate to confirm CR-05 cursor Index Scan | Plan 09.1-10 Task 4 | Operator action against staging Neon DB |

### Required Artifacts

| Artifact | Expected | Status | Details |
| -------- | -------- | ------ | ------- |
| `migrations/0046_phase_09_1_exchange_rates.sql` | Creates exchange_rates with composite PK | ✓ VERIFIED | Unchanged. |
| `migrations/0047_phase_09_1_sales_records_net_amount_gbp.sql` | Adds nullable column | ✓ VERIFIED | Unchanged. |
| `migrations/0048_phase_09_1_net_amount_gbp_not_null.sql` | Idempotent NOT NULL flip | ✓ VERIFIED | Unchanged. |
| `scripts/backfill-net-amount-gbp.ts` | One-shot backfill, idempotent + restartable + uuid-cursor (CR-05) | ✓ VERIFIED | Cursor at lines 142-164 uses (transaction_date, id) > ($1::date, $2::uuid); ORDER BY transaction_date, id (uuid-typed). daysBetweenIso() at line 70 (CR-04). |
| `src/lib/fx/boe-fetch.ts` | Pure parser + fetch wrapper | ✓ VERIFIED | Unchanged. |
| `src/lib/fx/rate-lookup.ts` | getRateForDate with carry-forward + DST-blind staleDays | ✓ VERIFIED | Line 90 uses daysBetweenIso() (CR-04 closed). |
| `src/lib/fx/days-between-iso.ts` | Pure-string-day helper, validates ISO format | ✓ VERIFIED (NEW) | Created by 09.1-10; pure Date.UTC arithmetic; throws naming malformed value. 8 unit specs incl. UK DST boundaries. |
| `src/lib/fx/alert-recipient.ts` | Env-required FX_ALERT_TO helper | ✓ EXISTS / ⚠️ MISWIRED | Helper itself is correct (throws on missing env). Both call sites invoke it in argument-eval position inside catch/send paths — **NEW CR-01**. |
| `src/lib/uuid.ts` | UUID_REGEX + isUuid + assertUuid + assertUuidArray | ✓ VERIFIED (NEW) | Created by 09.1-09 task 2; consumed by 3 analytics pages. |
| `src/lib/fx/currencies.ts` | BOE_SERIES_TO_CCY + BOE_SUPPORTED_CURRENCIES | ✓ VERIFIED | Unchanged. |
| `src/inngest/functions/fx-rates-fetch-daily.ts` | Daily cron + 3 steps + alert fan-out | ⚠️ PARTIAL | Cron + steps + audit row + event fan-out present. Plain-text template + getFxAlertRecipient() wired. But: getFxAlertRecipient() called in step.sendEvent arg-eval (NEW CR-01) — masks BoE error, prevents alert when env unset. |
| `src/inngest/functions/send-email.ts` | TEMPLATES dispatch + plain-text sentinel branch | ✓ VERIFIED | Lines 83-113 sentinel branch resolves fx_rate_fetch_failed + fx_rate_stale; throws on unknown plain-text kind. tests/inngest/send-email-fx-dispatch.integration.test.ts (3 specs) PASSES. |
| `src/emails/text-versions.ts` | fxRateFetchFailedText + fxRateStaleText body builders | ✓ VERIFIED | Plan 09.1-09 task 1; mirror passwordChangedText shape. |
| `src/inngest/events.ts` | EmailTemplate union extended for new template kinds | ✗ FAILED | Line 28 EmailTemplate union missing `"plain-text"` — **NEW CR-02**. |
| `src/app/api/inngest/route.ts` | fxRatesFetchDailyFn registered in serve() | ✓ VERIFIED | Unchanged. |
| `src/app/(app)/admin/performance-alerts/page.tsx` | Stale-rate banner + always-GBP | ✓ VERIFIED | Unchanged. |
| `src/lib/performance-alerts/classify-locations.ts` | SUM swapped to net_amount_gbp | ✓ VERIFIED | Unchanged. |
| `src/lib/commission/processor.ts` | Commission base on net_amount_gbp + bind-ceiling-safe (WR-05) | ✓ VERIFIED (improved by 09.1-10) | Line 244 NOT (id = ANY($1::uuid[])) — single bind. tests/commission/processor-bind-ceiling.integration.test.ts (70k id batch) passes. |
| `src/lib/performance-alerts/format-currency.ts` | UNCHANGED from Phase 9 | ✓ VERIFIED | Last commit 93eb9a5 (Phase 9 merge). |
| `src/lib/sales/etl/azure-etl.ts` | Pre-commit FX-stale gate emits fx_rate_stale | ⚠️ PARTIAL | Gate logic present. plain-text template + getFxAlertRecipient() wired. Same NEW CR-01 caller-side regression as fx cron (recipient resolution in arg-eval inside inngest.send). |
| `src/app/(app)/settings/data-import/sales/pipeline.ts` | Stamps net_amount_gbp at insert | ✓ VERIFIED | Unchanged. |
| `src/lib/analytics/queries/regions.ts` | inArray() / parameter-safe IN-list filters (CR-01) | ✓ VERIFIED | Lines 187, 224 use inArray(); zero sql.raw UUID interpolations remain. |
| `src/lib/analytics/queries/location-groups.ts` | inArray() + sql.param + ANY (CR-01) | ✓ VERIFIED | Lines 161-167; helper-call shape switches IN ${idList} → = ANY(${idList}). |
| `src/lib/analytics/queries/hotel-groups.ts` | sql.param + ANY + buildActiveLocationCondition migration (CR-01 + CR-03) | ✓ VERIFIED | Lines 21, 44-47, 244-245. |
| `src/app/(app)/analytics/{regions,hotel-groups,location-groups}/page.tsx` | Page-level isUuid filter | ✓ VERIFIED (NEW) | parseIdParam(...).filter(isUuid) in all three pages. |
| `src/lib/analytics/pivot-engine.ts` | Field id preserved + WR-09 asymmetry documented | ✓ VERIFIED (improved by 09.1-10) | Lines 393-413 tightened comment block; pivot-engine.test.ts has marker-assertion specs. |
| `.env.example` | FX_ALERT_TO documented | ✓ VERIFIED | Line 47: `FX_ALERT_TO=ops@weknowgroup.com`. |
| `09.1-HUMAN-UAT.md` | Step 4 (FX_ALERT_TO env propagation) REQUIRED | ✓ VERIFIED | Step 4 upgraded with preview + production commands + verification probe. |

### Key Link Verification

| From | To  | Via | Status | Details |
| ---- | --- | --- | ------ | ------- |
| Inngest cron `fx-rates-fetch-daily` | `exchange_rates` table | `db.insert(exchangeRates).onConflictDoNothing` | ✓ WIRED | Idempotent upsert. |
| Inngest cron failure (FX_ALERT_TO set) | Operator email via Resend | step.sendEvent → send-email plain-text branch → Resend.emails.send | ⚠️ WIRED-WITH-CAVEAT | The dispatch path itself works (verified by send-email-fx-dispatch.integration.test.ts). But getFxAlertRecipient() is called in arg-eval position; if FX_ALERT_TO is unset, the helper throws in the catch block and the event is NEVER dispatched (NEW CR-01). |
| Azure ETL pre-commit | `fx_rate_stale` alert | Same pattern | ⚠️ WIRED-WITH-CAVEAT | Same NEW CR-01 caller-side regression. |
| Sales pipeline insert | `net_amount_gbp` | getRateForDate (uses daysBetweenIso for staleness) → identity-or-divide → string at insert | ✓ WIRED | Unchanged. |
| classify-locations | `sales_records.net_amount_gbp` | SUM(s.net_amount_gbp::numeric) raw SQL | ✓ WIRED | Unchanged. |
| commission processor | `sales_records.net_amount_gbp` | coalesce(sum(salesRecords.netAmountGbp), 0) + NOT (id = ANY) | ✓ WIRED | WR-05 bind-ceiling closed. |
| Region/group analytics renderers | pickRevenueDisplay + formatNativeCurrency | imports + dispatch on metrics.currencyKey | ✓ WIRED | Unchanged. |
| Pivot engine | GBP sibling + currency_key emission with WR-09 asymmetry | CURRENCY_GBP_COLUMNS + gbpAlias + currencyKeyAlias + FILTER for booking_fee | ✓ WIRED | WR-09 documented + asserted. |
| Analytics page URL searchParams | SQL builder | parseIdParam → filter(isUuid) → inArray()/ANY | ✓ WIRED (NEW) | CR-01/WR-04 page-level gate. |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
| -------- | ------------- | ------ | ------------------ | ------ |
| admin/performance-alerts/page.tsx | latestFxFetchAt | db.select MAX(exchangeRates.fetchedAt) | Yes — populated by cron upserts | ✓ FLOWING |
| pipeline.ts | netAmountGbp | ratePairs.get(...).rate | Yes — DB query against real table | ✓ FLOWING |
| classify-locations.ts | total_revenue | SUM(s.net_amount_gbp) over sales_records | Yes — column populated by pipeline + backfill | ✓ FLOWING |
| commission/processor.ts | cumulative | sum(salesRecords.netAmountGbp) with single-bind ANY | Yes | ✓ FLOWING |
| send-email handler (FX kinds) | text | fxRateFetchFailedText / fxRateStaleText hand-crafted body | Yes (when reached) | ✓ FLOWING |
| FX cron catch-block | event payload `to` field | getFxAlertRecipient() call inside arg-eval | NO when FX_ALERT_TO unset — throw replaces err and event never sent | ✗ HOLLOW (NEW CR-01) |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
| -------- | ------- | ------ | ------ |
| TypeScript check | `npx tsc --noEmit -p tsconfig.json` | No errors | ✓ PASS |
| Vitest unit project | `npx vitest --no-coverage --reporter=basic run --project unit` | PASS (698) FAIL (1) — num-rooms-subquery.test.ts:126 | ✗ FAIL |
| Vitest FX library suites | `npx vitest run src/lib/uuid.test.ts src/lib/fx/days-between-iso.test.ts src/lib/fx/rate-lookup.test.ts` | PASS (19/0) | ✓ PASS |
| Vitest pivot + analytics queries | `npx vitest run src/lib/analytics/pivot-engine.test.ts src/lib/analytics/queries/` | PASS (~140/0) | ✓ PASS |
| Send-email FX dispatch integration | `npx vitest run --project integration tests/inngest/send-email-fx-dispatch.integration.test.ts` | PASS (3/0) | ✓ PASS |
| Azure ETL integration suite | `npx vitest run --project integration tests/etl/` | PASS (6/0) | ✓ PASS |
| FX cron integration suite | `npx vitest run --project integration tests/inngest/fx-rates-fetch-daily.integration.test.ts` | PASS (3) FAIL (1) — D-06/D-08 fan-out spec at line 131 | ✗ FAIL |
| FX_ALERT_TO documented in env example | `grep FX_ALERT_TO .env.example` | match at line 47 | ✓ PASS |
| Hardcoded prod admin literal in FX paths | `grep -rn "vedant.kalbag@weknowgroup.com" src/inngest/ src/lib/sales/ src/lib/fx/` | 0 hits | ✓ PASS |
| Cron registration | `grep fxRatesFetchDailyFn src/app/api/inngest/route.ts` | imported + registered | ✓ PASS |
| Migrations 0046+0047+0048 present | `ls migrations/004{6,7,8}_*.sql` | All three present | ✓ PASS |
| EmailTemplate union extended for plain-text | `grep "plain-text" src/inngest/events.ts` | 0 hits | ✗ FAIL (NEW CR-02) |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
| ----------- | ---------- | ----------- | ------ | -------- |
| FX-01 | 09.1-02, 09.1-03, 09.1-04, 09.1-09 (gap closure) | exchange_rates + Inngest daily cron + BoE fetcher + alert dispatch | ⚠️ PARTIAL SATISFIED | Table + cron + fetcher all exist and idempotent; cron registered; plain-text dispatch resolves to Resend (Gap 1 closed). But fan-out is gated behind getFxAlertRecipient() in arg-eval position — NEW CR-01 prevents alert when FX_ALERT_TO unset. |
| FX-02 | 09.1-05, 09.1-10 (gap closure) | net_amount_gbp + ETL stamp + backfill + DST-blind 7-day staleness + fx_rate_stale alert | ⚠️ PARTIAL SATISFIED | Column added, NOT NULL flipped, ETL stamps per-row, backfill script idempotent + uuid-cursor (CR-05), staleness uses daysBetweenIso (CR-04). fx_rate_stale alert subject to same NEW CR-01 caller-side regression. |
| FX-03 | 09.1-06, 09.1-07, 09.1-09 (CR-01 + CR-03) | Analytics dual-emit + renderer auto-pick + saved-pivot back-compat + parameter-safe IN-list filters + buildActiveLocationCondition consistency | ✓ SATISFIED | Dual-emit + renderer auto-pick + sql.raw replaced with inArray(); page-level isUuid filter; hotel-groups migrated. |
| FX-04 | 09.1-07, 09.1-10 (WR-05 + WR-09) | classifier + commission + admin banner; POC email native preserved + commission bind-ceiling | ✓ SATISFIED | All in place; format-currency.ts untouched; WR-05 single-bind ANY; WR-09 asymmetry documented + tested. |

No orphaned requirements detected — all four FX-0X IDs from REQUIREMENTS.md (lines 74-77, 128-131) accounted for in plans 09.1-{02,03,04,05,06,07,09,10}.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
| ---- | ---- | ------- | -------- | ------ |
| src/inngest/functions/fx-rates-fetch-daily.ts | 121 | `getFxAlertRecipient()` called in step.sendEvent argument-eval position inside catch block | 🛑 Blocker | NEW CR-01 — masks BoE error, prevents alert when FX_ALERT_TO unset. Net operator effect strictly worse than prior hardcoded literal. |
| src/lib/sales/etl/azure-etl.ts | 194 | Same pattern inside inngest.send arg-eval | 🛑 Blocker | NEW CR-01 — same consequence for fx_rate_stale alerts. |
| src/inngest/events.ts | 28 | EmailTemplate closed union missing `"plain-text"` | 🛑 Blocker | NEW CR-02 — closed-union typo-catch contract silently bypassed for FX kinds. |
| src/lib/analytics/queries/__tests__/num-rooms-subquery.test.ts | 126 | Regex `/lgm\.location_group_id\s+IN\s*\(/i` no longer matches inArray output | 🛑 Blocker | Stale post-CR-01 closure; breaks `vitest run` on the unit project. |
| tests/inngest/fx-rates-fetch-daily.integration.test.ts | 131 | inngestSendMock.toHaveBeenCalledOnce() — no FX_ALERT_TO in test env | 🛑 Blocker | Manifestation of NEW CR-01; breaks `vitest run --project integration` for this suite. |
| src/lib/analytics/queries/regions.ts | 31 | Unused import `calculatePeriodChange` (dead since prior phase) | ⚠️ Warning | WR-01 from 09.1-REVIEW.md HEAD; build cleanliness only — no runtime effect. |
| src/app/(app)/analytics/{regions,hotel-groups,location-groups}/actions.ts | ~24 | Server actions don't validate regionIds/groupIds with assertUuidArray | ⚠️ Warning | WR-02 from 09.1-REVIEW.md HEAD; defense-in-depth gap. Drizzle pg uuid cast still throws, but error message is generic 500. |
| src/inngest/functions/send-email.ts | 36-40 | `_resend` lazy singleton survives RESEND_API_KEY rotation | ⚠️ Warning | WR-03 from 09.1-REVIEW.md HEAD; serverless lifetime mostly hides this; warm Inngest workers might silently 401 after key rotation. |
| src/lib/sales/etl/azure-etl.ts | 160-209 | Per-blob FX-stale pre-check duplicates in-pipeline gate without shared cache | ⚠️ Warning | WR-04 from 09.1-REVIEW.md HEAD; latency, not correctness. Pre-existing; stacking the new alert path makes drift more dangerous. |

### Human Verification Required

| Test | Expected | Why Human |
| ---- | -------- | --------- |
| After fixing NEW CR-01, drive _handleFxRatesFetchDaily with FX_ALERT_TO unset | Throw fires AT RUN-START (not in catch); original BoE error preserved when env set and BoE 503 | Behavioural property of the proposed fix; best validated against a live preview deploy with FX_ALERT_TO toggled briefly. Otherwise the operator-facing tests in HUMAN-UAT.md are deferred (not gaps). |

The full HUMAN-UAT punch list (preview alias setup, BoE cron trigger, backfill apply, visual UAT, prod migration sequence, EXPLAIN ANALYZE staging) is captured in `09.1-HUMAN-UAT.md` and is intentionally outside the verification scope.

### Gaps Summary

The Wave 1 gap-closure pair (Plans 09.1-09 + 09.1-10) closed every prior verification gap and every prior REVIEW BLOCKER:

- **All 4 prior verification gaps closed** (Gap 1 FX dispatch, Gap 2 SQL injection, Gap 3 hardcoded admin email, Gap 4 hotel-groups inconsistency) — verified end-to-end in the merged code.
- **CR-04 + CR-05 + WR-04 + WR-05 + WR-09 closed** — verified with surgical fixes and pinning tests.
- **5 net-new artifacts wired in** — `src/lib/uuid.ts`, `src/lib/fx/days-between-iso.ts`, `src/lib/fx/alert-recipient.ts`, plus 2 integration test specs.

However, the gap-closure plans introduced **3 new gaps** that block phase close:

**Gap A (NEW CR-01 / Truth 4):** `getFxAlertRecipient()` is called in argument-evaluation position inside the FX cron's catch-block `step.sendEvent(...)` (`fx-rates-fetch-daily.ts:121`) and inside the Azure ETL's `inngest.send(...)` (`azure-etl.ts:194`). When `FX_ALERT_TO` is unset, the helper's intentional `throw new Error("FX_ALERT_TO env var is required...")` propagates instead of the original BoE/staleness error. The operator-visible failure is now strictly worse than the prior hardcoded literal: no email AND wrong root cause in the Inngest dashboard. The fix is to lift the recipient resolution to the top of `_handleFxRatesFetchDaily` / `runAzureEtl` so a misconfigured deploy fails at run-start, not in the catch.

**Gap B (NEW CR-02 / Truth 16):** `EmailTemplate` closed union in `src/inngest/events.ts:28` was NOT extended with `"plain-text"`, but `fx-rates-fetch-daily.ts:123` and `azure-etl.ts:196` emit `template: "plain-text"`. The events.ts comment block calls this contract load-bearing ("catches typos at the call-site"); the runtime works only because `inngest.send` and `step.sendEvent` are loosely typed and the SUT casts via `unknown`. The fix is a one-line union extension plus a typed compile-asserting test.

**Gap C (Truth 15 — test regressions):** Two test failures block `vitest run` on the merged branch: (1) `num-rooms-subquery.test.ts:126` regex stale post-CR-01 inArray migration; (2) `fx-rates-fetch-daily.integration.test.ts:131` is a manifestation of NEW CR-01. Both will be caught by CI. Fixes: update the regex to accept either `IN (...)` OR `= ANY(...)`; stub FX_ALERT_TO in the integration test env (or fix NEW CR-01 first, then the test passes naturally).

These three gaps are tightly clustered: Gap A and Gap B are direct quality misses in Plan 09.1-09 task 1 and task 3; Gap C-1 is a quality miss in Plan 09.1-09 task 2 (the regression-guard test wasn't updated alongside the SQL shape change); Gap C-2 is a downstream symptom of Gap A.

A single focused gap-closure pass should resolve all three:

1. Lift `getFxAlertRecipient()` to run-start in `fx-rates-fetch-daily.ts` and `azure-etl.ts` (closes Gap A).
2. Extend `EmailTemplate` with `| "plain-text"` and update the events.ts comment block (closes Gap B).
3. Update `num-rooms-subquery.test.ts:126` regex to match the new `= ANY(...)` shape; add FX_ALERT_TO setup to integration test env or stub `getFxAlertRecipient` (closes Gap C).

The remaining 4 warnings (WR-01 dead import, WR-02 server-action validation, WR-03 _resend cache, WR-04 ETL gate dedup) are not blockers and can be deferred to a follow-up plan.

---

_Verified: 2026-05-10_
_Verifier: Claude (gsd-verifier)_
_Re-verification of phase 09.1 after Wave 1 gap-closure pair (Plans 09.1-09 + 09.1-10) merged. Prior `gaps_found 9/13` → current `gaps_found 13/16` (all 9 prior gaps closed; 3 new gaps surfaced from gap-closure-introduced regressions)._
