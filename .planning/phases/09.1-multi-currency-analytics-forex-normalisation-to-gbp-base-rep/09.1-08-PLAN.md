---
phase: 09.1
plan: 08
type: execute
wave: 6
depends_on: [09.1-04, 09.1-07]
files_modified:
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/PROJECT.md
  - .planning/STATE.md
  - .planning/phases/09.1-multi-currency-analytics-forex-normalisation-to-gbp-base-rep/09.1-HUMAN-UAT.md
autonomous: false
requirements: [FX-01, FX-02, FX-03, FX-04]
must_haves:
  truths:
    - "ROADMAP.md Phase 9.1 wording is corrected: net_amount_gbp ONLY (vat/total companion column listing dropped per D-09)"
    - "ROADMAP.md Phase 9.1 rate source is locked: Bank of England daily spot rate (no longer 'TBD')"
    - "ROADMAP.md Phase 9.1 includes 7-day staleness ceiling rule (D-07)"
    - "REQUIREMENTS.md has finalised FX-01..FX-04 entries with the exact descriptions from RESEARCH § 'Phase Requirements' table"
    - "REQUIREMENTS.md traceability table includes FX-01..04 → Phase 9.1 mappings"
    - "PROJECT.md acknowledges cross-currency analytics is now in scope (new G. Forex normalisation section OR addition under existing C.)"
    - "STATE.md session-continuity row records Phase 9.1 close + branch + plans shipped"
    - "09.1-HUMAN-UAT.md operator checklist exists with the items from VALIDATION.md § 'Manual-Only Verifications' + the Playwright preview-alias run command"
    - "Playwright specs run against PLAYWRIGHT_BASE_URL=<preview-alias> (per CLAUDE.md gate: --list is NOT sufficient evidence)"
  artifacts:
    - path: ".planning/ROADMAP.md"
      provides: "Updated Phase 9.1 section + plan list filled in"
      contains: "Bank of England|net_amount_gbp|7-day"
    - path: ".planning/REQUIREMENTS.md"
      provides: "FX-01..04 added under new G. category + traceability rows"
      contains: "FX-01|FX-02|FX-03|FX-04"
    - path: ".planning/STATE.md"
      provides: "Phase 9.1 close session-continuity entry"
      contains: "09.1.*shipped|Phase 9.1.*complete"
    - path: ".planning/phases/09.1-multi-currency-analytics-forex-normalisation-to-gbp-base-rep/09.1-HUMAN-UAT.md"
      provides: "Operator checklist for the manual-only verifications"
      contains: "PLAYWRIGHT_BASE_URL|backfill|0048|preview"
  key_links:
    - from: "REQUIREMENTS.md FX-01..04"
      to: "ROADMAP.md Phase 9.1 Requirements field"
      via: "every REQ-ID appears in exactly one phase"
      pattern: "FX-0[1-4]"
    - from: "Playwright specs"
      to: "preview-alias deploy URL (CLAUDE.md rule)"
      via: "PLAYWRIGHT_BASE_URL=https://wkg-command-centre-git-<branch>-vedant-kalbag-wkgs-projects.vercel.app npx playwright test tests/fx-normalisation/"
      pattern: "PLAYWRIGHT_BASE_URL.*preview\\|wkg-command-centre-git"
---

<objective>
Close out Phase 9.1: doc surgery on ROADMAP/REQUIREMENTS/PROJECT/STATE per CONTEXT.md `<canonical_refs>` (the doc edits CONTEXT.md says MUST land in this phase), and produce the operator UAT checklist that gates the merge. Includes the Playwright run against the preview alias per CLAUDE.md rule that `--list` is not sufficient evidence.

Purpose: Implements the doc-surgery items CONTEXT.md `<canonical_refs>` calls out (drop vat/total mentions, lock BoE, draft FX-01..FX-04), records the Phase 9.1 close in STATE.md, and gives the operator a clear UAT runbook including the preview-alias Playwright command per CLAUDE.md.

Output: 4 doc edits + new HUMAN-UAT.md + Playwright preview-alias run executed by operator.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/09.1-multi-currency-analytics-forex-normalisation-to-gbp-base-rep/09.1-CONTEXT.md
@.planning/phases/09.1-multi-currency-analytics-forex-normalisation-to-gbp-base-rep/09.1-RESEARCH.md
@.planning/phases/09.1-multi-currency-analytics-forex-normalisation-to-gbp-base-rep/09.1-VALIDATION.md
@.planning/phases/09-poc-underperformance-alerts/09-HUMAN-UAT.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Edit ROADMAP.md Phase 9.1 section: drop vat/total companion column listing, lock BoE, add 7-day staleness rule, fill plan list</name>
  <read_first>
    - .planning/ROADMAP.md (entire — focus on Phase 9.1 block at lines 83-93)
    - .planning/phases/09.1-multi-currency-analytics-forex-normalisation-to-gbp-base-rep/09.1-CONTEXT.md (`<canonical_refs>` § "Locked architectural decisions (read first)" first item — explicit edit instruction)
    - .planning/phases/09.1-multi-currency-analytics-forex-normalisation-to-gbp-base-rep/09.1-RESEARCH.md (§ "Phase Requirements" table — FX-01..04 final shape)
  </read_first>
  <files>.planning/ROADMAP.md</files>
  <behavior>
    Update the Phase 9.1 block (currently lines 83-93) per CONTEXT.md `<canonical_refs>` instruction:
    - Goal sentence: drop "denormalises `net_amount_gbp` / `vat_amount_gbp` / `total_amount_gbp`" in favour of "denormalises `net_amount_gbp`" only (D-09 — no vat/total companions).
    - Replace "(ECB / BoE / NetSuite TBD)" with "Bank of England daily spot rate" (D-01 locked).
    - Add a sentence about the 7-day staleness rule: "When the most recent BoE rate for a currency is more than 7 calendar days older than a row's transaction_date, the sales ETL hard-fails the affected blob and emits an `fx_rate_stale` email alert (D-07)."
    - Update Requirements field from "TBD" to `FX-01, FX-02, FX-03, FX-04`.
    - Update Plans field from "TBD" to "8 plans" with the actual plan list filled in (mirror the format used by other phases on the same file):
      ```
      Plans:
      - [ ] 09.1-01-PLAN.md — Wave 0: Test fixtures + RED-stage scaffolds (FX-01..04)
      - [ ] 09.1-02-PLAN.md — Wave 1: Schema (exchange_rates table + sales_records.net_amount_gbp NULLABLE) + EmailKind (FX-01/FX-02)
      - [ ] 09.1-03-PLAN.md — Wave 1: FX library (boe-fetch + rate-lookup + currencies) (FX-01/FX-02)
      - [ ] 09.1-04-PLAN.md — Wave 2: Inngest cron `fx-rates.fetch-daily` + serve registration (FX-01)
      - [ ] 09.1-05-PLAN.md — Wave 3: ETL stamping + backfill script + 0048 NOT NULL flip (operator-gated) (FX-02)
      - [ ] 09.1-06-PLAN.md — Wave 3: Analytics SQL audit dual-emit (41 sites / 13 files) (FX-03a)
      - [ ] 09.1-07-PLAN.md — Wave 4: Renderer dispatch + tooltips + classifier/commission swaps + admin stale banner (FX-03b/FX-04)
      - [ ] 09.1-08-PLAN.md — Wave 5: Doc edits (ROADMAP/REQUIREMENTS/STATE) + operator UAT
      ```
  </behavior>
  <action>
    Use Edit tool to replace the existing Phase 9.1 block. Do not touch unrelated phases.

    Implements CONTEXT.md `<canonical_refs>` doc-surgery requirement.
  </action>
  <verify>
    <automated>grep -q 'Bank of England' .planning/ROADMAP.md && grep -q 'net_amount_gbp' .planning/ROADMAP.md && grep -q '7.day\|7-day' .planning/ROADMAP.md && grep -q 'FX-01.*FX-02.*FX-03.*FX-04\|FX-01, FX-02, FX-03, FX-04' .planning/ROADMAP.md && grep -q '09.1-01-PLAN.md' .planning/ROADMAP.md && ! grep -q 'vat_amount_gbp\|total_amount_gbp' .planning/ROADMAP.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'Bank of England' .planning/ROADMAP.md` returns ≥ 1 (D-01 locked)
    - `grep -cE 'vat_amount_gbp|total_amount_gbp' .planning/ROADMAP.md` returns 0 (D-09 — vat/total companion mentions removed from Phase 9.1)
    - `grep -cE '7.day|7-day' .planning/ROADMAP.md` returns ≥ 1 (D-07 staleness rule added)
    - `grep -cE 'FX-01.*FX-02|FX-01, FX-02' .planning/ROADMAP.md` returns ≥ 1 (REQ-IDs added)
    - `grep -c '09.1-01-PLAN.md' .planning/ROADMAP.md` returns ≥ 1 (plan list filled)
    - `grep -c '09.1-08-PLAN.md' .planning/ROADMAP.md` returns ≥ 1 (all 8 plans listed)
  </acceptance_criteria>
  <done>ROADMAP.md Phase 9.1 block updated per CONTEXT canonical_refs instructions; vat/total companion mentions removed; BoE locked; staleness rule added; plan list filled.</done>
</task>

<task type="auto">
  <name>Task 2: Add FX-01..FX-04 entries to REQUIREMENTS.md (new section G. Forex normalisation) + traceability table rows</name>
  <read_first>
    - .planning/REQUIREMENTS.md (entire — focus on the section letter pattern A-F + Traceability table at line 96)
    - .planning/phases/09.1-multi-currency-analytics-forex-normalisation-to-gbp-base-rep/09.1-RESEARCH.md (§ "Phase Requirements" table at lines 38-46 — final FX-01..04 descriptions)
    - .planning/phases/09.1-multi-currency-analytics-forex-normalisation-to-gbp-base-rep/09.1-CONTEXT.md (`<canonical_refs>` — REQUIREMENTS.md must be appended)
  </read_first>
  <files>.planning/REQUIREMENTS.md</files>
  <behavior>
    - Add a new section G between F (Polish + tech debt) and "Out of Scope (explicit)":
      ```
      ## G. Forex normalisation (cross-currency analytics)

      - [ ] **FX-01** — `exchange_rates(currency, rate_date, rate_to_gbp, source, fetched_at)` table + Inngest daily cron `fx-rates.fetch-daily` fetching Bank of England IADB CSV for the supported set of ~25 majors; idempotent upsert via composite PK (currency, rate_date); emits `fx_rate_fetch_failed` email_log row + alert on fetch error.
      - [ ] **FX-02** — `sales_records.net_amount_gbp numeric(12,2)` column; stamped at every ingest site using BoE rate for transaction_date with carry-forward; identity (= net_amount) for currency='GBP'; one-shot historical backfill script; 7-day staleness ceiling enforced per-row stamp; emits `fx_rate_stale` alert when ceiling tripped.
      - [ ] **FX-03** — Analytics audit + swap: every SUM(netAmount) site dual-emits (SUM(net_amount), SUM(net_amount_gbp), currency_key); renderer auto-picks native vs GBP per cohort cardinality. Both Sales mode and Revenue mode dual-emit. Saved-pivot field id `net_amount` preserved.
      - [ ] **FX-04** — `classifyEligibleLocations` ranks on net_amount_gbp; `commission/processor.ts` commission base on net_amount_gbp; `/admin/performance-alerts` always-GBP + stale-rate banner; POC underperformance email continues native via existing `format-currency.ts` (no change).
      ```
    - Add 4 rows to the Traceability table at the bottom of the file:
      ```
      | FX-01 | Phase 9.1 | SC1 — exchange_rates populated daily from BoE; gap-fill / staleness handling defined |
      | FX-02 | Phase 9.1 | SC2 — sales_records.net_amount_gbp populated on ingest using rate for transaction_date; backfill script for historical rows |
      | FX-03 | Phase 9.1 | SC3 — every analytics query audited; aggregations on multi-currency cohorts use GBP-normalised columns; per-kiosk views surface native currency |
      | FX-04 | Phase 9.1 | SC4 — classifyEligibleLocations ranks on GBP-normalised revenue; POC underperformance email continues to render each kiosk's native-currency revenue |
      ```
    - Update the totals line at the file bottom: "Total: 24 REQs across 7 categories" (was 20 across 6).
  </behavior>
  <action>
    Use Edit tool to insert section G + the 4 traceability rows + update the total. Do not touch other sections.

    Implements CONTEXT.md `<canonical_refs>` doc-surgery requirement (REQUIREMENTS.md draft FX-01..04 entries).
  </action>
  <verify>
    <automated>grep -q 'FX-01' .planning/REQUIREMENTS.md && grep -q 'FX-02' .planning/REQUIREMENTS.md && grep -q 'FX-03' .planning/REQUIREMENTS.md && grep -q 'FX-04' .planning/REQUIREMENTS.md && grep -q '## G\.' .planning/REQUIREMENTS.md && grep -q '| FX-01 | Phase 9.1' .planning/REQUIREMENTS.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE '\*\*FX-0[1-4]\*\*' .planning/REQUIREMENTS.md` returns 4 (all four bullet entries)
    - `grep -c '## G\.' .planning/REQUIREMENTS.md` returns 1 (new section header)
    - `grep -cE '^\| FX-0[1-4] \| Phase 9\.1 \|' .planning/REQUIREMENTS.md` returns 4 (4 traceability rows)
    - `grep -c '24 REQs\|24/24' .planning/REQUIREMENTS.md` returns ≥ 1 (totals updated)
  </acceptance_criteria>
  <done>REQUIREMENTS.md has section G with FX-01..04 + 4 traceability rows + totals updated.</done>
</task>

<task type="auto">
  <name>Task 3: Update PROJECT.md (acknowledge cross-currency analytics in scope) + STATE.md (Phase 9.1 close session-continuity row)</name>
  <read_first>
    - .planning/PROJECT.md (entire — focus on `## Active (v1.1)` section)
    - .planning/STATE.md (entire — focus on Session Continuity section + Phase 9.1 decisions block)
    - .planning/phases/09.1-multi-currency-analytics-forex-normalisation-to-gbp-base-rep/09.1-CONTEXT.md (`<canonical_refs>` — PROJECT + STATE doc-surgery items)
  </read_first>
  <files>.planning/PROJECT.md, .planning/STATE.md</files>
  <behavior>
    PROJECT.md:
    - In the `## Active (v1.1)` section, add a new bullet under the existing C. POC underperformance alerts section (or create a new "G. Forex normalisation" subsection): "Cross-currency analytics: Bank of England daily spot rates ingested via Inngest; sales_records.net_amount_gbp denormalised at ingest; analytics aggregates dual-emit native + GBP + currency-key; classifier + commission rank on GBP."

    STATE.md:
    - Update the `## Current Position` block: change `Phase: 09.1` Status to "Phase 9.1 (Multi-currency forex normalisation) shipped 2026-MM-DD on branch `gsd/phase-09.1-...`. 8 plans across 5 waves; operator UAT in `09.1-HUMAN-UAT.md` pending merge."
    - Append a new sub-section under `### Phase 9.1 decisions captured 2026-05-09` named `### Phase 9.1 close (post-execution)` listing: 8 plans shipped, the migration trio (0046/0047/0048), the FX library, the audit-and-swap of 41 SQL sites, the renderer dispatch via pickRevenueDisplay, the FX-04 surgical swaps, the admin stale-rate banner, and the doc surgery items (this plan).
  </behavior>
  <action>
    Use Edit tool to add the bullet to PROJECT.md and the close sub-section to STATE.md.

    Per CLAUDE.md GSD workflow preferences: phase-completion commits should summarise the full phase deliverable. STATE.md captures that summary as the source of truth for v1.1 progress.

    Implements CONTEXT.md `<canonical_refs>` doc-surgery requirement (PROJECT.md acknowledgement + STATE.md session continuity).
  </action>
  <verify>
    <automated>grep -qE 'cross.currency|Forex normalisation|Bank of England.*rate' .planning/PROJECT.md && grep -q 'Phase 9.1' .planning/STATE.md && grep -qE 'Phase 9\.1 close|9\.1.*shipped|9\.1.*complete' .planning/STATE.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE 'cross.currency|Forex normalisation|Bank of England' .planning/PROJECT.md` returns ≥ 1
    - `grep -cE 'Phase 9\.1 close|9\.1.*shipped|9\.1.*complete' .planning/STATE.md` returns ≥ 1 (close entry exists)
    - `grep -cE '09\.1-01.*09\.1-08|8 plans' .planning/STATE.md` returns ≥ 1 (8-plan deliverable summary)
  </acceptance_criteria>
  <done>PROJECT.md acknowledges scope; STATE.md has close sub-section with 8-plan summary.</done>
</task>

<task type="auto">
  <name>Task 4: Author 09.1-HUMAN-UAT.md operator checklist (manual verifications + Playwright preview-alias commands)</name>
  <read_first>
    - .planning/phases/09-poc-underperformance-alerts/09-HUMAN-UAT.md (analog — Phase 9 operator UAT shape)
    - .planning/phases/09.1-multi-currency-analytics-forex-normalisation-to-gbp-base-rep/09.1-VALIDATION.md (§ "Manual-Only Verifications" — 3 items already drafted)
    - CLAUDE.md (§ "Vercel preview env vars", § "Playwright specs against preview deploys", § "Prod admin password rotation" — operational reference)
  </read_first>
  <files>.planning/phases/09.1-multi-currency-analytics-forex-normalisation-to-gbp-base-rep/09.1-HUMAN-UAT.md</files>
  <behavior>
    Author the operator UAT checklist mirroring the 09-HUMAN-UAT.md shape. Include:
    - Header with branch name + status.
    - Pre-merge checklist:
      - Trigger one manual run of `fx-rates-fetch-daily` Inngest function via Inngest dashboard (preview env). Confirm exchange_rates table has rows for today.
      - Apply migration 0048 NOT NULL flip on the dev/preview Neon (post-backfill).
      - Set Vercel preview env vars: confirm `BETTER_AUTH_URL` is the git-branch alias (not per-deploy URL) per CLAUDE.md rule.
      - (Optional) Set `FX_ALERT_TO` env var on preview if a non-default admin email is needed for alert testing.
    - Playwright UAT: exact command per CLAUDE.md rule:
      ```bash
      PLAYWRIGHT_BASE_URL=https://wkg-command-centre-git-<branch>-vedant-kalbag-wkgs-projects.vercel.app \
      TEST_ADMIN_EMAIL='vedant.kalbag@weknowgroup.com' \
      TEST_ADMIN_PASSWORD='<from .env.test>' \
        npx playwright test tests/fx-normalisation/
      ```
      With explicit note: per CLAUDE.md, `--list` passing is NOT sufficient evidence. Both specs MUST run and pass.
    - Visual UAT: 3 surfaces to manually verify in the preview deploy (per VALIDATION § "Manual-Only Verifications"):
      - `/analytics/regions` filtered to a single-currency cohort (e.g. EUR-only) → revenue cell shows € symbol.
      - `/analytics/regions` un-filtered (mixed cohort) → revenue cell shows £ symbol.
      - `/admin/performance-alerts` → stale-rate banner is hidden when fresh, visible when MAX(exchange_rates.fetched_at) > 24h (operator manually mutates fetched_at via psql to trigger).
    - Backfill operator note: confirm `SELECT COUNT(*) FROM sales_records WHERE net_amount_gbp IS NULL` is 0 BEFORE applying migration 0048 (per Pitfall 7).
    - Sign-off: each checkbox ticked + brief note pasted into `STATE.md` Last Activity row.
  </behavior>
  <action>
    Author the file. Use the 09-HUMAN-UAT.md analog as the shape template. Include the exact Playwright command above with the placeholder branch name pre-filled (`gsd/phase-09.1-multi-currency-analytics-forex-normalisation-to-gbp-base-rep`).

    Implements CLAUDE.md gates:
    - "Vercel preview env vars" — checklist item for BETTER_AUTH_URL pinning
    - "Playwright specs against preview deploys" — explicit run-against-preview command + warning that --list is NOT sufficient
    - "npm lockfile must stay in sync" — sanity grep that no new entries crept into package.json (RESEARCH says zero new deps)
  </action>
  <verify>
    <automated>test -f .planning/phases/09.1-multi-currency-analytics-forex-normalisation-to-gbp-base-rep/09.1-HUMAN-UAT.md && grep -q 'PLAYWRIGHT_BASE_URL' .planning/phases/09.1-multi-currency-analytics-forex-normalisation-to-gbp-base-rep/09.1-HUMAN-UAT.md && grep -q 'wkg-command-centre-git\|preview' .planning/phases/09.1-multi-currency-analytics-forex-normalisation-to-gbp-base-rep/09.1-HUMAN-UAT.md && grep -q 'BETTER_AUTH_URL' .planning/phases/09.1-multi-currency-analytics-forex-normalisation-to-gbp-base-rep/09.1-HUMAN-UAT.md && grep -q 'backfill\|0048' .planning/phases/09.1-multi-currency-analytics-forex-normalisation-to-gbp-base-rep/09.1-HUMAN-UAT.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .planning/phases/09.1-multi-currency-analytics-forex-normalisation-to-gbp-base-rep/09.1-HUMAN-UAT.md` exits 0
    - `grep -c 'PLAYWRIGHT_BASE_URL' .planning/phases/09.1-multi-currency-analytics-forex-normalisation-to-gbp-base-rep/09.1-HUMAN-UAT.md` returns ≥ 1 (preview-alias rule honoured)
    - `grep -c 'wkg-command-centre-git\|preview' .planning/phases/09.1-multi-currency-analytics-forex-normalisation-to-gbp-base-rep/09.1-HUMAN-UAT.md` returns ≥ 1
    - `grep -c 'BETTER_AUTH_URL' .planning/phases/09.1-multi-currency-analytics-forex-normalisation-to-gbp-base-rep/09.1-HUMAN-UAT.md` returns ≥ 1 (CLAUDE.md Vercel rule)
    - `grep -c 'backfill\|0048' .planning/phases/09.1-multi-currency-analytics-forex-normalisation-to-gbp-base-rep/09.1-HUMAN-UAT.md` returns ≥ 1 (Pitfall 7 reminder)
    - `grep -cE "\[ \]|- \[" .planning/phases/09.1-multi-currency-analytics-forex-normalisation-to-gbp-base-rep/09.1-HUMAN-UAT.md` returns ≥ 5 (≥5 checkboxes for operator)
  </acceptance_criteria>
  <done>HUMAN-UAT checklist exists with all CLAUDE.md gate references + Pitfall 7 reminder + ≥5 operator checkboxes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5 [BLOCKING]: Operator runs Playwright suite against preview alias and walks the visual UAT</name>
  <what-built>
    All 7 prior plans landed. ROADMAP/REQUIREMENTS/PROJECT/STATE updated. 09.1-HUMAN-UAT.md exists with the full operator checklist. The preview deploy is ready for the final automated + visual UAT.
  </what-built>
  <how-to-verify>
    Execute every item in `09.1-HUMAN-UAT.md`. Critically:

    1. **Confirm preview env vars per CLAUDE.md `## Vercel preview env vars`**:
       ```bash
       vercel env ls preview gsd/phase-09.1-multi-currency-analytics-forex-normalisation-to-gbp-base-rep | grep BETTER_AUTH_URL
       ```
       Expect a value like `https://wkg-command-centre-git-gsd-phase-09-1-multi-currency-analytics-vedant-kalbag-wkgs-projects.vercel.app` (the git-branch alias, NOT a per-deploy hash URL).

    2. **Run Playwright against preview alias per CLAUDE.md `## Playwright specs against preview deploys`** (this is the critical CLAUDE.md gate — `--list` passing is NOT enough):
       ```bash
       PLAYWRIGHT_BASE_URL=https://wkg-command-centre-git-<branch-alias>-vedant-kalbag-wkgs-projects.vercel.app \
       TEST_ADMIN_EMAIL='vedant.kalbag@weknowgroup.com' \
       TEST_ADMIN_PASSWORD='<from .env.test>' \
         npx playwright test tests/fx-normalisation/
       ```
       Expect both specs to PASS (admin-stale-banner.spec.ts + analytics-currency-render.spec.ts). Failure = regression; do NOT merge.

    3. **Walk the 3 visual UAT items** in 09.1-HUMAN-UAT.md (analytics single-currency cohort renders €; analytics mixed cohort renders £; admin page banner toggles on/off as fetched_at is mutated).

    4. **Confirm no NULL net_amount_gbp** post-backfill + post-migration-0048:
       ```bash
       psql "$DATABASE_URL" -c "SELECT COUNT(*) FROM sales_records WHERE net_amount_gbp IS NULL;"
       # Expected: 0
       psql "$DATABASE_URL" -c "\d sales_records" | grep net_amount_gbp
       # Expected: net_amount_gbp shown as NOT NULL (no `null` after numeric(12,2))
       ```

    5. **Confirm zero new npm deps per CLAUDE.md `## npm lockfile must stay in sync`**:
       ```bash
       git diff main..HEAD -- package.json package-lock.json | head -20
       # Expected: empty diff (RESEARCH said zero new deps)
       ```
       If anything other than empty appears, follow the CLAUDE.md Docker linux/amd64 lockfile regen flow before merging.

    6. **Confirm phase summary commit per CLAUDE.md `## GSD Workflow Preferences`**:
       Last commit on branch should be a distinct phase-completion commit summarising the full Phase 9.1 deliverable (8 plans, exchange_rates table, BoE cron, dual-emit audit, classifier swap, admin banner). NOT just "merge plan 8".

    Once all items above pass: phase is ready for PR + merge to main.
  </how-to-verify>
  <resume-signal>Type "Phase 9.1 UAT complete; ready to merge" once all 6 verification steps above pass. Or paste the failing step's output for triage. If any Playwright spec fails, do NOT merge — fix the regression first.</resume-signal>
</task>

</tasks>

<verification>
- ROADMAP/REQUIREMENTS/PROJECT/STATE all updated per CONTEXT canonical_refs.
- 09.1-HUMAN-UAT.md exists with the full operator runbook.
- Operator UAT executed against preview alias (not list-pass).
- net_amount_gbp NOT NULL across all rows (post-Pitfall 7 sequence).
- Zero new npm deps committed.
- Phase-completion commit exists per CLAUDE.md GSD preferences.
</verification>

<success_criteria>
- 4 doc files updated (ROADMAP, REQUIREMENTS, PROJECT, STATE)
- 09.1-HUMAN-UAT.md exists
- Playwright specs PASS against preview alias (CLAUDE.md gate cleared)
- Visual UAT walked
- Zero npm deps drift
- Phase-completion commit on branch
- Operator resume-signal received
</success_criteria>

<output>
After completion, create `.planning/phases/09.1-multi-currency-analytics-forex-normalisation-to-gbp-base-rep/09.1-08-SUMMARY.md`. After plan 8 SUMMARY commit, create the phase-completion commit per CLAUDE.md GSD workflow preferences (a distinct commit summarising the full Phase 9.1 deliverable, NOT just the plan 8 summary).
</output>
