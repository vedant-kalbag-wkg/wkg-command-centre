---
phase: 9.1
slug: multi-currency-analytics-forex-normalisation-to-gbp-base-rep
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-05-09
---

# Phase 9.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution. Detail filled by gsd-planner from `09.1-RESEARCH.md` `## Validation Architecture` section.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | vitest (existing project default) + Playwright (E2E against preview alias) |
| **Config file** | `vitest.config.ts`, `playwright.config.ts` |
| **Quick run command** | `npx vitest run --no-coverage <changed-file-glob>` |
| **Full suite command** | `npx vitest run` (unit + integration), then `PLAYWRIGHT_BASE_URL=<preview-alias> npx playwright test tests/phase-09.1` |
| **Estimated runtime** | unit ~30s, integration ~2 min, Playwright ~3 min against preview |

---

## Sampling Rate

- **After every task commit:** Run quick `npx vitest run` scoped to the touched files
- **After every plan wave:** Run full `npx vitest run`
- **Before `/gsd-verify-work`:** Full suite green + Playwright suite green against preview alias
- **Max feedback latency:** 60 seconds (quick run); 5 min (full + Playwright)

---

## Per-Task Verification Map

> Filled in by gsd-planner using the test gap inventory in `09.1-RESEARCH.md` `## Validation Architecture`. One row per task; reference plan IDs and REQ-IDs (FX-01..04). Wave 0 entries refer to fixture/scaffold tasks.

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| _to be populated by planner_ | | | | | | | | | ⬜ pending |

---

## Wave 0 Requirements

> Per RESEARCH.md, Wave 0 establishes 12 test gaps before Wave 1 schema changes. Planner expands these into concrete task entries (e.g., fixture files, mock BoE CSV payloads, classifier ranking fixtures, formatter dispatch fixtures).

- [ ] BoE CSV fixture file (multi-currency, weekend gap example) — covers FX-01 ingest fetch+parse
- [ ] `exchange_rates` seed fixture for unit tests — covers FX-02 rate lookup
- [ ] Carry-forward staleness fixture (rate older than 7 days → reject) — covers FX-01 staleness ceiling
- [ ] Sales-records ingest fixture with mixed currencies — covers FX-02 dual-emit at ingest
- [ ] Classifier percentile-ranking fixture (single-currency cohort: ranking unchanged) — covers FX-04 regression
- [ ] Classifier percentile-ranking fixture (multi-currency cohort: ranking changes correctly) — covers FX-04 swap
- [ ] Renderer dispatch fixture (per-kiosk → native; cross-currency aggregate → GBP) — covers FX-03b
- [ ] Backfill batch test (1k-chunk pattern from `pipeline.ts:362–410`) — covers FX-02 historical backfill
- [ ] Inngest cron unit test (mocks BoE response, asserts idempotent upsert) — covers FX-01 daily ingest
- [ ] `fx_rate_stale` alert email test (recipient routing, content snapshot) — covers FX-01 staleness alerting
- [ ] Analytics SQL audit fixture covering 41 SUM(netAmount) sites (snapshot of dual-emit shapes) — covers FX-03a
- [ ] Playwright spec: admin views analytics widget, asserts GBP totals match seeded fixture — covers FX-03 + FX-04 end-to-end against preview alias

*Planner will refine each Wave 0 line into a concrete file path + task and link it back here.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| BoE multi-year backfill streaming behaviour | FX-02 historical backfill | Live API behaviour with real network — flake-prone to automate; one-time operator-tested first fetch | Operator runs `npm run scripts/fx-backfill -- --from=2024-01-01 --dry-run` against staging Neon, confirms row counts and sample rates against BoE published data |
| Production migration apply (0046 / 0047 / 0048 sequence) | FX-01 / FX-02 schema | Destructive against prod; gated through standard migration flow per CLAUDE.md | Operator runs `npx drizzle-kit push` against prod `DATABASE_URL` after preview UAT passes |
| Visual UAT of GBP-vs-native renderer in production-shape preview | FX-03b | Visual judgement: tooltip wording, currency-pair display, cross-currency aggregate readability | Operator opens preview alias, navigates analytics dashboards listed in 09.1-CONTEXT.md, screenshots each widget |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all 12 test gaps in RESEARCH.md `## Validation Architecture`
- [ ] No watch-mode flags
- [ ] Feedback latency < 60s for quick run
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
