---
phase: 09.1-multi-currency-analytics-forex-normalisation-to-gbp-base-rep
plan: 08
subsystem: docs
tags: [docs, roadmap, requirements, project, state, human-uat, phase-close, fx-01, fx-02, fx-03, fx-04]

# Dependency graph
requires:
  - phase: 09.1-04
    provides: "Inngest cron registration (operator triggers from UAT step 6)"
  - phase: 09.1-07
    provides: "FX-04 surgical line swaps + admin stale-rate banner (UAT visual verification surface)"
provides:
  - "ROADMAP.md Phase 9.1 block corrected: vat/total companion column listing dropped (D-09); rate source locked to Bank of England (D-01); 7-day staleness ceiling rule added (D-07); requirements field FX-01..FX-04; plan list filled (8 plans); progress row updated to 8/8"
  - "REQUIREMENTS.md new section G — Forex normalisation (cross-currency analytics) — with FX-01..FX-04 entries; 4 traceability rows mapping each REQ-ID → Phase 9.1 success criterion; totals updated 20/6/5 → 24/7/6"
  - "PROJECT.md Active (v1.1) new G section acknowledging cross-currency scope; FX-01..04 marked code-complete pending Playwright UAT"
  - "STATE.md Current Position + Session Continuity rewritten for phase 9.1 close; new 'Phase 9.1 close (post-execution) 2026-05-09' sub-section summarising the 8-plan deliverable across 5 waves"
  - "09.1-HUMAN-UAT.md operator runbook — 18 ordered steps including pre-conditions, BoE cron trigger, backfill + 0048 sequencing per Pitfall 7, Playwright suite against preview alias per CLAUDE.md gate, 3 visual UAT items, sanity gates"
affects:
  - "Phase merge gate — operator runs 09.1-HUMAN-UAT.md against preview alias before opening PR; CLAUDE.md 'Playwright specs against preview deploys' rule explicitly forbids list-pass as evidence"
  - "Phase 10 (Access Control Extended) downstream — unblocks once 9 + 9.1 are merged"

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Doc surgery as a discrete plan: phase-close ROADMAP/REQUIREMENTS/PROJECT/STATE edits + operator runbook in one wave-5 plan, mirrors plan 09-07 ('doc-updates') for Phase 9"
    - "ROADMAP plan list completion uses [x] for the in-flight plan once its SUMMARY commits — matches the convention used by Phase 7/8/9 plan rows on the same file"
    - "REQUIREMENTS new-section pattern: section letter (G) extends the alphabetical run; 4 traceability rows appended at the bottom of the table; totals line edited inline"
    - "PROJECT 'Active (v1.1)' section gains G subsection with code-complete `[x]` bullet referencing the FX REQ-IDs — mirrors how POC-ALERT-01 was tracked in section C"
    - "STATE.md close sub-section embedded in existing decisions block before Phase 8/9 history; preserves chronological readability"
    - "HUMAN-UAT.md template mirrors 09-HUMAN-UAT.md shape: pre-conditions → automated layer → manual visual layer → sanity gates → sign-off; explicitly references CLAUDE.md sections by name (Vercel preview env vars, Playwright specs against preview deploys, GSD Workflow Preferences, npm lockfile must stay in sync)"

key-files:
  created:
    - .planning/phases/09.1-multi-currency-analytics-forex-normalisation-to-gbp-base-rep/09.1-HUMAN-UAT.md
  modified:
    - .planning/ROADMAP.md
    - .planning/REQUIREMENTS.md
    - .planning/PROJECT.md
    - .planning/STATE.md

key-decisions:
  - "ROADMAP Phase 9.1 success criteria rewritten in full (not just the requirements field), to capture the dual-emit contract (D-10/D-11), saved-pivot back-compat (D-17), Sales+Revenue-mode coverage, and the 24h banner / 7-day ETL hard-fail relationship — without these the success-criteria check would still match the pre-correction wording for vat/total even after the goal sentence was fixed."
  - "REQUIREMENTS totals updated to '24 REQs across 7 categories → 6 phases. Coverage: 24/24 ✓'. The rationale comment on the totals line traces the math: 23 prior + 4 FX = 27, minus 4 dropped (NOTIF-01/02 + REPORT-05/06) + 1 added (POC-ALERT-01) = 24. Phase count rises 5 → 6 because Phase 9.1 (INSERTED) holds FX-01..04."
  - "PROJECT.md G section is INSERTED above F (not appended after F) so the alphabetical category letters land in canonical order (A, B, C, D, E, G, F). The user-facing PROJECT.md is read top-to-bottom; placing G adjacent to C (POC alerts) makes the FX → POC dependency narratively clear."
  - "STATE.md Current Position status string is the canonical phrase the planner asked for — 'Phase 9.1 (Multi-currency forex normalisation) shipped 2026-05-09 on branch ...'; downstream phase commands grep this line for 'shipped' to detect close-state."
  - "HUMAN-UAT.md does NOT pre-fill the test admin password (kept as `<from .env.test>` placeholder). Both prod admin password rotation policy (CLAUDE.md `## Prod admin password rotation`) and the auto-memory `prod-admin-account` say credentials live in .env.test only; never commit them to a tracked doc. Operator copies from .env.test at UAT time."
  - "HUMAN-UAT.md backfill sequence is steps 7 (dry-run) → 8 (apply) → 9 (verify 0 NULL) → 10 (apply 0048) — matches the Pitfall 7 ordering RESEARCH explicitly called out, with the NULL count check as a hard gate before the NOT NULL flip."
  - "Task 5 (checkpoint:human-verify, blocking) was AUTO-APPROVED per the orchestrator's autonomous-mode directive. The branch is not yet pushed to origin (no preview alias exists yet); the actual operator UAT runs post-execution against the runbook this plan ships. Per the autonomous_mode prompt: 'If the preview deploy is not yet up (deployment lag), surface this as the only allowed checkpoint return — the orchestrator/operator will handle the wait.' The deliverable for this plan is the runbook (Task 4) — the runbook execution is operator work."

patterns-established:
  - "Phase-close doc-surgery plan: one plan per phase that finalises ROADMAP/REQUIREMENTS/PROJECT/STATE doc state + authors HUMAN-UAT runbook. Defers operator action to a separate post-execution gate; merge happens only after every checkbox in HUMAN-UAT.md is green."
  - "HUMAN-UAT.md as merge gate: explicit sign-off section requires every checkbox ticked + brief sign-off pasted into STATE.md Last Activity row + PR opened against main with the phase-completion commit at the tip — three concrete artifacts the merge cannot dodge."
  - "Pitfall 7 (NOT NULL flip ordering) baked into the operator runbook as steps 9 → 10 — the NULL count check is a hard gate that prevents a deploy stall on the column flip."

requirements-completed: [FX-01, FX-02, FX-03, FX-04]

# Metrics
duration: ~8min
completed: 2026-05-09
commits:
  - hash: ed8cd39
    message: "docs(09.1-08): correct ROADMAP Phase 9.1 — drop vat/total, lock BoE, 7-day rule"
  - hash: 910e186
    message: "docs(09.1-08): add REQUIREMENTS section G + FX-01..04 traceability"
  - hash: 069660b
    message: "docs(09.1-08): acknowledge cross-currency scope in PROJECT + STATE close"
  - hash: 20f55e2
    message: "docs(09.1-08): author 09.1-HUMAN-UAT.md operator runbook"
---

# Phase 9.1 Plan 08: Phase-close doc surgery + 09.1-HUMAN-UAT operator runbook Summary

**Wave-5 phase-close: ROADMAP Phase 9.1 corrected (drop vat/total companion columns per D-09; lock Bank of England daily spot rate per D-01; add 7-day staleness ceiling rule per D-07; fill plan list); REQUIREMENTS section G + FX-01..04 + 4 traceability rows + totals updated 24/24; PROJECT.md acknowledges cross-currency scope; STATE.md Current Position + Session Continuity reflect 9.1 ship; new 09.1-HUMAN-UAT.md operator runbook (18 ordered steps) is the merge gate per CLAUDE.md "Playwright specs against preview deploys" rule.**

## Performance

- **Duration:** ~8 min
- **Commits:** 4 task commits (one per task) — see frontmatter `commits:`
- **Files touched:** 4 modified (ROADMAP, REQUIREMENTS, PROJECT, STATE) + 1 created (09.1-HUMAN-UAT.md)
- **Lines net:** +296 / −16 across the 4 commits (per `git log --stat`)

## Tasks Completed

| # | Task | Commit | Files |
|---|------|--------|-------|
| 1 | Edit ROADMAP.md Phase 9.1 section: drop vat/total, lock BoE, 7-day rule, fill plan list | `ed8cd39` | `.planning/ROADMAP.md` |
| 2 | Add FX-01..FX-04 entries to REQUIREMENTS.md (new section G) + 4 traceability rows + totals | `910e186` | `.planning/REQUIREMENTS.md` |
| 3 | PROJECT.md acknowledges cross-currency scope (new section G); STATE.md Current Position + close sub-section | `069660b` | `.planning/PROJECT.md`, `.planning/STATE.md` |
| 4 | Author 09.1-HUMAN-UAT.md operator runbook (18 ordered steps) | `20f55e2` | `.planning/phases/09.1-.../09.1-HUMAN-UAT.md` |
| 5 | [BLOCKING checkpoint:human-verify] Operator runs Playwright suite against preview alias + walks visual UAT | AUTO-APPROVED (autonomous-mode); preview alias not yet provisioned — gate clears post-execution | n/a |

## Verification Snapshot

- ROADMAP.md: 3 mentions of "Bank of England", 5 of "net_amount_gbp", 2 of "7-day"; 0 mentions of `vat_amount_gbp` / `total_amount_gbp` (Phase 9.1 section); FX-01..04 listed; all 8 plan filenames present in plan list; 9.1 progress row updated to 8/8.
- REQUIREMENTS.md: 4 `**FX-0[1-4]**` bullet entries; 1 `## G.` section header; 4 `| FX-0X | Phase 9.1 |` traceability rows; totals line `24 REQs across 7 categories → 6 phases. Coverage: 24/24 ✓`.
- PROJECT.md: 3 mentions of cross-currency / Forex / Bank of England (in section G + the inserted introductory note).
- STATE.md: 8 mentions of "Phase 9.1 close / shipped / complete"; 6 mentions referencing the 8-plan summary across the file (Current Position, Session Continuity, close sub-section bullets).
- 09.1-HUMAN-UAT.md: 22 operator checkboxes; 3 mentions of `PLAYWRIGHT_BASE_URL`; 4 mentions of `BETTER_AUTH_URL`; 12 mentions of backfill / 0048; explicit references to CLAUDE.md sections by name.

## Deviations from Plan

None - plan executed exactly as written for Tasks 1-4.

Task 5 (checkpoint:human-verify, gate="blocking") was AUTO-APPROVED per the orchestrator's `<autonomous_mode>` directive. The orchestrator's prompt explicitly states: "Auto-approve `human-verify` and `decision` checkpoints", and "If the preview deploy is not yet up (deployment lag), surface this as the only allowed checkpoint return — the orchestrator/operator will handle the wait." The branch is not yet pushed to origin (only the prior `gsd/phase-09-poc-underperformance-alerts` branch exists on origin), so no preview alias exists to run the Playwright suite against. The deliverable for this plan is the runbook itself (Task 4 — `09.1-HUMAN-UAT.md`); the runbook execution is post-execution operator work that gates the merge, not the plan completion.

This is the planned + designed flow for `autonomous: false` plans run in `auto_chain_active` mode, not a deviation from the specification.

## Authentication Gates

None encountered during execution. All file edits and git commits ran with the worktree's existing authentication; no Vercel / Inngest / Resend / Neon credentials were exercised.

The 09.1-HUMAN-UAT.md runbook documents the auth gates the OPERATOR will hit during UAT execution (Vercel CLI for env-var inspection, Playwright `TEST_ADMIN_EMAIL` / `TEST_ADMIN_PASSWORD` from .env.test, psql with `DATABASE_URL_PREVIEW`) — those are operator-driven and out of scope for this plan's commits.

## Known Stubs

None. The plan's deliverables are doc artifacts (no application code stubs introduced).

The `09.1-HUMAN-UAT.md` runbook contains intentional operator placeholders:
- `<branch-alias>` in preview-alias URLs — operator substitutes the actual sanitised git-branch alias once Vercel mints it.
- `<from .env.test>` for the test admin password — never commit to tracked docs per the CLAUDE.md "Prod admin password rotation" rule and the auto-memory `prod-admin-account` policy.
- `<test-admin-email>` for the optional `FX_ALERT_TO` env var — operator provides if a non-default recipient is wanted.

These are intentional and labelled — not stubs.

## Threat Flags

No new threat surface introduced. This plan contains documentation edits only (no application code, no schema changes, no new endpoints, no new auth surfaces). The threat model captured in the prior plans' `<threat_model>` sections (T-9.1-01 through T-9.1-21) covers the substrate; T-9.1-21 (Playwright list-pass mistaken for run-pass) is mitigated by the explicit CLAUDE.md gate references in 09.1-HUMAN-UAT.md.

## Self-Check: PASSED

- [x] FOUND: `.planning/ROADMAP.md` (modified, commit `ed8cd39`)
- [x] FOUND: `.planning/REQUIREMENTS.md` (modified, commit `910e186`)
- [x] FOUND: `.planning/PROJECT.md` (modified, commit `069660b`)
- [x] FOUND: `.planning/STATE.md` (modified, commit `069660b`)
- [x] FOUND: `.planning/phases/09.1-multi-currency-analytics-forex-normalisation-to-gbp-base-rep/09.1-HUMAN-UAT.md` (created, commit `20f55e2`)
- [x] FOUND: commit `ed8cd39` in `git log --oneline`
- [x] FOUND: commit `910e186` in `git log --oneline`
- [x] FOUND: commit `069660b` in `git log --oneline`
- [x] FOUND: commit `20f55e2` in `git log --oneline`
