---
context: phase
phase: 09-poc-underperformance-alerts
task: 28
total_tasks: 34
status: in_progress
last_updated: 2026-05-09T14:23:20Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: kiosk vs hotel granularity drift — the cron, schema, silencing UI, and tests are all mid-flight between kiosk-level (legacy) and hotel-level (target). Touching ANY one without a coordinated sweep of the others will leave the codebase in a half-state where vitest reds give a misleading signal. Proceed in the order written under `<remaining_work>` and only commit at logical seams (post-cron-rewrite, post-UI-move, post-test-rewrite).

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Trusting vitest greens mid-refactor | The kiosk→location refactor breaks the alert classifier, cron, and integration tests in a coordinated way. Running vitest after only the schema + classifier changes (current state) will compile-error or red on `kioskPerformanceAlertState` references. A green run only proves something on this refactor AFTER tasks 28–31 are all complete. | blocking | Do not commit "fix tests" patches until cron, email, UI, and integration tests are all rewritten. The intermediate fail is expected. |
| Re-implementing percentile rank in two places | `percentRanks` is duplicated in `src/lib/analytics/queries/heat-map.ts` and `src/lib/performance-alerts/classify-locations.ts`. Drift will silently produce a different composite score in the email vs the dashboard. | advisory | If a third caller appears, hoist `percentRanks` to a shared module (`src/lib/analytics/percent-ranks.ts`) and import in both. For two callers the duplication + comment-noting is acceptable. |
| Hardcoded ScoreWeights | The cron's `DEFAULT_COMPOSITE_WEIGHTS` constant in `classify-locations.ts` mirrors `DEFAULT_SCORE_WEIGHTS` in `src/lib/analytics/queries/heat-map.ts`. If the heat-map weights are ever moved into `app_settings` or a UI control, the alert classifier will silently keep using the old values. | advisory | The migration 0045 row `composite_score_alert_weights_snapshot` exists as documentation. If weights become runtime-tunable, change classify-locations.ts to read the live values + drop the constant. |

<current_state>
Mid-refactor of Phase 9 from kiosk-level alerts to hotel-level alerts (composite score + sub-metrics + weights footnote, sourced from heat-map dashboard logic). User-paused after 2 of 9 refactor tasks landed. The branch on origin (`gsd/phase-09-poc-underperformance-alerts`, last commit `c666188`) is GREEN; the local working tree has the schema + classifier work staged but NOT committed.

PR #38 is open against `main` and currently passing CI on commit `c666188` — do NOT merge while this refactor is in flight; the local schema removes `kioskPerformanceAlertState` and the cron at HEAD still imports it.
</current_state>

<completed_work>

Completed (this session, post-c666188):
- Migration `migrations/0045_phase_09_hotel_alerts.sql` authored + applied to dev DB. Adds `location_performance_alert_state` (PK location_id, tier, composite_score, classified_at, last_run_at, last_alerted_at), adds `locations.alert_silenced_at` + `alert_silenced_reason`, drops `kiosk_performance_alert_state`, drops `kiosks.alert_silenced_*`, seeds `app_settings.composite_score_alert_weights_snapshot`.
- `src/db/schema.ts` — kiosks silencing cols removed (lines 144-148 area); locations silencing cols added (post line 242); `kioskPerformanceAlertState` table replaced with `locationPerformanceAlertState` (location_id PK, +`compositeScore` numeric(6,2)).
- `src/lib/performance-alerts/classify-locations.ts` — new file. `classifyEligibleLocations()` returns `{ rows, tierConfig, windowDays, liveStageId, weights }` with a per-row `subMetrics` map (revenue, transactions, revenuePerRoom, txnPerKiosk, basketValue) each carrying `{ value, percentile }`. Composite score is `calculateCompositeScore` over the 5 percentiles using DEFAULT_COMPOSITE_WEIGHTS (mirrors heat-map's DEFAULT_SCORE_WEIGHTS). Tier classified via existing `classifyOutletTier(composite, tierConfig)`. SQL is one statement using LATERAL joins for kiosk_count + first_live_at + revenue/transactions/modal-currency aggregation; eligibility filter excludes archived, silenced, internal locations, locations without a live kiosk, and ramp-up sites (<90d since first kiosk assignment) AND no-data locations (zero in-window transactions).
- `src/lib/performance-alerts/poc-batching.ts` — `ClassifiedKiosk` constraint relaxed: only `internalPocId` is required, `kioskId` is now optional (covered by `[k: string]: unknown`). Lets `ClassifiedLocationRow` flow through `groupByPoc` without a parallel implementation. Existing tests still pass because they construct objects that satisfy the looser constraint.
</completed_work>

<remaining_work>

In order — do NOT skip ahead, each step depends on the previous:

1. **Cron rewrite (`src/inngest/functions/weekly-poc-alerts.ts`)** — IN PROGRESS, file fully read but body unchanged.
   - Replace `classifyEligibleKiosks` import with `classifyEligibleLocations` (path: `@/lib/performance-alerts/classify-locations`).
   - Replace `kioskPerformanceAlertState` references with `locationPerformanceAlertState` in the imports + the diff-state and write-state steps.
   - Replace `kioskIds` / `kioskId` with `locationIds` / `locationId` in the diff-state lookup.
   - In write-state, also persist `compositeScore` (the new column on `locationPerformanceAlertState`).
   - In emit-poc-emails: build `templateProps` with hotel-level fields (see `<next_action>`).
   - Subject line: `These hotels are underperforming — N hotel${N!==1?'s':''} need attention` (or similar — use the `runIsoWeek` for the date stamp).
   - In emit-skip-rows: `skipKiosks` → `skipLocations`; iterate by index (the `void k` smell removal in c666188 already happened).
   - Cron's `entityType: "performance_alert_run"` audit row stays — that's the cron-completion row.

2. **Email template rewrite (`src/emails/poc-underperformance.tsx` + `src/emails/text-versions.ts`)**:
   - Subject + body header switch from "kiosks" to "hotels".
   - `KioskRow` → `HotelRow` with these fields: `hotelName`, `region`, `currency`, `totalRevenue` (formatted string), `totalTransactions`, `kioskCount`, `numRooms`, `salesPerRoom` (formatted string or "—"), `compositeScore` (number), `subMetricPercentiles` (5 numbers), `detailUrl` pointing to the location detail page.
   - Body table: hotel name (link) | region | composite score | sales | sales/room | 5 sub-metric pcts.
     OR a tighter two-row layout (hotel name + region + composite score on row 1; sub-metrics tabular on row 2). Decide based on render width — the existing 4-col layout barely fits at 560px. A two-row-per-hotel pattern is probably cleaner.
   - Sticky footnote: `Composite = revenue 30% · transactions 20% · revenue/room 25% · txn/kiosk 15% · basket value 10%` plus a one-liner explaining "each metric ranked by percentile across all WeKnow hotels in this window".
   - Plain-text mirror in `text-versions.ts` — same fields, ASCII tabular layout.
   - Snapshot test (`src/emails/__tests__/poc-underperformance.test.tsx`): update fixtures + regenerate the snapshot. Existing 9 inline assertions need to be reviewed against the new field names.

3. **Silencing UI move**:
   - Move `src/app/(app)/kiosks/[id]/kiosk-admin-panel.tsx` → `src/app/(app)/locations/[id]/location-admin-panel.tsx`. Rewire all `kiosks.alert_silenced_*` references to `locations.alert_silenced_*`.
   - Update server actions in the same directory or under `src/app/(app)/locations/silence-actions.ts` (mirror existing kiosk-side action layout).
   - Audit log: `entityType: "location"` (already in the union), `action: "silence_alerts" | "unsilence_alerts"` (already in the union). The `kiosk` audit_log entries continue to exist for historical records but no new ones land.
   - Remove `KioskAdminPanel` rendering from `src/app/(app)/kiosks/[id]/page.tsx`.
   - Add `LocationAdminPanel` rendering to `src/app/(app)/locations/[id]/page.tsx`, gated by `session.user.role === "admin"` (mirror the kiosk-page pattern).

4. **Test refactor**:
   - `tests/performance-alerts/_seed.ts` — currently seeds 10 kiosks + sales records, structured for kiosk-level classifier. Rewrite to seed N locations (with rooms), each with M kiosks, with sales totals chosen so the composite score lands them deterministically across tiers. Calibrate so 1–2 land in Emerging.
   - `tests/performance-alerts/eligibility.integration.test.ts` — assertions about which kiosks are alertable shift to which hotels.
   - `tests/performance-alerts/null-poc-skip.integration.test.ts` — null POC is now `locations.internal_poc_id`, not `kiosks.internal_poc_id`.
   - `tests/performance-alerts/idempotency.integration.test.ts` — payloadHash key still `(poc_user_id, run_iso_week)` so logic is unchanged; assertions on which entities flow through need updating.
   - `tests/kiosks/silence-toggle.integration.test.ts` → `tests/locations/silence-toggle.integration.test.ts`. Update the action import path.
   - `tests/kiosks/silence.spec.ts` → `tests/locations/silence.spec.ts`. Navigate to `/locations/<id>` not `/kiosks/<id>`. The first-row-of-table click pattern needs updating because the locations table column layout differs.

5. **Verify**: `rtk proxy npx vitest run --no-coverage src/ tests/` — expect green. `rtk proxy npx tsc --noEmit` — expect green.

6. **Apply 0045 to preview Neon DB**:
   ```bash
   DB=$(grep ^DATABASE_URL .env.preview | cut -d= -f2- | sed 's/^"//;s/"$//')
   psql "$DB" -v ON_ERROR_STOP=1 -f migrations/0045_phase_09_hotel_alerts.sql
   ```

7. **Re-render email screenshot**: update `scripts/uat-render-emails.tsx` POC fixture to hotel-shape + run; replace `.planning/phases/08-email-infrastructure/uat-artifacts/uat-email-poc-underperformance.png`.

8. **Commit + push** in atomic chunks:
   - Commit A: schema + migration 0045 + drizzle types
   - Commit B: classify-locations.ts + poc-batching constraint relax
   - Commit C: cron rewrite
   - Commit D: email template rewrite + snapshot
   - Commit E: silencing UI move
   - Commit F: test refactor + seed
   - Push all to `origin/gsd/phase-09-poc-underperformance-alerts`. PR #38 picks up the new commits automatically.
</remaining_work>

<decisions_made>

- Hotel-level alerts (full shift) over kiosk-level-with-hotel-framed-copy. Drove the schema move, silencing UI move, and seed rewrite. Confirmed via AskUserQuestion in this session.
- Trailing 30d window, mature-only (≥90d since first kiosk assignment), no dimension filters. Mirrors existing kiosk classifier scope; the 90-day maturity gate is new (kiosk classifier had no maturity filter).
- Tier-classify the composite score (0–100) directly using the existing `OutletTierConfig` (default top=80/mid=50/bottom=20). Avoids introducing a parallel cutoff knob; the dashboard's `tierConfig` continues to be the single source of truth.
- Per-row composite + 5 sub-metric percentiles + sticky weights footnote in the email body (option A from the email-content question). Inline formula per row was rejected as too heavy; minimal-only was rejected as too thin.
- Snapshot the composite weights in `app_settings.composite_score_alert_weights_snapshot` as a documentation row. Runtime still reads `DEFAULT_COMPOSITE_WEIGHTS` from code; the snapshot is for audit-log readability.
- Relax `poc-batching.ts` `ClassifiedKiosk` constraint to require only `internalPocId`. Lets `ClassifiedLocationRow` flow through `groupByPoc` without a parallel impl. Field name `kiosks` is retained on `PocGroup` for backwards-compat with existing tests.
- Forex normalisation is OUT OF SCOPE for this refactor (tracked in #39). The percentile rank still compares raw revenue across currencies — fine for the all-GBP fixture but a real correctness gap on the multi-currency horizon.
</decisions_made>

<blockers>
- None active. The cron rewrite is the next clean step; no external dependency, no human action required mid-flight.
</blockers>

## Required Reading (in order)
1. `.planning/HANDOFF.json` — machine-readable state + uncommitted_files + next_action
2. `.planning/phases/09-poc-underperformance-alerts/09-HUMAN-UAT.md` — what the previous (kiosk-level) acceptance evidence looks like; mirrors what the new hotel-level UAT needs to capture
3. `migrations/0045_phase_09_hotel_alerts.sql` — schema source of truth for the new model
4. `src/lib/performance-alerts/classify-locations.ts` — the new classifier the cron must call
5. `src/lib/analytics/queries/heat-map.ts` — composite-score reference impl; `DEFAULT_SCORE_WEIGHTS` is the source the snapshot row in `app_settings` was taken from
6. `CLAUDE.md` § "Vercel preview env vars" — preview alias rules apply to the next deploy after this refactor merges
7. `CLAUDE.md` § "Playwright specs against preview deploys" — silence.spec.ts in its new home must be re-run against the preview alias before the PR is considered ready

## Critical Anti-Patterns (do NOT repeat these)
- ANTI-PATTERN: Running vitest mid-refactor and treating reds as new bugs → expect kioskPerformanceAlertState references to be unresolvable until the cron is rewritten (task 28). Treat reds as confirmation of expected breakage, not failure of the refactor itself.
- ANTI-PATTERN: Renaming `g.kiosks` → `g.locations` in poc-batching → would cascade to every test/integration touching it. Field name `kiosks` is preserved on `PocGroup`; semantically read it as "items grouped per POC".
- ANTI-PATTERN: Hardcoding the 90-day maturity gate elsewhere → the constant lives ONLY in `classify-locations.ts` (`MATURITY_DAYS`). If a second consumer ever needs the same gate, hoist to `app_settings` (e.g. `alert_maturity_min_days`) — do not duplicate the literal.

## Infrastructure State
- **Dev DB** (`DATABASE_URL` in `.env.local`): migration 0045 applied. `location_performance_alert_state` exists with 0 rows (no cron has run since the migration). `locations.alert_silenced_at` exists, NULL across the board. `kiosks.alert_silenced_at` and `kiosk_performance_alert_state` are gone.
- **Preview DB** (`DATABASE_URL` in `.env.preview` — Neon `ep-soft-breeze-abhk62iq.eu-west-2.aws.neon.tech`): NOT yet migrated. Still on the kiosk-level schema. Task 33 applies 0045 there before re-running the screenshot script.
- **Preview deploy**: alias `wkg-command-centre-git-gsd-p-01eb46-vedant-kalbag-wkgs-projects.vercel.app` is live on commit `c666188` (kiosk-level). The next push will trigger a fresh deploy on the new shape.
- **PR #38**: open against `main`, status green on `c666188`. Two passing checks: Vercel deploy + GitHub Actions test job. DO NOT merge until this refactor commits land.
- **Issue #39**: filed for forex normalisation; classify-locations.ts comment links to it.

## Pre-Execution Critique Required
- (not applicable — this is a code-side refactor, not a design-vs-execution gate)

<context>
The refactor was triggered by user feedback on PR #38: the alert email should reframe at hotel level using the dashboard's default composite score, with sub-metric percentiles + weights formula visible. The user explicitly chose "Hotel-level alerts (full shift)" + "trailing 30d, mature-only" + "tierConfig.bottom cutoff" + "per-row metrics + sticky weights footnote".

I scoped the work into 9 tasks (#26–34 in the task list); 2 are done, 7 remain. The schema + classifier are the load-bearing changes; the cron + email + UI are the wiring; the tests are the verification. The mental model: heat-map's `getHeatMapData` is the dashboard's source of truth, and `classify-locations.ts` was deliberately written to mirror its DEFAULT_SCORE_WEIGHTS + percentile-rank methodology rather than call it directly — getHeatMapData expects an `AnalyticsFilters` + `UserCtx` shape that doesn't fit a system cron context cleanly (it scopes by user). The duplication is intentional and the comment in classify-locations.ts notes the hoisting pattern if a third consumer appears.

The cron is the next step. The body of `_handleWeeklyPocAlerts` is large but the changes are mechanical — every `kiosk` becomes `location`, every `kioskId` becomes `locationId`, the templateProps grow to include hotel-level fields. The 7-step structure (load-config → diff-state → first-run detect → write-state → emit-poc-emails → emit-skip-rows → write-run-audit) stays.
</context>

<next_action>
1. Edit `src/inngest/functions/weekly-poc-alerts.ts` — change imports (`classifyEligibleLocations` from `@/lib/performance-alerts/classify-locations`; `locationPerformanceAlertState` from `@/db/schema`). Drop `ClassifiedKioskRow` import.
2. In `_handleWeeklyPocAlerts`, replace every `kioskPerformanceAlertState` with `locationPerformanceAlertState`, every `kioskId` with `locationId`. Update the write-state step to also persist `compositeScore: d.compositeScore.toString()` (numeric column expects a string in Drizzle).
3. In emit-poc-emails, build new templateProps shape:
   ```ts
   templateProps: {
     pocName: u.name ?? "there",
     hotels: truncated.map((row) => ({
       hotelName: row.hotelName,
       region: row.region,
       currency: row.currency,
       totalRevenue: formatRevenueForKiosk(row.totalRevenue, row.currency),
       totalTransactions: row.totalTransactions,
       kioskCount: row.kioskCount,
       numRooms: row.numRooms,
       salesPerRoom: row.numRooms ? formatRevenueForKiosk(row.totalRevenue / row.numRooms, row.currency) : "—",
       compositeScore: row.compositeScore,
       subMetricPercentiles: {
         revenue: Math.round(row.subMetrics.revenue.percentile ?? 0),
         transactions: Math.round(row.subMetrics.transactions.percentile ?? 0),
         revenuePerRoom: row.subMetrics.revenuePerRoom.percentile === null ? null : Math.round(row.subMetrics.revenuePerRoom.percentile),
         txnPerKiosk: row.subMetrics.txnPerKiosk.percentile === null ? null : Math.round(row.subMetrics.txnPerKiosk.percentile),
         basketValue: Math.round(row.subMetrics.basketValue.percentile ?? 0),
       },
       detailUrl: `${BRAND.prodUrl}/locations/${row.locationId}`,
     })),
     moreCount,
     windowDays: classification.windowDays,
     runIsoWeek,
     portfolioUrl: `${BRAND.prodUrl}/analytics/heat-map`,  // heat-map page is the canonical "see why these are bottom-tier" destination
     bottomPercentile: classification.tierConfig.bottom,
     weights: classification.weights,  // for the footnote
   }
   ```
4. Update the subject line: `These hotels are underperforming — ${sortedRows.length} hotel${sortedRows.length === 1 ? "" : "s"} need attention`.
5. Sort by `compositeScore` ASC (lowest score first → most-underperforming at the top of the email).
6. Then move to step 2 of `<remaining_work>` (email template rewrite).
</next_action>
